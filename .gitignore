__pycache__/
*.pyc
.DS_Store
.~*

# 真实经营数据永不进库（“真实数据不出本机”）。
# 部署时把真实数据放进 数据/ 覆盖同名文件即可，git 不会带走它。
# 想试跑：用 数据_样例/ 里的合成样例数据（见 数据/README.md）。
数据/*.xlsx

# 生成物：每次 run.py 都会重新生成，无需进库（也含真实数字，别外泄）
output/
data/驾驶舱数据.json

# 样例/参考数据也不上库（“样例”实为真实行改造，含真实客户/员工名——2026-07-06教训）
数据_样例/
docs/参考/
