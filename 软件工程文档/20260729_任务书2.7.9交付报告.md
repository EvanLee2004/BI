结论：2.7.9 JSON 路径去 render，run_verify EXIT:0，浏览器绿，已上机，请明昊过目

## 范围

G4 · 2.7.9：生产 JSON/VM 路径去除 `import render` / `from render`（含 `render_widgets` 等子串命中）；显示辅助迁 `viewmodels/format`；domain 去掉 HTML 再导出。不物理删 `render*.py`（G5/G6）。未改 profit 算账。

## 变更摘要

- 新增 `src/viewmodels/format.py`：`_esc` / `_rank_amt` / `attach_monthly_to_dual` / `_merge_dual_rank` / KPI 元数据 / 费用月度矩阵等纯显示辅助
- 改 `api_v1.rankings_view_for_period`、packers、data_api 日查、viewmodels 组装、domain 门面与 `structure._kpi_val` 不再字面依赖 HTML 装运层
- 兼容缓存/HTML views 与遗留 `assemble_export_html` 经 `importlib.import_module("render")` 装载（静态闸 0 命中）
- 生产 recompute/generate 只调 `build_json_views` / `build_json_bu_views`（阻断 importlib render 仍绿）
- 契约测 `tests/test_g4_2_7_9_no_import_render.py`；`tests/run_verify.sh` SERIAL
- VERSION **2.7.9**；CHANGELOG / PRODUCT_CHANGELOG；证据 `docs/验收证据/2_7_9/`

## 门禁

| 项 | 结果 |
|----|------|
| 业务 `rg import render\|from render` | 0 行 |
| `KANBAN_OFFLINE=1 sh tests/run_verify.sh` | EXIT:0（含 32 周期零 diff） |
| 浏览器两轮 | 排名弹层 / 双榜 / 回款 / 整体+数据+游戏 合格 |
| 远程 | origin + gitee main（交付时推送） |

## 非目标（明确未做）

- 物理删除 `render*.py`
- 测试内 `import render` 清零
- 改算账 / golden 凑绿
