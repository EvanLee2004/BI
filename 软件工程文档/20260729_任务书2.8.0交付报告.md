结论：2.8.0 测试已迁出 HTML SHA 锁，run_verify EXIT:0，请明昊过目

## 范围
G5 · 2.8.0：废除 `render_pl_table` 整段 HTML SHA 架构锁；改为 `pl_structure` / packers / extract_numbers 数字与 `*_disp` 契约。未改 profit 算账。

## 变更
- 删除 `tests/fixtures/pl_table_year_sha.txt` 与 `test_render_pl_table_sha_golden`
- 新增 `tests/test_g5_2_8_0_pl_structure_contract.py` 进 run_verify SERIAL
- VERSION 2.8.0；CHANGELOG / PRODUCT_CHANGELOG

## 门禁
- `rg sha256|pl_table_year_sha|render_pl_table_sha tests/`：无有效架构锁
- `KANBAN_OFFLINE=1 sh tests/run_verify.sh` → EXIT:0
- 浏览器冒烟：整体 + ≥1 BU 非空
