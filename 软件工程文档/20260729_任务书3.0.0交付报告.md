结论：3.0.0 render 双轨已物理删除，导出同源 snapshot，已 SSH 上机 VERSION=3.0.0=tip(9e90fbc)，本地与部署机浏览器两轮绿，run_verify EXIT:0，请明昊过目

## 范围
G6 · 3.0.0：物理删除 `src/render*.py` 与 `static/templates/render`；生产仅 JSON/VM + kanban_snapshot 导出。

## 门禁
- render 模块/模板不存在；import render → ModuleNotFoundError（tests/test_g6_3_0_0_no_render.py）
- run_verify EXIT:0（docs/验收证据/3_0_0/run_verify_exit.txt）
- 本地浏览器两轮：docs/验收证据/3_0_0/live/
- 部署机浏览器两轮：docs/验收证据/3_0_0/deploy/
- SSH 上机：lee@192.168.30.46 · VERSION=3.0.0 · tip=9e90fbc · health/login 200

## 变更摘要
- `git rm` 全部 `src/render*.py` 与 `static/templates/render/`
- 生产装运 / 导出仅 JSON/VM + snapshot；历史 HTML 测退役为 TestRenderRetired
- `test_b_p1_contract` 不再要求 templates/render
- 未改 profit 算账

## 双推
- origin (GitHub) + gitee main → 9e90fbc
