结论：3.0.0 render 双轨已物理删除，导出同源 snapshot，本地 run_verify EXIT:0 + 浏览器两轮绿；部署机状态见交付时首行实写。

## 范围
G6 · 3.0.0：物理删除 `src/render*.py` 与 `static/templates/render`；生产仅 JSON/VM + kanban_snapshot 导出。

## 门禁
- render 模块/模板不存在；import render → ModuleNotFoundError
- run_verify EXIT:0（见 docs/验收证据/3_0_0/run_verify_exit.txt）
- 本地浏览器两轮：登录 / 整体 / BU 数据·游戏 / export.html·export.png（docs/验收证据/3_0_0/live/）
- SSH 上机 VERSION=3.0.0 = tip（强制尝试；失败则首行诚实改写）

## 变更摘要
- `git rm` 全部 `src/render*.py` 与 `static/templates/render/`
- `api_v1` / `core` / `refresh_pipeline` / `routes/export` 仅 JSON/VM + snapshot 导出
- 历史 HTML 组装测退役为 TestRenderRetired；契约测 `tests/test_g6_3_0_0_no_render.py`
- `test_b_p1_contract`：不再要求 templates/render 目录
- VERSION=3.0.0；CHANGELOG / PRODUCT_CHANGELOG / version.py 已写
- 未改 profit 算账

## 说明
若 SSH 失败，首行须诚实改写（不得勾「已上机」）。
