# 任务书37 交付报告 · 功能与 UI 收口（明昊 0716 走查）

> 分支：`main` · 只 commit 不 push · 未碰 .bat / 部署手册 / gzip / 日历解析 / 2027 专属文件  
> 日期：2026-07-16

## 需求 × commit × 证据

| # | 需求 | Commit | 证据 |
|---|------|--------|------|
| A1 | 删管理端「改数后此看板会自动刷新」提示条 | `7e29ca3` | `tests/test_task37_ui.py` TestA1；admin.html/golden 无该串 |
| A2 | 双血条图例 上·紫=下单 / 下·青=回款 | `b556da1` | dual_card.html + rankings.js + theme；test A2（卡头+月度弹窗） |
| A3 | 下单/回款两卡 50/50 | 同 `b556da1` | `.dual-grid{minmax(0,1fr)×2}`；test A3 |
| A4 | 总下单/总回款周期标注 | `3d9cfc9` | `rc_totals` + `_receipt_insight_*`；test A4 |
| A5 | 人工填写 sticky 不挡行 | `b39766c` | admin.css chrome+toolbar top；test A5 |
| A6 | 业绩目标 H1 行组上移 | `bb1a82d` | BUDGET_METRICS 序；存储键不变；test A6 + test_budget 绿 |
| B7 | 六表列筛+导出跟随 | `5a2c17a` | `db.query_detail` filters + `/api/detail*`；test_task37_filters 14 例 |
| B8 | 整体费用明细+工资默隐 | `6feb05a` | hide_salary + overall_see_salary 覆盖层；test_task37_expense_perm 6 例 |
| B9 | 抓数降级黄横幅 | `d851a29` | `build_fetch_fallback_banners` + health.fetch_banners；看端/管理端 paint |
| B10 | 未来日期体检黄 | 同 `d851a29` | profit `_scan_future_dates_*`；test_task37_fetch_banner |
| 测试 | A1–A6 守卫 | `2b1ec41` | test_task37_ui.py |

## golden / 基线 diff 归因

| 变化 | 归因 |
|------|------|
| `golden/admin_baseline.html` 删自动刷新 note | A1 |
| 同文件加「清除列筛」/ colFilterPop / 工资开关 / fetchBanner | B7、B8、B9 |
| `dashboard_body` 增板块五费用明细 + fetchBanner | B8、B9 |
| dual_card / rc_totals / theme / rankings.js | A2–A4 |
| **未**改 `golden/baseline.html` 数字红线数据 | 本批不改算账；32 周期金额应不变 |

## 活体说明（无真浏览器时以接口+结构测试为 bar）

- 图例：结构断言 dual-legend 文案存在于服务端模板与 JS 组装器/月度弹窗。  
- 50/50：CSS dual-grid minmax。  
- 周期标注：Python 渲染注入 period_label。  
- sticky：CSS top=chrome+toolbar。  
- H1 序：BUDGET_METRICS 键序断言。  
- 六表筛：DB 层文本/数字/日期/多列 AND + HTTP 导出。  
- 姜总无工资：整体会话 hide_salary 默认；开关开后可见；管理员始终见。  
- BU 越权 403：既有 + B8 用例。  
- 降级横幅：桩 local_fallback → fetch_banners 非空。  

控制台零报错：本批前端仅 DOM 拼装/参数拼接，无金额运算（铁律2）。

## 验证命令

```bash
KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?
# 勿接 | tail
```

证据日志目录：goal scratch（ui_a / filter6 / expense_perm / fetch_banner / run_verify / git_process）。

## 禁区自检

- 无 `.bat` / Windows 部署手册 / gzip 中间件 / 日历解析核心 / 2027 专属路径改动。  
- 未 `git push`。
