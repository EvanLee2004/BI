# 任务书54 交付报告 · Vue SciFi 皮肤 + 删口径卡 + 去 Windows + 废码清理

- **日期**：2026-07-18
- **仓库**：`程序/看板正式程序/`
- **范围**：只 commit 不 push；tag `stage54`
- **红线**：数据/算法/32 周期零变化；VM 字段只加不删；vue 模式后端纯 JSON；legacy 壳保留

---

## 结论

| 阶段 | 结果 | 说明 |
|------|------|------|
| A 删口径卡 | ✅ | 管理端卡与 `/api/config/caliber*` 下线；引擎默认直通保留；MADR-0012 |
| B SciFi 视觉 | ✅ | vendor kit CSS（MIT）+ SciFiPanel + kit 派生 ECharts 主题；亮暗纯前端 |
| C 逐卡 + 截图 | ✅ | 费用面积→堆叠柱；`docs/pixel/vue54/` 亮暗×1440/375 + 逐卡 + legacy 并排≥4 |
| D 去 Windows | ✅ | 全部 `.bat` 删除；schtasks 分支删除；Linux cron/看门狗单线；退出码 42 保留 |
| E 废码 | ✅ | 6 个无引用模板删除；legacy/golden/测试基建未动 |
| F 收口 | ✅ | `run_verify` 见证据；交付本报告；tag stage54；服务可 `KANBAN_OFFLINE=1` :8018 |

---

## 截图证据行（`docs/pixel/vue54/`）

| 证据 | 文件 |
|------|------|
| 整体暗 1440/375 | `vue_overall_dark_1440.png` / `vue_overall_dark_375.png` |
| 整体亮 1440/375 | `vue_overall_light_1440.png` / `vue_overall_light_375.png` |
| BU 暗/亮 1440 | `vue_bu_dark_1440.png` / `vue_bu_light_1440.png` |
| 逐卡（整体暗） | `vue_overall_dark_{kpi,trend,pl,expense_donut,expense_trend,rank,receipts,ledger}.png` |
| 逐卡（整体亮） | `vue_overall_light_*.png` 同上 |
| 逐卡（BU） | `vue_bu_{dark,light}_*.png` |
| legacy 并排≥4 | `compare_legacy_{overall_dark_1440,kpi,trend,expense,pl}.png` + `compare_vue_*` |
| 脚本 | `tests/frontend/playwright_task54_pixel.py` |

费用月度趋势截图可见 **堆叠柱** 标签「按有数月份 · 堆叠柱」，非面积。

---

## 删除清单（grep 证据）

### 阶段 A
| 删什么 | 为何死码/下线 | 证据 |
|--------|---------------|------|
| `static/admin` 口径配置卡 + JS | 明昊拍板不需要可写 UI | admin 无 `setCardCaliber`/`caliberLoad` |
| `/api/config/caliber*` 路由 | 写面下线 | `config_engine_api.register` 空操作；测试 `TestCaliberApiOffline54` |
| `golden/admin_baseline.html` 卡块 | 唯一授权 golden 改动 | 与 admin.html 同步删卡 |

**保留**：`domain.config_engine` 内核 / seed / 表结构。

### 阶段 D
| 删什么 | 为何 | 证据 |
|--------|------|------|
| `*.bat` 五个 | 部署=Ubuntu | 根目录 `ls *.bat` → 无 |
| `docs/Windows部署手册.md`、`docs/周一部署checklist.md` | 纯 Windows | 文件删除 |
| `server._win_*` / schtasks 调用 | 平台单线 | `src/server.py` 无 `["schtasks"`；`sync_schedule` 仅 linux |

**保留**：退出码 42、`.update_rollback`、`deploy/linux/start_with_rollback.sh`。

### 阶段 E
| 删什么 | 为何 | 证据 |
|--------|------|------|
| `static/templates/render/dual_monthly.html` 等 5 个 + `partials/login_err.html` | 零引用 | 见 `docs/pixel/vue54/stage_e_deletes.txt` |
| 空 `src/docs/` | 空目录 | 已删 |

**禁删已守**：`static/` shell 全链、`render*`、`frontend=legacy`、golden、`数据/`、备份回滚、测试基建。

---

## 待拍板

1. 历史交付报告 / v1.4·v1.5 文档是否进归档夹（本轮未删）。
2. VM 中 `body_by_period` 等 deprecated HTML 字段何时从契约移除（55 号 legacy 删壳后再议）。
3. 像素主观「比 legacy 更好」需明昊人审 `docs/pixel/vue54/`。

---

## 验证

```text
sh tests/run_verify.sh; echo EXIT:$?   # 期望 EXIT:0
.venv/bin/python tests/run_test.py tests/test_task54_scifi.py
.venv/bin/python tests/run_test.py tests/test_config_engine.py
.venv/bin/python tests/run_test.py tests/test_schedule.py
.venv/bin/python tests/run_test.py tests/test_update.py
```

人审：`KANBAN_OFFLINE=1 KANBAN_FRONTEND=vue .venv/bin/python run.py --serve` → http://127.0.0.1:8018/

---

## Git

- 阶段 commit：`stage54-A` … `stage54-E`（message 前缀 feat/chore）
- tag：`stage54`（本轮不 push）
