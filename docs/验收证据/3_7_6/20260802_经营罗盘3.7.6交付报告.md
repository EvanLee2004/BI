# 经营罗盘 3.7.6 交付报告

> 日期：2026-08-02  
> 角色：EXECUTOR  
> 基线：`2255387`（3.7.5）→ **3.7.6**  
> 工位：`kanban-376-pl-export-expand` · 分支 `task/20260802-pl-export-expand-other`

## 一句话

A) 管理利润表 Excel 展开「其他 N 项」全部 children；B) 全站字号/按钮略放大（看板六大板块 + 管理端壳/密表 small）；C) 未单独加装饰项（B 内已含 KPI 周期 tag 弱化与 padding 微调）。

## A · 导出展开

| 项 | 结果 |
|----|------|
| 每个 expandable「其他 N 项」后写出全部 children | ✅ `_write_detail_line` + children 循环 |
| 保留合计行 | ✅ |
| 子项 impact 之和 = 该行 ±0.01 元 | ✅ 测试断言 |
| `structure._fine_pairs` limit=8 | ✅ 未改；守卫测 |

测试：`tests/test_task_2_3_6_pl_xlsx.py::TestPlXlsxExpandOtherChildren`（3 例绿）。

## B · 全站可读性

| ID | 落点 | 结果 |
|----|------|------|
| B1 | `.sec-n` 13.5 / `.sec-t` 18.5（scifi-bridge + theme `--fs-sec`） | ✅ |
| B2 | `.bu-nav-a` 16.5px · min-h 40 | ✅ |
| B3 | `.toggle`/ghost/period trigger 字号+高 | ✅ |
| B4 | KPI 标题 15.5 + 居中；hint 12；大数 38 不动 | ✅ |
| B5 | tokens `--fs-meta/caption/body/card-h` → 12/13/15/15 | ✅ |
| B6 | 无整页 zoom；KPI `--fs-kpi: 38px` | ✅ |
| B7 | rank dual amt 弃 11px → `--fs-caption` | ✅ |
| B8 | daily/ghost mini min-height 32 | ✅ |
| B9 | `.kc-chip` caption + min-h 32 | ✅ |
| B10 | `--fs-card-h: 15px` 面板头 | ✅ |
| B11 | `.pl-export-btn` 13/34 | ✅ |
| B12 | admin gtab/ver-pill/admin-pill | ✅ |
| B13 | `--el-font-size-small: 13`；base 14 | ✅ |

守卫：`tests/test_376_readability_tokens.py`。

## C · 顺手美学

未单独加 C 批次；B4 已含周期 tag 弱化与标题居中后布局。零新 bug 优先。

## 门禁

| 命令 | 期望 |
|------|------|
| `KANBAN_OFFLINE=1 sh tests/run_verify.sh` | EXIT 0 |
| `npm run typecheck` / `npm run build` | EXIT 0 |
| `git diff --check` | EXIT 0 |

证据落 implementer scratch：`run_verify.log` / `typecheck.log` / `build.log` / `pl_export_test.log` / `b_tokens.log` / `push_tip.txt` / `prod_publish.log`。

## 发布

| 项 | 值 |
|----|-----|
| push tip | （门禁后填） |
| origin / gitee main | ff-only |
| 生产 VERSION | 3.7.6 |
| 生产 HEAD | 对齐 tip |
| health | 200（业务黄=手填缺月可接受） |

## 禁止项自检

- [x] 未 force、未 push tags  
- [x] 未写业务数据（无更新数据/手填/改剔除）  
- [x] 未改 limit / 利润公式  
- [x] 无密钥/真实业务表进库  
