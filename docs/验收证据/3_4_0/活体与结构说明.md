# 3.4.0 重点客户分析 · 活体与结构证据

日期：2026-07-30

## 结构（源码断言）

| 检查 | 结果 |
|------|------|
| `App.vue`：RankingsDual 后挂 KeyCustomersPanel | ✅ |
| `BUPage.vue`：同上 | ✅ |
| `domain/key_customers/compute.py` 纯函数 | ✅ |
| `summary["key_customers"]` 顶层挂载（非 period 内） | ✅ 测 `test_summary_mount_once` |
| `GET /api/v1/key-customers/tier` 未登录 401 | ✅ |
| 前端 F-1～F-4 守卫 | ✅ |
| 导出 `_build_vm_from_summary` `embed_key_customers_full=True` | ✅ |

## 单测（真实入口）

```
PYTHONPATH=src .venv/bin/python -m pytest tests/test_key_customers_3_4_0.py -v
# 10 passed
```

边界：80万→A、79.99→B、200→S、2.99→E；ytd=0 排除；年过滤；守恒；静默；BU 隔离；lazy/embed。

## 活体最低集（点了什么→看到了什么）

> 本施工环境以单测 + 结构守卫 + `run_verify` 为主；浏览器全链路若本机有数据服务可复测。

| # | 点了什么 | 期望看到 |
|---|----------|----------|
| 1 | 整体页滚到「四」底 | 重点客户块；六档 + 双饼；S 默认展开有客户；点客户见 12 月 |
| 2 | 切某 BU | 名单变为本 BU；无他 BU 大客 |
| 3 | 顶栏切「某月」 | 等级/文案仍「自然年…不随月/季重算等级」 |
| 4 | 开日查 | 本块不变；有日查提示文案 |
| 5 | 390 宽 | 列表与饼上下堆叠，无横向溢出 |

## 旧数字回归

`extract_numbers` 不读 `key_customers`；32 周期 golden 旧键零 diff（允许新增字段）。
