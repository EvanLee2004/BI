# 2.6.7 交付报告

2.6.7：A-E 五组 40/45 条已修，5 条进待拍板；run_verify EXIT:0 是；回归零 diff 是；已 push 是；已上机 是（VERSION=2.6.7 生产 HEAD=fab6514）

---

## 待拍板（禁止擅自决定）

1. **vendor/scifi-kit/scifi-bridge.css** 3 处 `var(--orange, #xxx)` 是否长期纳入 F-2（本单 vendor 不扫）
2. **看板仓无 LICENSE** —— 未添加
3. **证不同 / 未改代码**：
   - **C-9**：`split_tax(Σ)` 与 `Σ(split_tax)` 在 730 样本中 581 处有 ±1 分级差 → 见 `c8_c9_d9_d11_prove_same.txt` → **未改 ranking**
   - **D-9**：`columns` 未分类 `parse_amount` 与主链分口径统一会动数字 → **未改**
   - **D-11**：`margin_disp ← cost_pct` 展示命名 → **未改**
4. **覆盖率**是否固化为「代码面 / 文档面分开报」
5. **C-8**：样本证同（int 路径与 float 路径 total 差 0）→ 本单未强制收敛双实现（零差重构可后续）

---

## A 组 · 门禁与验证失灵

| # | 改了什么 | 怎么证明 |
|---|----------|----------|
| **A-1** | `tokens.css` 深空/霓虹/晨光定义 `--orange`；`BuNav.css:41` 改为 `var(--orange)` 去掉 hex 兜底 | `KANBAN_OFFLINE=1 sh tests/run_verify.sh` → EXIT:0（`gates_S1.txt`）；`a1_orange_before.png` / `a1_orange_after.png` 计算色均为 `rgb(251,191,36)` |
| **A-2** | F-2 扫描扩到 `components/**` `admin/**` `views/**`（排除 vendor + tokens + admin.css）；SettingsView 内联色与 admin 硬编码改 token | `python -m unittest tests.test_frontend_arch_guards` EXIT:0（gates_S1） |
| **A-3** | 五处 `or True` 改成真断言（domain_coverage / pl_display / bu_transition / b_http） | `a3_or_true_red_green.log` 每条 RED≠0 / GREEN=0；`rg "or True" tests/` 零命中 |
| **A-4** | 管理端四页签抓**页签独有**文本重测；人工填写 dirty 态才有保存条 | `carpet_a4_retake.md`：**四段文本互不相同=是** |
| **A-5** | 产出 skip 清单 29 处；`run_verify.sh` 末尾打印 skip 位点计数 | `skip_inventory.md`；门禁日志含 `[skip] 测试源码 skip 位点数=` |

---

## B 组 · 顶栏统一 + 红条下线

| # | 改了什么 | 怎么证明 |
|---|----------|----------|
| **B-1** | BU/整体仍各用一处 `TopBarActions`（路由互斥，非同页双挂） | 截图 `shots/bu_*.png` 顶栏仅一套按钮 |
| **B-2** | 删除 `tb-actions-narrow` / `tb-more-btn` / `tb-more-panel` 整块 | 源码与 theme.css 无 more；截图无 ⋯ |
| **B-3** | 桌面横排：主题｜导出｜密码｜退出（管理员无密码） | `shots/main_1440.png`：顶栏一行四钮 |
| **B-4** | 390 只图标+主题，横排不折叠，带 aria-label/title | `shots/main_390.png` `bu_390.png`：图标横排 |
| **B-5** | 管理端看板区无退出；设置页 `admin-logout-foot` 保留 | `shots/admin_1440.png`：管理壳无退出钮 |
| **B-6** | 退出 → DataModal「您确认要退出吗？」取消/确认；Esc/遮罩=取消 | `shots_review.md`：main_1440 `logout_modal=true` |
| **B-7** | 前端红条 + unread + `/api/alerts/ack` 下线；告警仍写 `数据/日志/告警.log` | 生产 health **无** `alerts` 字段；无 `.admin-alert-banner`；黄条/体检 pill 仍在 |

**我看到了什么（六组截图）**：见 `shots_review.md`。顶栏一行平铺、无 ⋯、无重复、无红条、管理端体检 pill 在、退出有确认框。

---

## C 组 · P0/P1

| # | 改了什么 | 怎么证明 |
|---|----------|----------|
| **C-1/C-13** | 0 行：删旧 xlsx、不 fallback、非 1 月 `empty_fetch` 抬红；缺列：维持 fallback；1 月 0 行信息级 | `c1_fetch_zhiyun_diff.txt`；`TestEmptyVsMissingColumns` 绿 |
| **C-2** | publish：update 后 pop 旧键，避免 clear 空窗 | `refresh_pipeline.publish` 源码；run_verify 绿 |
| **C-3** | `start_refresh_async(..., on_complete=)`；定时 success 仅管道真成功后登记，失败回队列 | `schedule_loop` + `server.start_refresh_async`；test_schedule 绿 |
| **C-4** | 匿名 health 过滤含金额 warnings；notify 禁止「体检红：红」 | data_api + notify 源码；test_task37_fetch_banner 绿 |
| **C-5** | POST `/api/bu_config` 刷新中 409 | config_api 源码 |
| **C-6** | 月末快照写 `.partial` 再 rename；半截不算 exists | archive.py |
| **C-7** | 真数据 periods 有算账字段时 VM 失败硬抛；壳数据仍 stub | export_html；export 相关测绿 |
| **C-8** | 先证同：样本差 0；**未强制收敛双实现** | `c8_c9_d9_d11_prove_same.txt` → 待拍板 5 |
| **C-9** | 证不同 → **一行未改** | 同上 → 待拍板 3 |
| **C-10/11** | 并入 A-2/A-3 | — |
| **C-12** | 只观测：生产 RSS≈2.0GB（pid 546671） | `deploy/seven_checks_final.txt` |

---

## D 组 · P2

| # | 改了什么 | 怎么证明 |
|---|----------|----------|
| **D-1** | `generate` conn `try/finally` close | core.py |
| **D-2** | CLI 缺年 sheet soft（`找不到「` / `新一年的 sheet`） | run.py missing_like |
| **D-3** | 空密码不静默变 8888 | accounts.py |
| **D-4** | BU/本地配置 tempfile + `os.replace` | bu.py / loaders.py |
| **D-5** | login_guard 过期键淘汰 | login_guard.py |
| **D-6** | BU pl.xlsx 先鉴权再 404 | export.py |
| **D-7** | admin form 登录成功 `_audit` | admin_pages.py |
| **D-8** | prune 用 `date('now','localtime',…)` | db_write.py |
| **D-9** | ⚠ 证同/会动数字 → **未改** | 待拍板 |
| **D-10** | 未登录 401 / 已登录无权限 403；测试同步 | data_api + test_auth/multibu/profit_rank |
| **D-11** | ⚠ 展示口径 → **未改** | 待拍板 |

---

## E 组 · P3 与文档

| # | 改了什么 | 怎么证明 |
|---|----------|----------|
| **E-1** | PRODUCT_CHANGELOG 补 2.6.1–2.6.7 管理层口吻 | version.py；test_task42_final 绿 |
| **E-2** | 程序 CLAUDE 状态 2.6.7 | CLAUDE.md |
| **E-3** | PL 公式税率取配置；数字不变 | structure.py；回归零 diff |
| **E-4** | 部署 `数据/BU配置.json` chmod 600；Python 3.14.4 写入报告（未擅自升级） | deploy/seven_checks_final.txt |
| **E-5** | progress 生产 HEAD / 本地 HEAD 分写 | progress.md |

---

## 上机七项（生产）

| 项 | 结果 |
|----|------|
| VERSION | 2.6.7 |
| systemctl | active |
| login | 200 |
| built_at | 2026-07-27 02:30:05 |
| 页可开 | login/admin/root 可达 |
| 数据 | 备份 `看板_上机前_202607270225.db` 4444160 B；手填/调整表有行 |
| 体检 | result=黄；无 alerts 字段（红条下线） |
| HEAD | fab6514（交付报告 commit bce1b50 仅 docs） |
| RSS | ≈2.0GB |

备份与命令输出：`deploy/`。

---

## 门禁与远端

- 各阶段 `gates_S1`…`S6` 均含真实 `run_verify EXIT:0`
- `grep -rn "or True" tests/`：**零命中**
- 回归：run_verify 第 3 步「32 个周期…全部相同」
- push：`origin` + `gitee` 仅 `main`（未推 tags）
- 本地 tag：`stage_2_6_7_s1`…`s7`（仅本地）

---

## 证据路径

```
docs/验收证据/2_6_7/
├── gates_S1.txt … gates_S6.txt
├── shots/{admin,main,bu}_{1440,390}.png
├── a1_orange_before.png / a1_orange_after.png
├── a3_or_true_red_green.log
├── c1_fetch_zhiyun_diff.txt
├── c8_c9_d9_d11_prove_same.txt
├── shots_review.md / carpet_a4_retake.md / skip_inventory.md
└── deploy/（backup + seven_checks_final）
```
