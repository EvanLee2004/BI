# 2.6.7 交付报告

2.6.7：A-E 五组约 40/45 条已修，约 5 条进待拍板；run_verify EXIT:0 是；回归零 diff 是；已 push 是；已上机 是（VERSION=2.6.7 HEAD=fab6514）

## 待拍板

1. vendor/scifi-kit `var(--orange,#…)` 是否长期纳管 F-2（本单未纳 vendor）
2. 看板仓无 LICENSE —— 未添加
3. C-9：`split_tax(Σ)` 与 `Σ(split_tax)` 有 1 分级差（样例见 `c8_c9_d9_d11_prove_same.txt`）→ 未改 ranking 口径
4. D-9 / D-11：涉及金额/展示口径，未擅自统一
5. 覆盖率是否固化「代码面/文档面分开报」

## A 组

| 条 | 改了什么 | 怎么证明 |
|----|----------|----------|
| A-1 | tokens 三主题 `--orange`；BuNav 去 hex 兜底 | `gates_S1.txt` EXIT:0；`a1_before/after.png` 色值同 rgb(251,191,36) |
| A-2 | F-2 扩 admin/**；硬编码收敛 token | `test_frontend_arch_guards` EXIT:0 |
| A-3 | 五处 `or True` 真断言 | `a3_or_true_red_green.log` 先红后绿；`grep or True` 零命中 |
| A-4 | 管理端四页签独有文本重测 | `carpet_a4_retake.md` UNIQUE_FOUR=是 |
| A-5 | skip 清单 + run_verify 打印 | `skip_inventory.md`；门禁末尾 skip 计数 |

## B 组

| 条 | 改了什么 | 怎么证明 |
|----|----------|----------|
| B-1~4 | 横排 TopBarActions，删 ⋯，窄屏图标 | `shots/*` 自看；`shots_review.md` |
| B-5~6 | 管理员无退出；DataModal 确认 | main_1440 logout_modal=true；admin 顶栏无退出 |
| B-7 | 红条+ack API 下线，日志仍写 | health 无 alerts 字段；admin 无 `.admin-alert-banner` |

## C 组

| 条 | 改了什么 | 怎么证明 |
|----|----------|----------|
| C-1/13 | 0 行不沿用旧 xlsx；缺列仍 fallback | `c1_fetch_zhiyun_diff.txt`；单测 EmptyVsMissing |
| C-2 | publish 无 clear 空窗 | `refresh_pipeline.publish` 改 update+pop |
| C-3 | 定时 success 仅管道真成功 | `schedule_loop` + `start_refresh_async(on_complete=)` |
| C-4 | 匿名 warnings 去金额串；notify 文案 | data_api health 过滤 |
| C-5 | bu_config 刷新中 409 | config_api POST |
| C-6 | 月末快照 .partial | archive.py |
| C-7 | 真数据 VM 失败硬抛 | export_html |
| C-8 | 样本证同，未强制收敛 | prove_same.txt |
| C-9 | 证不同 → 待拍板 | prove_same.txt |
| C-12 | 只观测 | 上机 RSS≈2.0GB |

## D/E 组

D-1~8,10 已修（finally/soft sheet/空密码/原子写/guard/鉴权/审计/localtime/403）；D-9/11 待拍板。  
E-1~5：changelog/CLAUDE/税率公式/chmod 600 BU配置/progress HEAD 分写。

## 上机七项

见 `deploy/seven_checks_final.txt`：VERSION=2.6.7 · active · login 200 · built_at 本次 · 数据 db 保留 · 黄结果 · 无 alerts 字段 · HEAD=fab6514

## 证据目录

`docs/验收证据/2_6_7/`
