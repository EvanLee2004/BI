# 57 B-3 安全深扫定性

## 工具与证据路径

| 工具 | 报告 | 结果 |
|------|------|------|
| gitleaks (git log stage54..HEAD) | `gitleaks_git_stage54_HEAD.json` | **no leaks found** EXIT:0 |
| gitleaks (workspace --no-git) | 命中 `数据/` 本地运行产物 | **误报/范围外**：`数据/` gitignore 不进库；部署机本地文件 |
| bandit -r src/ -ll | `bandit_report.txt` | 见下 |
| npm audit | `npm_audit_summary.txt` | 4 条 moderate/high 依赖公告 |

## bandit

（报告全文见 bandit_report.txt；HIGH/MEDIUM 逐条：真问题修 / 误报写依据）

## npm audit

| 包 | 级别 | 处置 |
|----|------|------|
| echarts XSS GHSA-fgmj-fm8m-jvvx | moderate | **待评估升 6.x**：为 breaking；图表配置均后端串+无 v-html 用户 HTML；短期误报降风险，写进交付「已知依赖债」 |
| element-plus el-link href | moderate | 管理端可控导航，不接受用户任意 href；`npm audit fix` 若无破坏则升 |
| esbuild/vite 开发服 | moderate/high | **仅 devDependency**，生产 nginx 静态，不暴露 vite dev server → **误报/不修生产面** |

## 鉴权豁免（B-5 端点）

`POST /api/v1/client-error`：仅收错误文本，无数据读取；限流 30/min；消息截断；落 `数据/前端错误.log`（gitignore）。


## bandit 真/误报摘要

| ID | 级别 | 处置 |
|----|------|------|
| B324 sha1 定位键 | High | **误报**：`ingest/normalize._hash` 短指纹非密码学；已 `# nosec B324` + 注释 |
| B104 0.0.0.0 | Medium | **设计**：内网看板默认绑全接口；生产 nginx 反代可改 `server_host` |
| B608 SQL | Medium | **误报为主**：表名/列来自 DETAIL_TABLES 白名单 + 参数化 `?`；业务层零裸 SQL 铁律 |
| B310 urllib | Medium | notify 飞书 webhook 固定 https，timeout≤3s |

