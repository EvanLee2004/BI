# 施工报告 · 经营罗盘 3.7.14 审计全量修复（除密码）

| 项 | 值 |
|----|-----|
| 日期 | 2026-08-05 |
| 基线 | `32a6a84e5ff24414ff8bbaaac48d6e97a8a40937` |
| 分支 | `task/20260805-audit-fix-3.7.14` |
| VERSION | **3.7.14** |
| 候选 tip | `8672f31477c74d402e740ddd9dc117b8e014b878` |
| 执行 | GrokBuild EXECUTOR · worktree `kanban-3714-audit-fix` |

## 1. 范围落地

| ID | 结果 |
|----|------|
| 004/H01 | `csrf_guard.client_ip_for_auth` + 登录三路径使用；测 `test_audit_3_7_14_backend` |
| 005 | 导出 HTML/PNG/xlsx：未登录 401；已登录无主权限/无 cap/无权 BU → 403 |
| 006 | 导出 except 对外固定短句；`log.exception` 留服务端 |
| 003 | `apply_sid_cookie(secure=)`；HTTP 无 Secure；loopback+XFP=https 有 Secure |
| 010 | `fetchRace` 世代闸 + `apiGet` signal；cockpit `loadMain`/`loadBu` |
| 017 | `sessionSingleflight`；login/logout invalidate |
| H20 | `caliber_note` + ExpenseSection 旁注 + Ledger 默认文案 |
| 007 | CSP + object/frame/worker；**残余** script/style `unsafe-inline`（Vue/ECharts） |
| DOC | Runbook §0.2 BESTEASY/排障 |
| OPS | CIFS 步骤单对齐；`kanban.service` 预留 `/mnt/kanban-ledger`；`ledger_path_exists` |
| 001/002 | **未做**（密码 hold） |

## 2. CSP 残余说明（AUDIT-007）

- **已收紧**：`object-src 'none'`、`frame-src 'none'`、`worker-src 'self' blob:`。
- **保留** `script-src`/`style-src` 的 `'unsafe-inline'`：现网 Vue 启动壳与 ECharts 内联样式依赖；去掉会白屏。
- 活体：本地 OFFLINE typecheck 绿；无强制去 inline 的部署。

## 3. preflight

见 `00_prod_readonly_preflight.md`：现网仍 gvfs、无 CIFS、BESTEASY、kanban active、health 黄。**未 umount**。与审计 08 一致 → 不 BLOCKED。

## 4. 自审清单（方案 §5）

- [x] 未改 profit 公式  
- [x] 未改密码存储/回显契约  
- [x] 未在生产 mount/umount  
- [x] HTTP 下仍能登录（Secure 未误伤；单测覆盖）  
- [x] BU 隔离测仍绿（run_verify 串行含 multibu）  
- [x] CSRF 测仍绿（`test_s13` / `test_g3_security`）  
- [x] 导出权限矩阵（`test_audit_3_7_14_backend`）  
- [x] 无敏感写入 git（preflight 脱敏；无真实密码/客户金额）  

## 5. 门禁

- `KANBAN_OFFLINE=1 sh tests/run_verify.sh` → EXIT 见同夹 scratch 与总控  
- 守卫：`tests/test_audit_3_7_14_backend.py` · `tests/test_audit_3_7_14_frontend.py`（已入 run_verify 清单）

## 6. 禁令遵守

- 未 `git push` / publish / 生产写 fstab/cred / umount  
- 未改 MADR-0020 密码明文

收口冻结 hash: `8672f31477c74d402e740ddd9dc117b8e014b878`
