# 经营罗盘 3.7.9 施工报告（EXECUTOR · REVIEW_READY）

> 日期：2026-08-04  
> worktree：`kanban-379-caps-simplify` · 分支 `task/20260804-caps-simplify`  
> 基线：`e57444122279755459605614d7d37c08a31e752c`  
> **角色停点**：候选完整 commit + 门禁 EXIT:0；**未** push / SSH / 部署。

## 交付摘要

| 项 | 结果 |
|----|------|
| VERSION | **3.7.9** |
| 硬规则 | `materialize_caps`：管理员全 true；非管理员管理类/view_main/管端导出/归档强制 false；仅四导出可读 JSON |
| `can_main` | 管理员或整体；BU 脏 view_main / 旧「可看整体页」不放行 |
| `is_admin` | 角色 `accounts.is_admin`；禁止半管理 |
| 设置页 | 管理员「管理员 · 全部权限」；整体/BU 仅四导出；副标题更新；`CAP_KEYS` 收敛 |
| API | 无 cap 导出 403；非管理员写/刷/归档 401/403；管理员路径可达 |
| 密码回显 / 写锁 | 3.7.8 行为不回退 |
| 门禁 | `KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?` → **EXIT:0**（runtime passed=1400 failed=0 skipped=0） |
| 活体 | `docs/验收证据/3_7_9/live/L1_L4_notes.md`（API 级 + 源码静态；无交互浏览器） |

## 主要改动文件

| 路径 | 说明 |
|------|------|
| `src/authz.py` | 3.7.9 硬规则 materialize / can_main / is_admin / template / defaults |
| `src/accounts.py` | 保存写盘前 materialize 清洗脏管理类 |
| `src/api_v1.py` | session can_main 角色权威 |
| `frontend/src/admin/composables/useSettingsForm.ts` | CAP_KEYS 仅四导出 |
| `frontend/src/admin/views/SettingsView.vue` | 管理员固定文案 + 副标题 |
| `frontend/dist/*` | 在线 SPA 重建 |
| `tests/test_task_3_7_9_caps.py` | 新增硬规则 + API + 源码静态 |
| `tests/test_task_3_7_8_caps.py` | 对齐冲突期望（半管理 ops_ro 改为非管理员） |
| `VERSION` / `CHANGELOG.md` / `src/version.py` | 3.7.9 |

## 测试

- `tests/test_task_3_7_9_caps.py`（T8 脏数据、四导出独立、can_main 角色、403、保存清洗、设置页源码）
- `tests/test_task_3_7_8_caps.py` / `tests/test_task_3_7_8_write_lock.py`（对齐后绿）
- 全量 `tests/run_verify.sh`

## 疑问 / 保守点

- 管理写/刷路径对看端会话常先 401（未管理会话），与 403（无 cap）同属「非管理员不可达」；测例二者均接受。
- 存量无「能力」字段的整体/BU 默认四看端导出仍开（与 3.7.8 存量可达一致）；新 BU 模板四关。

## 下一步（非本会话）

VERIFIER 用验收 START_HERE + 候选完整 hash → PASS 后 RELEASER push 双远端 + `publish_kanban --pull`。


## 候选 tip

| 项 | 值 |
|----|-----|
| 功能主体 | `95e849d78e90007799ea6f4b46303ec3c23adc30` |
| 分支 tip | 见总控 / `git rev-parse HEAD` |

## 候选 tip

- 分支 tip（控制卡 candidate）: `319608a4902556085995383fc05e3a68905dd262`
- 功能主体: `95e849d78e90007799ea6f4b46303ec3c23adc30`
