# 经营罗盘 3.7.7 交付报告

> 日期：2026-08-02  
> 角色：EXECUTOR（Grok）  
> 基线：`58dee739b7e5ae5d210fb2ab27e3b274294847af`（3.7.6）→ **3.7.7**  
> 工位：`kanban-377-desktop-polish` · 分支 `task/20260802-desktop-polish-logo`

## 一句话

桌面优先：左上角 Logo 放大至约 42px；`theme.css` 静态 cache-bust（`?v=` + VERSION）；P1 清扫 &lt;12px 与 KPI 副文/管理端密表；窄屏仅 `@media (max-width:520px)` 内小修。门禁绿后 push 双远端 + 上机。

## 改动摘要

| 优先级 | 项 | 结果 |
|--------|----|------|
| P0 Logo | 桌面 height **42**、max-width **56**；合并 theme 历史 40/28 双规则；App/BU img 42 | ✅ |
| P0 Logo 窄屏 | ≤520 height **34**（32–36 档） | ✅ |
| P0 缓存 | `frontend/index.html` + dist `theme.css?v=3.7.7`；`app_factory._vue_index` 注入 `?v=`+PRODUCT_VERSION | ✅ |
| P1 | meta-head ≥12；ld-funnel 12；kpi-sub 13；admin 表 13 / input min-h 32；us-hint 12 | ✅ |
| P2（已做） | 窄屏 KPI 22→**26**（仅 media）；排名名左对齐 | ✅ |
| 桌面 KPI | `--fs-kpi: 38px` 未改；无整页 zoom；无桌面手机化 | ✅ |

主要路径：`static/css/theme.css` · `scifi-bridge.css` · `tokens.css` · `LedgerTable.css` · `admin.css` · `UserStatsView.vue` · `App.vue` / `BUPage.vue` · `app_factory.py` · `VERSION` / `version.py` / `CHANGELOG.md`。

## 门禁

| 命令 | 结果 |
|------|------|
| `KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?` | **EXIT:0**（runtime passed=1354 failed=0 skip=0） |
| `npm run typecheck` | EXIT:0 |
| `npm run build` | EXIT:0 |
| `git diff --check` | 干净 |
| 守卫 | `tests/test_377_desktop_logo_tokens.py`（8 例） |

证据（implementer scratch）：`run_verify.log` · `typecheck.log` · `build.log` · `logo_cache_asserts.txt` · `p1_grep.txt` · `version.txt` · `diff_check.txt`。

## 发布

| 项 | 值 |
|----|-----|
| 候选 tip | **`5a2a79e`** (`5a2a79e62b9354d5c4b898583efe7ac86ae86850`) · 功能 tip `951926f` |
| origin / gitee main | ff-only `58dee73..5a2a79e`（无 force、无 tags） |
| 生产 VERSION | **3.7.7** |
| 生产 HEAD | **`5a2a79e`**（对齐 tip） |
| systemctl | **active** |
| health | **200** |
| 上机 | `publish_kanban.sh --pull` **SUCCESS** · candidate :8019 → kill reload :8018 |
| 回滚基线 | `58dee739b7e5ae5d210fb2ab27e3b274294847af` |

## 禁止项自检

- [x] 未 force、未 push tags  
- [x] 未写业务数据（无更新数据/手填/改剔除）  
- [x] 未改利润/分摊/六档算法  
- [x] 未把桌面 KPI/顶栏做成手机样式  
- [x] 未换色板  

## 已知限制

- 活体 1280/390 截图未在本机浏览器会话内强制采集；静态守卫 + dist 产物 + 门禁全绿。  
- 首轮 `test_datalayer` 曾因并行/状态污染偶发红线 diff，单跑与二次全量均为绿（非口径回归）。  
