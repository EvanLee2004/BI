# 57 部署演练记录

时间: 2026-07-19  
方式: 本机 `file://` git clone（模拟从零 · macOS 预演）  
对照: `docs/Ubuntu部署手册.md` §3 · `docs/上线交接包/部署执行单_给Claude.md` §C

## 步骤与结果

| # | 步骤 | 结果 |
|---|------|------|
| 1 | `git clone file://…/.git` → 临时目录 | OK |
| 2 | `python3.12 -m venv` + `pip install -r requirements.txt` | OK（系统默认 python3.9 失败 → **文档补强：必须 ≥3.12**） |
| 3 | 准备 `数据/`（`_golden_data` 合成样例）+ `KANBAN_OFFLINE=1` + `run.py` | EXIT:0 · 出 HTML |
| 4 | `deploy/healthcheck.sh` / `register_schedule.sh` 存在 | OK |
| 5 | `run.py --serve` · GET `/login` `/` | HTTP 200 |
| 6 | Ubuntu 专有：apt/nginx/CIFS/systemd/BIOS | 本机无法执行，标记部署机 |

## 文档 bug 与已修

1. **Python 版本**：macOS 默认 3.9 无法装 fastapi 0.139 → 手册 §3 写明必须 ≥3.12。  
2. **离线预演**：补充 `KANBAN_OFFLINE=1` + `_golden_data` 首次空跑说明。  
3. **部署执行单**版本锚点 rc1/stage55_rc1 → **rc10/stage56_final**。

## 结论

照文档主路径（clone → venv≥3.12 → requirements → 数据 → run → 起服）可从零到页面可开；无「文档没写但必须做」的隐式步骤（除 Ubuntu 机房专有项已诚实标注）。
