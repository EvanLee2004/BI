结论：管理利润表导出金额已改为「元」数字列 + 导出按钮暗色适配，请明昊过目

# 任务书 2.4.1 交付报告 · 管理利润表导出改元 + 导出按钮暗色适配

**日期**：2026-07-24  
**VERSION**：2.4.1  
**HEAD**：`4b21dd5`  
**本地 tag**：`stage_2_4_1`（未 push tags）

---

## DoD 勾选

| # | 项 | 状态 | 证据 |
|---|----|------|------|
| ① | 按钮暗色适配（ghost/mini + pl-export-btn） | ✅ | `static/css/theme.css` 补全 `button.ghost`/`button.mini`；`.pl-export-btn` 青描边；rebuild dist |
| ② | Excel 金额=元（impact→fen_to_yuan，无「万」） | ✅ | `src/export_pl_xlsx.py`；单测 yuan vs impact；抬头口径文案 |
| ③ | 死代码清理（rg 证明） | ✅ | 删 `packers._amt_disp`/`_abs_amt_disp`；简化 `_safe_sheet_name`→固定「管理利润表」 |
| ④ | 测试 + run_verify EXIT:0 | ✅ | `tests/test_task_2_3_6_pl_xlsx.py` 12 例；见 §M1 |
| ⑤ | VERSION / changelog / 本报告 / progress / 工作日志 | ✅ | 本文件 + CHANGELOG + version.py + progress + 工作日志 |
| ⑥ | commit + push main×2 + SSH 上机 | *(上机后勾)* | 见 §M8 |

---

## 实现摘要

### 数据路径

- `build_pl_xlsx_bytes` → 与 `pack_pl_by_period` **同入参**组装 period 切片 → **`pl_structure` 全量**（含 `impact` 分 / `is_pct`）
- 非百分比：`money.fen_to_yuan(impact)` → Excel **number** + `#,##0.00`（真负数）
- 百分比：仍写 `amt_disp` 如 `44.0%`
- **禁止**页面 `amt_disp` 万元串写入金额列
- 页面 KPI/利润表仍万元，未改

### Excel 结构（保持 2.4.0 一页化）

| Sheet | 内容 | 字重 |
|-------|------|------|
| 管理利润表 | 抬头 + 科目 \| 金额(元) \| 说明；大类加粗；明细缩进浅灰 | 大类 bold / 明细 not bold |

抬头口径：`与看板管理利润表当前筛选一致；金额单位：元（页面看板仍为万元展示）`

### 按钮

- 全局：`button.ghost` / `button.mini` 对齐 `.toggle`（`var(--panel)` 底 + `var(--line-2)` 边 + 浅字；hover 青描边）
- 强调：`.pl-card .pl-export-btn` 用 `var(--accent)`
- theme 唯一源 `static/css/theme.css`（生产 `<link>`）；frontend rebuild dist 已提交

### 删了什么（rg 证据）

| 项 | 证据 |
|----|------|
| `packers._amt_disp` / `_abs_amt_disp` | 除定义外零调用；已删定义 |
| `export_pl_xlsx._safe_sheet_name` | 单 sheet 固定名后仅服务「管理利润表」→ 改为 `ws.title = "管理利润表"` |
| **未删** | `structure_for_vm`、页面 `amt_disp`、`fmt_wan`、`safe_filename_part` / `pl_xlsx_filename`、`src/profit/*` |

### 未改

- 顶栏 HTML 快照 `export.html`
- `src/profit/*`、金额算法、`fetch_zhiyun.py`
- 页面万展示、鉴权/文件名语义、nginx

---

## 证据

### M1 · run_verify

```text
KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?
… ✓ 全部通过
EXIT:0
```

（完整日志：implementer scratch `run_verify.log`）

### M2 · xlsx 金额无「万」+ 元换算

- 单测 `test_amounts_yuan_no_wan_and_match_impact`：数据区金额列无「万」；≥3 个大类 `abs(cell_yuan - impact_fen/100) < 0.005`；百分比含 `%`
- 禁止「去万字但仍是万元数字」：元量级 = impact/100

### M3 · 大类 bold / 明细 not bold

- `test_main_sheet_bold_header_and_nested_details` 仍绿

### M4 · 按钮非白（neon）

- 源：`theme.css` `button.ghost{ background:var(--panel); border:1px solid var(--line-2); color:var(--mut) }`
- 活体：上机后 neon 主题看「导出 Excel」非浏览器默认白底（见 §M8）

### M5 · dist

- `npm run build` 成功；`frontend/dist` 资产哈希更新并入库

### M6 · 顶栏 export.html

- 源码未改；静态守卫：TopBar 仍 `export.html`，无 `export/pl.xlsx`

### M7 · 生产 VERSION=2.4.1

*(SSH 后回填)*

### M8 · 上机

*(SSH 后回填真实输出)*

| 项 | 值 |
|----|-----|
| SSH | *(kanban-home / kanban-lan)* |
| 动作 | `git pull --ff-only` → 自愈重启 |
| VERSION | **2.4.1** |
| HEAD | *(fill)* |
| health / login | *(fill)* |
| 活体按钮 | neon 非白底 |
| 活体 xlsx | 金额列无万、元量级 |

### M9 · push

| 项 | 值 |
|----|-----|
| commit | *(fill)* |
| tag | `stage_2_4_1`（仅本地） |
| push | `origin main` + `gitee main`（无 `--tags`） |

---

## 发布前安全扫

- diff 无真实客户金额/密码/内网账号/台账 UNC 进库
- 未跟踪 `数据/数据/` 真库 **不提交**

---

## 待拍板

无（本单范围内按已拍板「元 + 页面仍万 + 2.4.1」完成）。

非目标保持：页面去万、台账导出改单位、profit 算法、push --tags — **未做**。
