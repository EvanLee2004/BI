# MADR 0005：告警走飞书自定义机器人 webhook

- **状态**：已采纳（2026-07-16 · 任务书43）
- **上下文**：部署机无人盯屏；需体检红/关键失败时通知。

## 决策

- 配置 `feishu_webhook_url`（本地覆盖层，不进 git）。  
- 触发：管道结果红（含磁盘不足等）。  
- **未配置 = 零行为变化**；**发送失败 try/except + 超时 3s，绝不影响主流程**。  
- 用 stdlib `urllib`，不加 SDK。

## 理由

- 飞书为公司现成通道；自定义机器人无额外账号体系。  
- 静默默认保证测试/开发机无副作用。

## 后果

- 仅 text 消息；富卡片/签名校验未做（待需要再扩）。  
- 包装脚本启动即崩告警可在部署机 shell 里补 curl（仅部署机可验）。
