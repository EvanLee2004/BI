# MADR-0014：删除看端 legacy 壳（补录）

- **状态**：Accepted（补录）
- **日期**：2026-07-18（决策）· 2026-07-19（补录）
- **背景**：Vue 看端已达生产可替换；保留 legacy 双轨增加维护与口径漂移风险。
- **决策**：删除看端 legacy HTML/JS 壳，只保留 Vue `frontend/dist` + 服务端 VM/API。
- **备选**：长期双轨（否：运维面翻倍）；feature flag 灰度（否：内网 5 人无需）。
- **后果**：部署只发 dist；旧书签依赖 legacy 路径需改 `/`；测试改结构守卫。
- **出处**：任务书 54.4 PERF / 变更迈进计划书。
