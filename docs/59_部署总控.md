# 任务书 59 · 部署总控（断点续跑）

> 版本锚点：**2.0.0-rc12** / **stage58_ui** · HEAD `d4aa73c`  
> 远程仅 `ssh kanban-home` · 实际产品树：`/home/lee/kanban/看板正式程序`（sudo 阻塞故未用 `/opt/kanban`）

## 前置
- [x] `git tag` 含 `stage58_ui`
- [x] `ssh kanban-home hostname` 含 `lee-ThinkCentre`
- [x] 证据目录已建

## 步骤
- [x] 1 机况检查（磁盘/内存/时区/python≥3.12/nginx）
- [x] 2 push main 双远端（diff 快扫）+ 机上 clone/对齐 HEAD
- [x] 3 venv + deps + playwright chromium
- [x] 4 数据目录初始化（无合成进生产）
- [x] 5 systemd（user）+ 看门狗 enable/now
- [x] 6 nginx 80 — **跳过（需 sudo）**；改用 :8018 直连
- [x] 7 crontab（日更 + healthcheck + 备份）
- [x] 8 冒烟 D 清单（login 200 / 版本 rc12 / healthcheck 0 / is-active）
- [x] 9 自愈演练（kill 后看门狗拉起；证据见 selfheal.log）
- [x] 10 scp 智云配置（600/lee，内容不上屏）
- [x] 11 真实「更新数据」首跑四硬标准（黄/fetched/今日/有数）
- [x] 12 防回归快照 + 明昊待办卡 + 交付报告

## 卡点记录
- sudo 无 NOPASSWD → 不能写 `/opt`、不能装 system unit、不能改 nginx:80；已用 home + user systemd + :8018 等价可用。
- 台账：gvfs guest 挂载成功；刷新后 **fetched**（非 local_fallback）。
