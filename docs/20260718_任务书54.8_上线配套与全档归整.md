# 任务书 54.8：上线配套四件套 + 全项目文档归整（排队单·跑完 54.7 后执行）

> working_directory=`程序/看板正式程序/`（文档范围含上级 `方案与文档/`）；**只 commit 不 push**；tag `stage54p8`。
> 你是执行机器：按序做、不猜、不扩范围。**前置条件**：54.7 已收口（tag `stage54p7` 存在、终验报告结论为"上线就绪"）；若前置不满足，停下只写一份说明，不干活。
> 本单**不改任何产品代码**（A~D 只新增文件；E 只动文档）；唯一例外是 B 项允许在 `tests/` 新增脚本。

## 前科条款

1. 历史上出现过把"计划"写成"现状"——本单所有文档如实；用户手册里的每个操作步骤必须亲手在 8018 上走一遍验证可行再写。
2. 禁止为归档而破坏引用——移动任何文件前先 `grep -rn` 找到全部引用处并同步更新，移完复查零死链。

## 必读输入（按序）

1. `docs/20260718_任务书54.7终验报告.md`（最终形态的权威描述）
2. `方案与文档/00_项目文档地图_从这里开始.md` + `方案与文档/README.md`（现有索引结构）
3. 根仓库 `甲骨易实习/CLAUDE.md` 的「给领导写汇报材料」节（写 D 项前必读）
4. `README.md`（项目门面现状）

## A. 用户手册 + FAQ（给同事和陆总的，不是给工程师的）

- 新建 `docs/用户手册/`：`看板使用手册.md`（怎么打开/扫码、每个页面看什么、周期切换、下钻、BU 页是什么）、`管理端操作手册.md`（更新数据、手填、调整、账号权限，每步配一张 8018 实操截图——**合成数据环境截图,零真实数据**）、`FAQ.md`（数字对不上怎么办/黄条警告什么意思/打不开找谁/手机看不了怎么办等,至少 12 条,从数据体检面板和历史交付报告里挖真实场景）。
- 语言要求:写给完全不懂技术的财务同事,零术语;每篇开头一句话说明"这篇解决什么问题"。md 为源（后续人工贴飞书）。

## B. 终验能力资产化（一次投入,永久红线）

- 把 54.7 用过的性能/兼容/鲁棒检查固化成可重复脚本:`tests/perf_check.py`（首屏/交互延迟/长会话内存,输出 指标|阈值|实测|结论 表,超标 exit 非 0）、`tests/robust_check.py`（坏输入/双击/深链 F5 的 Playwright 走查）。
- 写清用法进 `tests/README.md`（何时跑:每个大批次收口时;怎么跑;结果怎么读）。**不改动 run_verify.sh 本体**（保持日常验证速度）,这两个是批次级大验专用。

## C. 健康监测脚本（回答"坏了我们怎么知道"）

- 新建 `deploy/healthcheck.sh`:curl 登录页 200 + 首页可达 + 数据新鲜度（最新数据日期落后超 2 天=告警）;失败时写入 `deploy/health_alerts.log`（含时间与原因）。
- 配套 `deploy/healthcheck_cron说明.md`:部署机上怎么挂每小时 cron、告警日志在哪看、常见告警怎么处理（对应 FAQ）。本机模拟跑通一次留证（起服务→跑脚本→制造一次失败→确认告警落盘）。

## D. 上线公告草稿（给陆总的一页纸,人定稿）

- 新建 `方案与文档/给陆总的汇报件/20260718_看板2.0上线公告_草稿.md`:开门见山（上线了什么/怎么访问/对她意味着什么）、三条核心变化（对比旧版,站使用者角度不站技术角度）、一句话找谁支持。**半页以内、可直接转发**;同事一律写岗位不写姓名;文末标注「草稿·待明昊定稿」。

## E. 全项目文档归整（最终态:任何人 30 秒找到任何东西）

1. **docs/ 瘦身**:历史批次任务书/交付报告/验收报告（54.7 之前的全部）移入 `docs/历史批次/`,只留最新权威文档在 docs/ 一级;移动前 grep 全部引用并同步改,移后 `grep -rn` 复查零死链。
2. **docs/README.md 索引（新建）**:表格列出 docs/ 每个文件/子夹 = 是什么 + 什么时候看;明确标出「当前权威」的那几份（终验报告/CHANGELOG/用户手册/部署手册）。
3. **`方案与文档/00_项目文档地图` 重写**:按"我想知道 X → 看哪份"组织（想知道现状→/想部署→/想看口径→/想教同事用→/想看历史决策→）,每类只指向**一份最新权威文档**,历史版本统一指向归档夹。
4. **项目根 CLAUDE.md 与 progress.md 不动**（那是 Claude/明昊的账本）;但检查项目 README.md 门面是否与 54.7 终态一致,不一致就改。
5. 归整结果出「文档地图终态表」进交付报告:每份权威文档 | 位置 | 内容一句话。

## 收口交付

- [ ] A~E 全做完;用户手册每步骤亲手验证过;B/C 脚本本机实跑通过留证
- [ ] `sh tests/run_verify.sh; echo EXIT:$?` 仍 EXIT:0（证明没碰产品代码;禁 `| tail`）
- [ ] 全项目 `grep -rn` 零死链（移动过的文件逐个核）
- [ ] 交付报告 `docs/20260718_任务书54.8交付报告.md`（含文档地图终态表）;tag `stage54p8`;只 commit 不 push

## 非目标

push、部署机操作（Claude 做）、贴飞书（明昊做）、公告定稿（明昊做）、改产品代码。
