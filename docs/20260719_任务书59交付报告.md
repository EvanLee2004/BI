结论：生产部署完成,待明昊手机验+陆总首秀

# 任务书 59 交付报告 · 远程生产上线

## 一句话

公司机 **lee-ThinkCentre** 已跑通 **2.0.0-rc12**（HEAD `d4aa73c`）：智云四源今日更新、收单台账**共享拉取 fetched**、体检**黄非红**、看板 fragments 有完整载荷；入口 **`http://192.168.30.46:8018/`**。因 **sudo 无免密**，未落 `/opt`+nginx:80（见偏差与待办卡）。

## 完成对照

| 门闩 | 状态 |
|------|------|
| 前置 stage58_ui + ssh hostname | ✅ |
| push main 双远端（无 tags） | ✅ origin + gitee |
| 机上 HEAD = 本地 main | ✅ d4aa73c |
| 冒烟 login/version/healthcheck/active | ✅ |
| 自愈（看门狗拉起进程） | ✅（冷启动 HTTP 约 2–3min） |
| 智云配置 scp 600/lee | ✅ 内容不上屏 |
| 首跑：无红 / 台账非 fallback / 四源今日 / 有数 | ✅ 黄灯可解释；fetched；今日；fragments 4.9MB |
| 交付报告首行 | ✅ 本文件 |

## 偏差（诚实）

1. **路径**：`/home/lee/kanban/看板正式程序` 而非 `/opt/kanban`（`sudo -n` 失败）
2. **进程**：`systemctl --user` 而非 system unit
3. **入口**：`:8018` 直连 static，而非 nginx `:80`
4. **台账挂载**：gvfs guest 用户挂载（非 root CIFS `/mnt/caiwu`）；服务环境已设 `XDG_RUNTIME_DIR` 使刷新可见共享

## 安全

- 未碰 `/opt/OpenCode`、`/opt/todesk`
- 未 push tags
- 智云配置未进 git/对话正文

## 明昊下一步

见 `docs/验收证据/20260719_59_部署/明昊待办卡.md`：手机验、改密、BU、BIOS、可选 sudo 固化到 `/opt`+nginx。

## 证据目录

`docs/验收证据/20260719_59_部署/`（部署实录 · 待办卡）+ 本机 scratch 日志（precheck/push/smoke/selfheal/final_hard）。
