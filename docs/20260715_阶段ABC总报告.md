# 总报告：陆总需求(A) + 前后端分离(B) + 架构(C) · 2026-07-15

## 阶段 A · 陆总需求（已双推 main `4d273c6` 含 A1~A7）

| Commit | 需求 | 证据 |
|--------|------|------|
| `23e38e1` A1 | #9 删BU保存键 | run_verify_A1 ✓ |
| `b9ab20b` A2 | #4 译员姓名 | a2_inhouse_totals.log；run_verify_A2 ✓ |
| `32da74d` A3 | #1#2 KPI/回款卡 | run_verify_A3 ✓ |
| `d9db00e` A4 | #3 税前利润率 | run_verify_A4 ✓ |
| `11b4e5a` A5 | #5 BU明细隔离 | a5_bu_isolation.log 3 tests OK；run_verify_A5 ✓ |
| `4d273c6` A6/A7 | #6#7 双血条/目标条 | run_verify_A6 ✓；golden numbers 已刷 |

**A8** 排名月度展开：**跳过**（中优先，试运行后补）。  
**A9** 文档：CHANGELOG / 概要设计 / 需求台账已更；GitHub+Gitee push 成功（见 push_A.log）。

### A5 隔离证据原文
```
test_bu_a_cannot_see_bu_b ... ok   # 乙BU → 403；甲BU 仅本BU行
test_bu_cannot_open_other_tables ... ok
test_admin_sees_all ... ok
```

### run_verify 尾行（A/B 均）
```
✓ 全部通过
```

## 阶段 B · 完全前后端分离（本批落地 P0~P2 基础，P3~P5 未切默认）

| 期 | 状态 | 说明 |
|----|------|------|
| P0 | ✅ | 排名双血条 view+组装；py 路径 vs 同构组装逐字节相等；零运算守卫 |
| P1 | ✅ | cockpit_render_contract_v1.md；模板清单 vs 合同 diff 空；payload.rankings_view |
| P2 | 部分 | body `data-assembled=1`；导出 wait selector；全页 KPI 组装未默认切换 |
| P3 | 未做 | 经营利润大卡 DOM 全等未开工（止损未触发：未两轮失败，是未开干） |
| P4 | 未做 | 依赖 P3 |
| P5 | 未做 | **未删旧路径**（试运行继续 A 形态 SSR；符合总计划「可带着 A 形态+B 半程上线」） |

**止损**：未触发。B 默认路径仍为服务端渲染；JSON 合同与排名试点可并行迭代。

### P0 证据
```
test_py_render_equals_view_assemble ... ok
test_assemble_js_no_money_math ... ok
```

### P1 证据
```
test_every_render_template_mentioned_or_family ... ok
test_payload_has_rankings_view ... ok
```

## 阶段 C · 架构完善（起步）

| 项 | 状态 |
|----|------|
| app_state.py 骨架 | ✅ 已落盘，未全量接线（避免鉴权回归） |
| server 路由拆分 | 未做（侦察报告蓝图仍有效） |
| run_verify 并行提速 | 未做 |

## 红线核对

1. 铁律2：金额 `_disp`/后端算；组装 JS 守卫无四则  
2. A5 BU 隔离：专门测试绿  
3. 公式禁区：A2/A3/A4 仅展示与维度  
4. B 无新 UI 功能（相对 A）  
5. 存储键：预算旧键未改，仅新增税前利润率键  

## 推送

- Phase A：`6814ef8..4d273c6` → origin + gitee  
- Phase B/C：见本批后续 commit push  

## 建议下一迭代

1. 试运行反馈收口  
2. B-P2 全页 KPI 组装 + headless DOM  
3. B-P3 大卡；稳后 P5 真删 SSR  
4. C 接线 app_state + APIRouter 拆 server  
