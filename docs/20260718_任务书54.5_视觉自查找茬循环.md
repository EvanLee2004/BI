# 任务书 54.5：对本批次全部 UI 产出做「截图自查找茬」循环（排队单·跑完当前 goal 后执行）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；tag `stage54p5`。
> 你是执行机器：按序做、不猜、不发挥、不扩范围；有歧义按最保守（不改变任何数字/口径）执行并在报告记疑问。
> **本单性质**：不是新功能单，是**质检返修单**——把你前面 54.x 各批次做的 UI 逐屏照镜子，自己找茬、自己修，修到并排对照挑不出问题才准交。"代码写了≠效果达标"。

## 前科条款（点名具体行为，再犯=整单作废重来）

1. 54 号批次你交付报告称视觉达标，人审当场打回 **8 条**视觉差评（标题对比、卡片布局、图表呼吸感等）——说明你此前**截了图但没有真的看图评判**。本单每张截图必须附「我看到了什么→挑出了什么茬→改了什么」三行文字，缺一行=该项不算完成。
2. 54 号你曾把**整页截图伪影**（趋势图空白）当真实 bug 写进报告——本单凡发现疑似问题，先用视口内截图（非整页拼接）复核一次再下结论。
3. 历史上出现过**行为清单虚假打勾**——本单禁止；每个勾必须有对应证据文件路径。

## 必读输入（按序读完再动手）

1. 本文件全文
2. `原始素材/legacy前端视觉基准_20260718/` 全部定格图（**只许看，绝不复制进 git/交付文档**——含真实数据）
3. `docs/20260718_任务书54.1交付报告.md` 中「8 条人审打回」清单（那是人审的口味基准）
4. `AGENTS.md` / 程序目录 `CLAUDE.md` 铁律清单

## 范围 In / Out

**In**：`frontend/` 下 Vue 前端的样式/模板/主题文件；本单产出的截图与找茬报告。
**Out（碰=作废）**：`src/` 后端任何 `.py` 的数字逻辑；`_golden_data`/`golden/`；`area_series` 等 VM 数据结构；legacy（`static/`）任何文件；`config.json`；不得新增任何依赖库。

## 执行步骤（单屏 LOOP，逐屏勾选）

对以下每个屏，独立走完一轮 LOOP 才进下一屏：**看板主页（深色 1440）→ 看板主页（375 单列）→ 各 BU 页 → 明细页 → 管理端各视图（54.4 产出的 ConsoleView/ManualView/BudgetView/DetailView/SettingsView 等）**。

单屏 LOOP：

1. 起服务（`KANBAN_OFFLINE=1`，合成数据），Playwright 打开该屏，视口内截图。
2. **打开截图，自己看**。与两样东西并排对照：① legacy 基准定格图（8019 同屏）；② 54.1 那 8 条打回意见（同类毛病是否复发）。
3. 写找茬记录：**每屏至少列 3 条茬或明确写"并排对照后无差异"**并说明对照依据。判定句式从严：任何眯眼才看清的字=不合格；任何切换周期/查询时的版面跳动=不合格；任何图表溢出/被裁/留白失衡=不合格。
4. 有茬→改（仅限 In 范围）→重新构建→回到第 1 步复测，直到该屏挑不出茬。
5. 该屏收口：before/after 截图 + 找茬记录落 `docs/pixel/vue54p5/<屏名>/`，commit 一次（一屏一 commit，禁止批量糊完一把交）。

## 自测硬性要求

- 全部截图 = 真浏览器 Playwright 视口截图；HTTP 客户端桩不算数。
- 亮色主题至少抽 3 屏复走 LOOP。
- 全部屏收口后跑 `sh tests/run_verify.sh; echo EXIT:$?`——**判绿只认 EXIT:0，禁止 `| tail` / `| head`**。

## 验收门闩（全勾才算完成）

- [ ] 每屏一份找茬记录（≥3 条茬或"无差异"+依据），落 `docs/pixel/vue54p5/`
- [ ] 8 条历史打回意见逐条核对表：每条写"已复核·现状如何·截图路径"
- [ ] `run_verify` EXIT:0 真实输出贴报告
- [ ] 交付报告 `docs/20260718_任务书54.5交付报告.md`：改动清单 + 找茬总数 + 修复数 + 待拍板清单
- [ ] tag `stage54p5`，只 commit 不 push

## 待拍板 / 非目标

- 审美上拿不准的（如费用图面积 vs 折线）→ 列"待拍板"原样上报，禁止擅自决定。
- 非目标：新功能、后端改动、删 legacy、性能优化（54.4-A 已做）。
