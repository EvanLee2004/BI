结论：全站展开层（构成/其余明细）可点开，B-01～B-05 已关，run_verify EXIT:0，请明昊过目

# 任务书 2.6.11 交付报告 · 全站展开层与真 bug 清零

| 项 | 值 |
|----|-----|
| VERSION | **2.6.11** |
| 日期 | 2026-07-28 |
| 门禁 | `KANBAN_OFFLINE=1 sh tests/run_verify.sh` → **EXIT:0**（`docs/验收证据/2_6_11/gates_run_verify.txt`） |
| 回归红线 | 32 周期逐数字零 diff |

## 关闭缺陷

| ID | 状态 | 证明 |
|----|------|------|
| B-01 | 关 | PLTable `v-if="drawerOpen"` + drawer-empty；测 `test_pltable_drawer_open_without_detail_gate` |
| B-02 | 关 | `structure_for_vm` → `_line_for_vm` 透传 expandable/children |
| B-03 | 关 | `frontend/dist/assets/*.css` 含 `.drawer{` |
| B-04 | 关 | `z-index:var(--z-drawer, 80)`；无 drawer z-index:60 |
| B-05 | 关 | `test_structure_for_vm_preserves_expandable_children` |
| R-01 | 关 | `#periodSync` 默认无 will-change；仅 is-period-switching |

## 活体

见 `docs/验收证据/2_6_11/live/live_notes.md` + 截图 L1–L5。L6 本数据无「其他 N 项」，靠单测。

## O-0

`docs/验收证据/2_6_11/O0_repro.md`：本机 DOM 出且可见；代码层缺陷已坐实并全量修。

## 待拍板

- 拆 admin iframe：未做（默认保证 iframe 内 fixed 可见）
- TopBarActions 内联 fixed / vendor will-change：非同族，未扩

## 说明

任务书要求 drawer 基座进 SPA 且 theme 可兼容；仓库守卫 `test_css_no_dup_classes` **禁止** theme 与 tokens 同名 class，故 drawer 基座**仅**保留在 SPA tokens/dist，theme 删除同名规则（注释指向 SPA）。pl-drow 等明细行仍在 theme。


## 真根因
neon body>* 覆盖 drawer fixed（见 CHANGELOG/代码）。
