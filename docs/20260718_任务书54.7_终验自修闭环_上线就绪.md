# 任务书 54.7：终验 + 自修闭环 → 交出「人审签字即可上线」的成品（排队单·跑完 54.6 后执行）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；最终 tag `stage54p7`。
> 你是执行机器：按序做、不猜、不扩范围。
> **本单目标**：你现在换上**对抗性验收方**的帽子，把 54.4/54.5/54.6 的全部产出当作"别人交来的可疑工程"逐项复测；**查出的问题当场修复**，修完重测，循环直到全部达标（上限 10 轮）；最后交出一份终验报告——**明昊回来只做一件事：人审签字，然后直接部署。不允许再留任何"待修/建议后续"的技术项。**

## 前科条款（点名具体行为，再犯=整单作废）

1. 54 号你自评视觉达标，人审打回 8 条——终验里每个视觉判断必须附「打开截图看了→与基准并排对照→结论」证据行，不许"跑了测试就算看过"。
2. 历史上有**行为清单虚假打勾**——本单每个勾必须有命令+真实退出码或截图路径。
3. 曾把整页截图伪影当真 bug——一律用视口内截图复核后再下结论。
4. **禁止"验收放水"**：你同时是验收方和修复方，最大的风险是"懒得修所以判它过"。判"达标"的每一项都要按最严标准（任何眯眼才看清的字=不合格；任何版面跳动=不合格；任何 grep 残留=不合格）。宁可多修一轮，不许放过一条。

## 必读输入（按序读完再动手）

1. `docs/历史批次/20260718_任务书54.5_视觉自查找茬循环.md` + `docs/历史批次/20260718_任务书54.6_全项目文档补全.md`（验收标的的验收门闩）
2. 54.4/54.5/54.6 各交付报告（口供，仅当线索，不当结论）
3. `docs/历史批次/20260718_任务书54.1交付报告.md` 里的 8 条人审打回清单
4. `AGENTS.md` / 程序目录 `CLAUDE.md` 铁律清单

## 范围 In / Out

**In**：为修复终验查出的问题，可动 `frontend/`、`src/`、`docs/`、`方案与文档/软件工程文档/`。
**Out（碰=作废）**：`_golden_data`/golden 与 32 周期红线数字（零未授权 diff）；`config.json` 口径；不新增依赖库；真实数据/凭据不进 git；**不 push、不删 tag**。

## 明昊人审前置发现（R-00 系列·必修,第一轮最先处理,修不掉不许判通过）

- **R-00a 卡死**：管理端「异常处理→下单未填部门（99+）」点击后页面卡死。排查方向：该入口大概率一次性全量渲染未分页/未虚拟滚动;修复标准=点击后 2 秒内可交互,列表可翻页/滚动,Playwright 真点击验证并附「点了什么→发生了什么」。
- **R-00b 斑马纹（全部 tab,一个不漏）**：管理端「数据调整」下**每一个** tab——收入明细/下单/回款/内部译员/费用明细/人工填写/业绩目标——表格隔行黑白/发灰条纹,暗色主题下刺眼(Element Plus 默认 stripe 色未适配)。明昊确认**所有页面都有此病**。修复标准=统一改一处主题级样式(禁止逐表各改各的),暗色下隔行色差收敛到与看板端表格一致的同色系深浅,亮色主题同步核;**逐 tab 截图留证,7 个 tab 一张不少**;「异常处理」「设置」下若有同病表格一并修。

## 终验清单（每轮全量跑，逐项过；任一不过=修复后进下一轮）

1. **自动化红线**：`sh tests/run_verify.sh; echo EXIT:$?` → 必须 EXIT:0（禁 `| tail`/`| head`）。
2. **端到端**：`KANBAN_OFFLINE=1 python run.py` 全量 → EXIT:0。
3. **视觉终验（真浏览器）**：起服务，Playwright 视口截图走查全部屏（看板主页 1440 深/浅、375 单列、各 BU 页、明细页、管理端各视图）；每屏与 `原始素材/legacy前端视觉基准_20260718/` 定格图并排对照（基准图只看不进 git）；54.1 那 8 条打回逐条复核。每屏写「看到了什么→有无茬→依据」。
4. **交互终验**：三角色登录分流；周期切换 KPI/PL 真变且版面不跳；PL 下钻；排名弹窗；明细翻页/筛选/导出；退出后旧 cookie 401；BU 越权 403；375px 可用。**另外:管理端每个可点击入口(每个 tab、每个徽章/chip、每个按钮)至少真点一次,尤其带大数字徽章(99+)的入口——任何点击后 2 秒内页面无响应=卡死=R 级问题**。每条附「点了什么→发生了什么」。
5. **生产就绪**：git log 里所有声称的删除项逐条 `grep -rn` 零残留；声称保留的回退开关亲手起服务验证可用；鉴权矩阵 curl 抽测（未登录/看端/BU 账号 × 管理端与他人 BU 端点 = 401/403 如预期）。
6. **性能与体验终验（Playwright+CDP 实测，逐项出数字，不达标=R 级修复）**：
   - **首屏**：冷开看板主页与管理端首页，`load` 完成且可交互 **< 3 秒**（本机 offline 条件下）。
   - **交互延迟**：周期切换、tab 切换、下钻展开、弹窗打开——点击到界面响应 **< 300ms**（CDP Performance 计时）；任何 **Long Task > 500ms** 都要归因（哪个脚本/哪次渲染），能消则消。
   - **长会话稳定性**：脚本连续操作 **10 分钟**（循环：切周期×20 → 开关 PL 下钻×10 → 开关弹窗×10 → 切 BU 页往返×10 → 明细翻页×20），全程页面可交互；用 CDP 采 JS 堆内存,**首尾增幅 > 50% 或持续单调上涨=判内存泄漏**,须修。
   - **长表滚动**：收入明细（8000+ 行表）与各"数据调整"tab,快速滚动时无白屏/无秒级冻结;大数据入口(含 R-00a 的 99+ 列表)必须分页或虚拟滚动,禁止一次性全量渲染。
   - **弱机模拟**：CDP 开 **4× CPU 节流** 重跑"首屏+交互延迟"两项,阈值放宽一倍(首屏<6s/交互<600ms)仍须达标——这近似同事的普通办公电脑,不达标=真实用户会卡,须优化(减包/懒加载/减少首屏渲染量)。
   - 全部数字写入终验报告的「性能实测表」(指标|阈值|实测|结论),不许只写"流畅"二字。
7. **文档终验**：54.6 审计表抽 5 条"已补全"核磁盘为真；数字类事实抽 2 个重跑命令验证；README 跑法照着做一遍能跑通。
8. **兼容性终验**：Playwright 用三内核各跑一遍冒烟走查（Chromium=Chrome/Edge、WebKit≈iPhone Safari/微信内嵌、Firefox）：登录→看板→切周期→下钻→管理端一屏,截图对比无布局崩;WebKit + 375 视口重点走（同事手机扫码就是这个环境）。浏览器缩放 150% 与半屏窗口各抽 2 屏,无溢出错位。**亮色主题全屏走查一遍**（不只抽 3 屏）。
9. **边界与鲁棒性终验（坏输入不崩、错误有话说）**：
   - **坏输入**：手填/调整表单里输负数、超长串、纯文字进数字框、`<script>alert(1)</script>` 进文本框——必须被校验拦下或安全转义,提交后页面不崩、不弹脚本（XSS 转义验证）;客户名等数据字段含特殊字符时正常显示。
   - **重复提交**：保存/更新数据按钮快速双击——只生效一次,无重复调整记录（核数据库/调整记录列表）。
   - **会话边缘**：管理端表单填一半 → 另一窗口退出登录使会话失效 → 回来提交——必须有明确"登录已过期"提示,不静默丢数据不白屏。
   - **空态/异常态**：切到无数据的月份/BU——页面出"暂无数据"类友好提示,不是空白或 NaN;数据抓取失败场景（体检面板黄条）文案可读、指引明确。
   - **并发**：两个窗口同时登录（管理员改数 + 看端刷新）——看端无报错,改数后刷新能看到新值。
   - **深链与刷新**：管理端每个子路由直接 F5 刷新、浏览器前进/后退——不 404、不白屏、状态正确。
10. **发布安全扫**：`git diff stage54p3..HEAD` 全量 grep 无真实客户名/金额/内网 IP/账号/密码。

## 留痕规范（所有证据按此归档,乱放=该项不算完成）

- 统一根目录：`docs/验收证据/20260718_54p7/`,按关卡建子夹：`01_红线/ 02_端到端/ 03_视觉/ 04_交互/ 05_生产就绪/ 06_性能/ 07_文档/ 08_兼容/ 09_鲁棒/ 10_安全扫/`。
- 每个子夹一份 `README.md`：本关做了什么/怎么复现（确切命令）/结论,并列出本夹每个文件是什么;截图命名 `屏名_主题_视口[_轮次].png`,日志命名 `内容_轮次.log`。
- 每轮修复清单 `R清单_第N轮.md` 放根目录：R-xx | 问题 | 修复 commit | 复验证据相对路径,表格形式。
- **终验报告是唯一入口**：`docs/20260718_任务书54.7终验报告.md` 里每个结论都用相对路径链接到对应证据;一个不懂上下文的 AI 或人,只读终验报告就能顺着链接核完所有证据。
- 中间产物（临时脚本/废截图）一律放 scratch 不进 docs;`docs/验收证据/` 里只留最终证据。

## 修复纪律

- 查出的问题编号 R-01、R-02…记入轮次清单；**当场修**，每条单独 commit（`fix(54p7-rN): R-xx 一句话`）；修完重跑本轮未过项+红线全量。
- 拿不准的**业务口径/审美取舍**问题不许擅自定：列入「待明昊拍板」清单，但**技术类问题（bug/残留/文档不实）不允许进这个清单**——技术问题只有"修完"一种状态。

## 上线前扫尾清单（第 11 关·同样 R 级修复,不过不许判就绪）

- **控制台清零**：全部走查过程中浏览器 console 无 error;warning 逐条归因,能消则消,消不了的写明原因。
- **登录防爆破**：核对登录接口有无失败限速;若无,加最简单的内存级限速（同账号/IP 连续失败 5 次锁 5 分钟）并补测试;涉及口径不确定就列待拍板,但"完全裸奔"不许过。
- **日志可运维**：服务端错误日志落盘有固定位置、格式含时间与请求路径;确认 python 异常不会静默吞掉;日志文件有大小控制或轮转方案（写进部署手册也算）。
- **版本一致**：`VERSION` 文件、页面页脚/管理端显示的版本号、CHANGELOG 最新条目三处一致,统一升到本次发布号。
- **依赖安全扫**：`pip list` 对照已知漏洞（有 pip-audit 用之,没有则 npm audit + 手查核心包）;npm audit 高危项必须升级或写明不受影响的理由。
- **构建可复现**：删 `frontend/dist` 重新 `npm run build`,产物与 git 里提交的 dist 一致（或重新提交一致版）——保证部署机上构建出来的就是你验收过的。

## 最终文档同步（打 tag 前最后一步·一个不落）

本单修复（R-xx）若改变了任何行为/结构/接口/跑法,收口前把受影响文档**全部**改到与最终代码一致:CHANGELOG（补 54.7 全部修复条目）、README、接口清单、概要设计现状、`2_设计/` 各架构图（SVG 源文件,54.6 刚对齐过,若被本单修复再次改变则再改）、测试说明（用例数变了就更）、部署手册。逐份列「文档终态核对表」（文档 | 是否受影响 | 改/未改+理由）进终验报告——**目标:本单收口后,全项目没有任何一份文档、一张图与代码现状不符,后期零补账。**

## 收口交付（全部达成才打 tag）

- [ ] 终验清单 1~10 全绿，最后一轮零新增 R-xx；证据全部按「留痕规范」归档
- [ ] 终验报告 `docs/20260718_任务书54.7终验报告.md`：**第 1 行写 `结论：上线就绪，待明昊人审签字`**；正文含每轮 R-xx 清单与修复 commit、每项证据（命令+退出码/截图路径）、「待明昊拍板」清单（只许业务/审美项）
- [ ] 人审起服命令写在报告末尾（8018 对照 8019 那套）
- [ ] tag `stage54p7`；**只 commit 不 push**
