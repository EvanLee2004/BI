结论：2.7.1 干净目标态接力，run_verify EXIT:0，回归零 diff，浏览器活体可看，已上机，请明昊过目

# 经营罗盘 2.7.1 交付报告

- **日期**：2026-07-28
- **VERSION**：`2.7.1`
- **基线**：2.7.0（不重做上刀双源/int/文档）

## 勾选

| 项 | 状态 |
|----|------|
| S0 盘点 + 基线绿 | ✅ |
| S1 只 kanban_sid；旧 cookie 不能登录 | ✅ |
| S2 业务读仅 `/api/v1/*`；旧 GET 404 | ✅ |
| S3 只 vue；无 mode==legacy 建造 | ✅ |
| S4 int 分（2.7.0 已做，跳过） | ✅ 跳过 |
| S5 render 零引用死码（本刀无整包删） | ✅ 未盲删 |
| S6 文档三源 + SSOT 指针 2.7.1 | ✅ |
| run_verify EXIT:0 / 回归 32 周期零 diff | ✅ |
| live 截图 `docs/验收证据/2_7_1/live/` | ✅ |
| push 双远端 main / 上机 VERSION=2.7.1 | ✅（上机后勾） |

## 写路径未迁清单（POST，非硬门闩）

仍可为 `/api/*`（前端 adjust 等仍用旧 POST）：

- `POST /api/adjust` · `/api/adjust/batch` · `/api/adjust/{id}/revoke|rearm` · `/api/adjust/expired/revoke_all`
- `POST /api/refresh` · `POST /api/update/apply` · `POST /api/my_passwd`
- 部分已随客户端迁至 `/api/v1/admin/*`：manual/budget/settings/accounts/detax/alloc_rates 等

## 禁碰自检

- 未合并双榜；未合并 detail+ledger；未删 `_legacy_定位键`；未整包删 render；未碰 fetch_zhiyun；未恢复旧 cookie/旧读 URL

## 用户须知

**上机后必须重新登录**；业务读只走 `/api/v1/*`。
