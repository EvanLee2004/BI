# 任务书43 交付总报告

> 产品 **v1.6.0-beta** · 架构与质量收官 · **零 UI / 零口径变化** · 只 commit 不 push

## 阶段 × commit × 证据

| 阶段 | 内容 | 证据 |
|------|------|------|
| ① nginx 双进程 | `deploy/linux/nginx-kanban.conf`；`serve_static`/`server_host` 双模；systemd 环境变量 | `test_task43_nginx_mode`；MADR 0003 |
| ② 存储抽象 | `db_write.py`；业务层零裸 SQL 守卫；换库须知；EXPLAIN 无新建索引 | `test_task43_arch`；`stage2_explain.log` |
| ③ 数据治理 | 运行日志 365d 清理、月末 VACUUM、磁盘红、共享盘只读 | prune/vacuum 单测；share 守卫 |
| ④ 可观测 | 飞书 webhook 静默/失败吞；RotatingFileHandler；登录锁 | notify/login_guard 测；手册 ufw/fail2ban |
| ⑤ 文档 | mermaid+svg 架构/部署；MADR 0003–0005；08 换库须知；CLAUDE 铁律 | 本报告 + 设计图 |
| ⑥ review+起服 | 连续 2 轮无新确凿缺陷；run_verify；:8018 | 见下 |

## EXPLAIN 索引审计（阶段二）

见 scratch `stage2_explain.log`：费用明细/收入/调整均为 **SCAN**，当前规模未建新索引（证据先行，禁止预防性索引）。

## 仅部署机可验

- 真实 nginx 反代全链路、`nginx -t` 完整主配置  
- CIFS 挂载、systemd+cron 到点、fail2ban/禁休眠实机  
- 飞书机器人真实收消息  

## 待明昊拍板

1. 备份异机方案（外置盘/另一台机）  
2. 是否要给 `归属月`/`业务BU` 建索引（数据量上去再定）  
3. 飞书 webhook 正式 URL  

## 阶段六 review

### 第 1 轮 · 正确性
- 发现：ingest `_STD_ORDER` 引用曾漏定义 → 已修  
- 无其它确凿缺陷  

### 第 2 轮 · 安全
- 共享盘只读守卫绿；SQL 业务层绿；告警失败不抛  
- **连续第 1 轮零新缺陷**  

### 第 3 轮 · 资源/一致性
- prune/vacuum 有测；双模静态挂载有测  
- **连续第 2 轮零新缺陷 → 停**  

## 终检

- `run_verify` **EXIT:0**（`run_verify_final2.log`；32 周期红线通过）  
- `test_task42_final` 三账号流程 EXIT:0  
- 主 commit：`e17a3fb` nginx · `9462479` store · `a4aeeaf` ops · `be5b6d0` 原子重建桩测  
- **不 push**；git status 干净  

---

# 服务已起，明昊请开 http://localhost:8018 测试

（直连模式交付测试；生产用 nginx :80 + `KANBAN_SERVER_HOST=127.0.0.1`。账号：管理员 `lushasha` / 整体 `123` / BU `zhengrui` 等，不写密码。）
