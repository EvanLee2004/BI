# 任务书 54.4 · 批次 F 安全留痕（活体）

日期：2026-07-18

## F1 明文密码
- 交付物/源码无密码对照表；账号口令仅本地 `数据/看板账号.json`（gitignore）。

## F2 logout / 会话
- **活体**：管理员 form 登录 → `/api/accounts` 200 → `/admin/logout` → `/api/accounts` **401**  
- 证据：`docs/pixel/vue54p4/sec/results.json`（after_logout=401）  
- Vue 看端 `api/client.ts`：401 → `/login`

## F3 BU 越权
- **活体** BU 登录后：
  - `/api/v1/vm/cockpit` → **403**
  - `/api/accounts` → **401**
  - `/api/detail` → **401**
- 证据：`docs/pixel/vue54p4/sec/bu_403_matrix.json`

## F4 明细白名单
- 既有 `tests/test_task51_authz_access.py` 等守卫；未改口径。

## F5 文档
- 本文件 + 交付报告无真实密码。
