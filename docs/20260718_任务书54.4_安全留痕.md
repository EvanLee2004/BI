# 任务书 54.4 · 批次 F 安全留痕

日期：2026-07-18  
范围：本仓源码/测试/文档（不含 `数据/`、不含真实凭据）

## F1 明文密码

- 测试用默认口令仅经 `server.DEFAULT_PW` / `DEFAULT_VIEW_PW` 与运行时读账号表，**不**进交付文档密码表。
- 源码无硬编码业务账号真实密码；`rg` 扫交付文档无密码对照表。
- 账号口令存本地 `数据/看板账号.json`（gitignore）。

## F2 logout / 改密踢会话

- 既有：`POST /api/v1/logout` bump 会话版本；旧 cookie 401（`tests/test_auth.py` / task51）。
- 管理端：`GET /admin/logout` 同样 bump。
- Vue 看端 `api/client.ts`：HTTP 401 → `location.replace('/login')`。

## F3 BU 越权 403

- 守卫：`tests/test_authz.py`、`tests/test_task51_authz_access.py`、`tests/test_a5_bu_ledger_isolation.py`。
- BU 账号访问他 BU / 整体出口 / 管理写接口 → 403/401。

## F4 明细白名单

- 看端明细：`db.VIEW_EXPENSE_COLUMNS*` 白名单；导出跟随白名单。
- 守卫：`tests/test_task51_authz_access.py` 等；复发三次的坑有回归用例。

## F5 文档

- 本文件 + 交付报告不写真实密码/账号/金额客户名。

## 活体抽查（执行方）

| 项 | 方式 | 结果 |
|----|------|------|
| 无会话 /api/detail | TestClient | 401 |
| 管理员登录后 detail | TestClient | 200 + columns |
| logout 后旧 cookie | 既有用例 | 401 |
| BU 隔离 | 既有用例矩阵 | 绿 |
