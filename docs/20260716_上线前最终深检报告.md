# 上线前最终深检报告 · 2026-07-16

> 基线 `df0074b` → 本轮收口（批次 0–5）。判绿：`KANBAN_OFFLINE=1 sh tests/run_verify.sh` **EXIT:0**（真实退出码，铁律20）。  
> 纪律：确凿才修；拿不准列「待明昊拍板」；不顺手重构。

## 本轮交付摘要

| 批次 | 结论 | 证据 |
|------|------|------|
| 0 登录缓存 P0 | **做了** | `docs/20260716_批次0_登录缓存P0.md`；`tests/test_login_cache_p0.py` |
| 1 陆总#8 月度排名 | **做了** | `docs/20260716_批次1_…`；`tests/test_ranking_monthly_a8.py` |
| 2 publish-once | **做了** | `docs/20260716_批次2_publish_once.md`；`tests/test_publish_once.py` |
| 3 routes 拆分 | **做了** | `docs/20260716_批次3_routes拆分.md`；server 1896→~730 行 |
| 4 质量地基 | **做了** | `docs/20260716_批次4_质量地基.md`；ruff/gitleaks/pre-commit/CI fixtures |
| 5 深检 | **本文件** | 下列 A–F |

**明确未做**：#10 人工填写项改名；#11 去税率折叠；#12 季度目标；不改 profit 口径。

---

## A. 功能与口径

对照 `方案与文档/需求与方案/20260715_陆总走查需求_试运行前改进清单.md` **#1–#9 逐条**（现象 → 证据 → 结论）。代码摘录见 scratch `a_walkthrough_code_evidence.log`。

### A1. 陆总走查 #1–#9

| # | 她要的 | 现象→证据 | 结论 |
|---|--------|-----------|------|
| **#1** | 已交付未回款隐藏可恢复 | `config.json` 默认 `"show_delivered_unpaid": false`；`render_widgets.render_basic` / `render._receipt_insight_totals` 仅当开关 true 才渲染该行；注释写明恢复条件 | **做对**（隐藏非删；可配恢复） |
| **#2** | 回款卡：图加下单柱；右侧**第一行=总下单/总回款**；第二行去掉；三/四保留 | `render.render_receipts` 双柱；`rc_totals.html` 首块文案「总下单 · 总回款」+ `tot_o`/`tot_r`；`rc_recv` 受 #1 开关；`rc_rate`/`rc_bud` 保留 | **做对**（侧栏首行即总下单总回款，非次行） |
| **#3** | （清单：销售利润率等 KPI 命名/口径） | A4 阶段已把结构侧 meta 改系统成本率、业绩目标含税前利润率；`render.py` 利润表有「税前利润率」行 | **做对**（阶段 A 落地；本轮未改口径） |
| **#4** | 销售利润率→税前利润率；目标矩阵 | `render_widgets` KPI 键 `pretax_margin`/`pretax_margin_pct`；管理端业绩目标矩阵页（CHANGELOG A4） | **做对** |
| **#5** | 台账明细开放 BU：含**摘要**列、**年份**、筛选、**BU 严格隔离** | `db` 费用明细列含「事项」（≈摘要）；`query_detail(year=, bu=)`；`data_api` BU 会话强制 `force_bu` 否则 403；`cockpit-bu.js` 带 `year`/`q`/`bu` | **做对**（隔离测试 `test_a5_bu_ledger_isolation`） |
| **#6** | 下单与回款双血条；去按部门 | `render_rankings` 标题「下单/回款 · 按销售/客户」+ `dual-grid`；`test_iter22` `assertNotIn("按部门")`；板块名「下单与回款」 | **做对** |
| **#7** | KPI 目标进度条统一 | `_target_bar` / `kpi_tgt_*`；有目标才显示；A7 CHANGELOG | **做对** |
| **#8** | 排名行 1–12 月 | 本轮：`rankings_monthly`→views `monthly`→`data-monthly`；`test_ranking_monthly_a8` 守恒 | **做对**（本轮新增） |
| **#9** | （清单中若为其它中低项） | 未在本轮扩 scope | **本轮不改**；#10 等陆总 |

### A2. 三种账号 / 已知疑点

| 项 | 现象→证据 | 结论 |
|----|-----------|------|
| 管理员/整体/BU 登录分流 | `test_auth`：管理员→/admin shell；整体→shell；BU→/bu/* + fragments 403 他 BU | **做对**（TestClient；部署机活体仍待上机） |
| ① 看端无退出 | 深检前模板仅 `pwBtn`、无 logout；管理端有 `/admin/logout` | **确凿 bug → 已修**：`logoutBtn` + `POST /api/v1/logout`；`test_login_cache_p0.test_viewer_pages_have_logout_button` |
| ② 极端完成率 % | 宽度 `min(max(pct,0),100)`；文案 `pct_s` 可 >100%；回款侧栏对 >999 显示「目标偏小」 | **定性**：更像目标过小/测试数据；**文案是否一律封顶待明昊拍板** |
| ③ 旧 `/view` | `test_b_p5_no_view` GET 404；shell 无该 URL 字符串 | **无入口** |

## B. 安全

| 项 | 结论 | 证据 |
|----|------|------|
| 会话过期 401→登录 | **有** | shell/shell-bu `status===401` → `/login`；admin.js 同理；`test_login_cache_p0` |
| 改密后旧会话 | **待明昊拍板** | cookie HMAC 不随密码变失效；**不擅自改** |
| BU 越权 | **有** | `test_auth` 他 BU fragments 403；`test_a5` 费用明细隔离 |
| secrets gitignore | **在** | `.gitignore`：`数据/*.json`/`*.xlsx`/`*.db`、密钥文件名 |
| **gitleaks 实测** | **已跑** | 命令：`gitleaks detect --source . -c .gitleaks.toml -v --report-path …/gitleaks_report.json`；**EXIT:1**；**128 findings**（见下节） |
| 依赖 CVE | **人工快查** | requirements 全 `==` 钉死；未跑 pip-audit 全量 |
| **C3 审计抽查** | **两条写路径生效** | 见下节；scratch `c3_audit_two_writes.log` |

### B·gitleaks 跑批（2026-07-16）

```text
$ gitleaks version
8.30.1
$ gitleaks detect --source . -c .gitleaks.toml -v --report-path {SCRATCH}/gitleaks_report.json
# 197 commits scanned; ~9.59 MB in 1.5s
# WRN leaks found: 128
# EXIT:1
```

| 规则 | 条数 | 定性 |
|------|------|------|
| `kanban-forbidden-filename-anywhere` | 122 | **误报为主**：源码/文档**提及** `管理员密钥.json` / `看板账号.json` 文件名（加载路径注释），非提交真实密钥文件；真实密钥在 `.gitignore` |
| `kanban-zhiyun-session-fields` | 6 | 测试/fixture 中合成会话字段字样 |
| 真实 `数据/看板账号.json` 等 | **0 进 git** | gitignore + 路径规则保护 |

**结论**：闸门能跑；当前红是「文件名字符串」规则过宽。后续可 allowlist `src/**` 中文件名字面量，**禁止**全局关规则。全工作区 `--no-git` 会扫到 gitignore 的 `数据/备份/*.html` 海量假阳，**正式验收以 git 历史扫描为准**。

### B·C3 两条真实写路径（TestClient，临时 root）

命令/输出全文：`{SCRATCH}/c3_audit_two_writes.log`。

| # | 写路径 | HTTP | 审计摘要（`manual_配置变更` / `/api/config_changes`） |
|---|--------|------|------------------------------------------------------|
| 1 | `POST /api/settings` `schedule_times=["09:00","17:30"]` | 200 | `类别=设置` · `摘要=设置：更新时间 09:30→09:00、17:30` · `操作账号=lushasha` |
| 2 | `POST /api/accounts` 改 lushasha 密码 | 200 | `类别=账号` · `摘要=账号：lushasha 改密码`；**摘要不含新密码明文** |

库内：`SELECT COUNT(*) FROM manual_配置变更` → **2**。

## C. 数据与回滚

| 项 | 结论 | 证据 |
|----|------|------|
| 回归红线 32 周期 | **绿** | `run_verify` [3/4] 通过 |
| golden / node-assemble | **绿** | `test_b_*_assemble` / shipped 全 OK |
| 台账守恒 | **既有测试覆盖** | BU 隔离/分摊相关 unittest |
| 回滚步骤 | **成立** | 部署手册：`git checkout <tag>` + 看门狗；本轮**不 push**；基线可回 `df0074b` 或本轮 tag |
| 空库引导 F-02 | **有路径** | `/admin` 无 `admin_html` → bootstrap；`test_serve`/admin 相关 |
| DB 备份 | **页面快照有**；库文件靠部署机备份 | `数据/备份/页面_*.html`；SQLite 备份机制如实：**依赖运维拷贝 `看板.db`，应用内无自动全库 dump** |

## D. 配置与部署

| 项 | 结论 | 证据 |
|----|------|------|
| config.json 出厂 | **无本机 Users 路径**；含铁律6 UNC | 深检扫 `paths []`；ledger UNC 出厂 |
| 本地配置覆盖层 | **有** | `loaders.load_config` + `数据/本地配置.json` |
| 一键更新诚实 toast | **既有** | `test_update` / updater |
| 计划任务读合并配置 | **F-02 已修** | `test_schedule` |

## E. 可观测性

| 项 | 结论 | 证据 |
|----|------|------|
| `/api/health` | **端点在 + 红黄可真触发** | 见下造条件抽查；全文 `{SCRATCH}/health_trigger.log` |
| 日志敏感 | **改密不记明文** | C3 写路径2 摘要仅「改密码」 |
| 异常路径 | **有** | refresh `last_refresh` error；HTTPException detail |

### E·/api/health 造条件真触发（3 例）

实现：`src/routes/data_api.py` `api_health` 读 `meta_运行日志` 最新结果 + `summary.meta.unassigned` 升黄 + `health.warnings`。

| 条件 | 操作 | 响应片段（摘要） |
|------|------|------------------|
| **黄** | 运行日志结果=`绿` + `unassigned.count=3` | `"result":"黄"`，`run_reasons` 含「3 名销售未归属 BU…」 |
| **红** | 插入最新日志结果=`红`，体检 `fetch.status=no_source` + `adjust.expired=2` | `"result":"红"`，`run_reasons` 含「收单台账无可用数据源…判红」「2 条调整过期疑似…」 |
| **警** | `meta.health.warnings=["收单台账 2 笔未填…"]` | `"warnings":["收单台账 2 笔未填对应报表大类（合成）"]`（与黄红独立） |

`HEALTH_TRIGGER_OK` 断言通过。

## F. 性能（只记录不优化）

publish-once 后一次 `generate`（`_golden_data`，trigger=`deepcheck-perf`）：

| 指标 | 值 |
|------|-----|
| generate 耗时 | **4.69 s**（`_golden_data`，本机） |
| 全量 HTML 字节 | **18_124_952**（~17.3 MB，含全周期预渲染） |
| client fragments+views JSON | **11_123_783**（~10.6 MB） |

改造前无精确计时基线 → **只记录现状，不对比优化**。

## 本批修复 commit

| 缺陷 | 修法 | 测试 |
|------|------|------|
| 看端无退出 | 模板 `logoutBtn` + JS `POST /api/v1/logout` | 静态守卫 + 已有 logout API |

## 待明昊拍板

1. **回款/目标完成率数字是否强制封顶 100% 显示**（宽度已封顶；极端 % 文案是否保留）。
2. **改密码后是否作废全部会话**（当前 cookie 不因密码变失效）。
3. **#10 人工填写项新名称**（等陆总）。
4. 部署机是否要把 **pip-audit 周跑** 写进运维日历。

## 活体浏览器

本实现环境：**无稳定 GUI 浏览器缓存复现**；登录缓存以 TestClient 头/body 翻转 + 部署机普通导航自验为准（见批次0 文档）。  
`browser_limit.log` 记录该限制。

## 总验收清单（对照任务书）

- [x] 每批 commit 中文一句话、只 commit 不 push  
- [x] `run_verify` EXIT:0（最终 log：`run_verify_final.log`）  
- [x] golden + 红线 32 周期  
- [x] docs 如实标注做了/没做（含概要设计 2026-07-16 边界）  
- [x] 陆总 #1–#9 逐条复检表（本节 A）  
- [x] gitleaks 实测命令+EXIT+摘要  
- [x] health 红黄造条件触发 3 例  
- [x] C3 两条写路径对照  
- [ ] 部署机三种账号普通导航活体（需上机；本环境诚实标注未做）  
- [ ] push + 上线（**外部 Claude 逐批验后**）  
