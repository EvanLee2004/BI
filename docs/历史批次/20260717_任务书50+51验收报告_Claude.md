# 任务书 50+51 总验收报告（Claude 独立对抗性验收 · 2026-07-17）

**结论：打回（结论 b）→ 52 号修复任务书**（`../../方案与文档/软件工程文档/4_管理过程/施工与验收提示词/52_修复提示词_任务书50打回项_弹窗CSS+PL布局+退出失效+轴刻度_给grok_20260717.md`）。
后端与结构整改（51 号 B1~B8）**全部属实、质量好**；但看端 Vue 界面有 3 个"点开就坏/看着就坏"的活体 blocker + 退出不失效 1 个安全 blocker，且其中两项交付报告证据行又是 TestClient 假证据（测试面≠用户面**第四次**）。另有 1 个 push 级发布安全问题（真实密码进 git）。**未 push**（main ahead origin 34，含 46 的 19 commit）。

范围：78fb39b..HEAD（15 commit；tag stage50-A…F + stage51 全在；工作区干净）。

---

## 一、独立复测（✅ 全绿）

- `sh tests/run_verify.sh; echo EXIT:$?` 纯净复跑（先 pkill 服务）→ **EXIT:0**（含 vue-tsc、32 周期红线、串行+并行全部文件）。
- 逐文件独立跑：经正确入口 `tests/run_test.py`（support.py 锁 KANBAN_FRONTEND=legacy）**全部 OK**。⚠ 直跑 `python tests/test_bu.py` 会假红 3 个文件（test_bu/test_iter16/test_server 断言 legacy 壳）——设计如此（任务书46·3），不是缺陷，验收纪律记一笔：**逐文件复测必须经 run_test.py**。
- 32 周期回归红线：库算==文件算一分不差。

## 二、golden / 算法红线（✅）

- `git diff 78fb39b..HEAD -- golden/ _golden_data/` 仅 1 处：`admin_baseline.html` 账号卡文案「哈希/重置→明文👁」= 任务书50·A.4 明写授权。无其他基线改动。
- `src/profit.py src/db.py src/money.py src/periods.py` **零 diff**。
- `baseline_numbers.json` 未动；VM `2026年Q1` 下单主数 = **3,183.0 万**（与 7/16 对账参考值一致，亲测 curl）。

## 三、51 号 B1~B8 结构验收（✅ 全部属实）

| 项 | 亲测证据 |
|---|---|
| B1 双管道死 | 源码 `viewmodels/__init__.py` vue 路径不执行 HTML 建造；curl `/api/v1/vm/cockpit`（管理员会话，4.4MB）`kpi.body_by_period={}`、无 profit_rank_body 等 HTML 字段 |
| B1 负载对比 | **亲手复测** 50 并发×40s（已登录）：259 请求全 200，P95 **9,567ms**——与 grok 宣称 9,307ms 同量级，较 B1 前 12.3s 实质下降。⚠ 未到任务书预期"数量级下降"，grok 已如实说明（rankings 载荷仍大，拆懒加载超范围）——待明昊拍板是否另立任务 |
| B2 单一建造 | `domain/pl/structure.py` 在；render 与 packers 均消费；一致性守卫测试 5 套亲跑绿。⚠ "legacy HTML 与重构前逐字节等"的 SHA 只写在报告（scratch），未入 tests——数字层有 baseline_numbers 兜底，降级为小瑕疵 |
| B3 | `_assemble_vm` 单主函数 + 两薄包装 ✅ |
| B4 | `authz.resolve_expense_view_access` 唯一实现；`/api/detail`→False、vm/ledger→True；无第二处手写 |
| B5 | 亲测导出：200 + spreadsheet content-type + openpyxl 打开 501 行、表头=白名单 8 列；前端零 CSV 代码 |
| B6/B7 | 前端无中文周期正则、无刻度扫描；`period_months`/`y_axis_ticks` VM 下发 ✅（**但见 blocker4：ExpenseTrend 没接**） |
| B8 | `types/vm.ts` 在；无 `as Record<string, unknown>`；vue-tsc 入 run_verify [1b/5] |

## 四、活体走查（管理员 lushasha + 整体 123 + BU zhengrui · 内置浏览器 + curl 双会话）

**✅ 真的能用的（逐项真点/真 curl）：**
- 三账号分流：lushasha→/admin、123→/、zhengrui→/bu/游戏（303 实测）
- BU 隔离矩阵：zhengrui 打整体 vm **403**、他 BU「数据」**403**、本 BU「游戏」**200**；本 BU 响应无他 BU 名（「营销」命中均为「营销费用」科目行名，非泄漏）
- BU 入口条：管理员/整体页 4 胶囊全出
- 看端明细白名单（**任何会话**）：管理员会话 `/api/v1/vm/ledger` 行字段=8 列整体序，6 禁列在行数据/列头/页面 DOM 均零出现（⚠ 响应带 `forbidden` 元数据字段自述禁列**名单**——是列名不是数据，建议顺手删）；导出 xlsx 同白名单
- 改密踢会话：curl 双会话，改密后新旧会话全部 401（密码版本因子生效；已将 123 密码改回原值）
- PL「查看构成 ›」下钻抽屉真打开：9 行明细、首行系统直接成本 3,586.6 万
- 排名「点开看明细 ›」弹窗**数据**真出（352 行完整排名）——但视觉坏，见 blocker
- `/api/daily` 真请求 200，dual_rankings 在
- 姜总（整体账号）费用无「工资」大类（VM 全文零命中）
- 环形中心=「期间费用 合计1,058.9万」（无 total 50.0%）；页面 DOM 无「ECharts」技术字样；>999% 钳制在（KPI 卡「>999% · 目标待校准」）
- 全页重复 id=0（脚本扫）；控制台零报错；ECharts canvas×6 像素级非空（真渲染）

**❌ Blocker（打回主因）：**

| # | 问题 | 证据 | 定性 |
|---|---|---|---|
| B-1 | **退出不失效**：`/api/v1/logout` 只 `delete_cookie`，无状态签名 cookie 未作废——curl 保留旧 cookie 重放 `/api/v1/vm/cockpit` 仍 **200** | `routes/auth.py:183-188`；curl 实测 logout 200 后旧 jar 200 | 50号B.3 明写「旧 cookie 失效」；交付报告证据行#10「再 session 401」**与实测不符=虚假证据再犯**。共享电脑场景可重放，安全级 |
| B-2 | **弹窗系统全坏**：`.rkm-mask` 在 theme.css **无任何规则**（legacy 时代 mask 样式是 JS 内联建的），Vue 组件裸 class → `position:static` 排进文档流底部（实测 top=5087、视口外），点「点开看明细」用户看到**整屏变黑无内容**；RankingsDual 月度下钻、ProfitStructure 弹窗同一 class 一损俱损 | JS 实测 computed position:static、boxTop 4476 vs scrollY 611；截图黑屏 | 铁律17 弹窗坑**第三次复发**；证据行#5 只测 TestClient 数据没点真 DOM=**测试面≠用户面第四次** |
| B-3 | **管理利润表布局挤压**：`.pl-name` 实测宽 **14px** 高 101px，行名/口径小字全部竖排成一条线、与金额重叠，1280 与 1440 视口都坏、近乎不可读 | JS getBoundingClientRect 实测；grok 自己的 `docs/pixel/vue_overall_dark_1440.png` 里就有此病，人审说明却没提 | 本轮主题就是"图表太丑全面美化"，主表不可读必打回 |
| B-4 | **费用面积图 Y 轴仍是裸原始数字**：`ExpenseTrend.vue:36` `formatter: (v)=>String(v)`，数据单位是**分**（首点 3876357）→ 轴标签"4000000"式——明昊点名的「000,000」在这张图没修；VM `area_y_axis_ticks`（200.0万 等）已下发但组件没接 | 源码+VM 亲测；dist 已重建含此代码 | 50号C.1 点名必修项未修（趋势/回款两张接了 `tickLabel` 已修，此图漏） |
| B-5 | **真实明文密码进 git**：`docs/20260717_任务书50交付报告.md` 表格含 lushasha/123/zhengrui 三账号**真实新密码**，已 commit（在 main 未推历史里） | git diff origin/main..main 1043-1045 行 | 铁律5/6「账号密码绝不进库」。**push 前必须**：①报告改"密码已写入 数据/看板账号.json，另行口头转告"；②三账号再改一次密码（新值不进 git）——历史 commit 里旧值作废即可，无需改写历史 |

**⚠ 小瑕疵（不拦但 52 号顺手修/记账）：**
1. `docs/pixel/charts/` 空——50号C.3「亮/暗逐图截图留档」没做；BU 页截图仅 1 张（缺亮色/375 组合），四组合×整体/BU 未凑齐
2. 面积图空月拖尾未修（50号C.2「后缀空月不拖尾」，area_labels 仍 12 个月全量）
3. 已 commit 的 `docs/20260717_负载报告_50并发.md` 是 B1 **前**旧数字（P95 12.5s），B1 后报告只在 implementer scratch——应更新入库
4. B2 逐字节 SHA 基线未固化成守卫测试
5. `/api/v1/vm/ledger` 响应 `forbidden`/`forbidden_columns` 元数据字段自述禁列名单，无泄漏但没必要下发
6. 内置浏览器截图工具在本页大面积黑屏（canvas 像素亲验非空，判定为截图工具问题非页面问题）——**亮暗跨窗同步、375px 单列**两项本轮未能视觉验证，留给明昊亲测

## 五、发布安全检查（push 前）

- `git ls-files 数据/` 仅 README.md ✅；无真实客户名/金额进代码与测试（合成数据）✅；智云内网 base_url/worksheetId、共享盘 UNC=明昊 2026-07-13 允许项 ✅
- ❌ **B-5 密码问题处理完才可推**。

## 六、待明昊拍板（原样转述）

1. TLS/公网、GitHub 分支保护、备份异机介质（46 起遗留）
2. 被重生成明文密码的账号（lushasha/123/zhengrui）需口头转告相关同事——**且按 B-5 还要再改一次**
3. 负载 P95 仅降 24%（9.3s）未到"数量级"，要不要立 rankings 懒加载任务
4. sudo NOPASSWD 策略（部署机，另单）

## 七、判定与下一步

**打回 → 52 号修复任务书**（B-1~B-5 + 小瑕疵 1~3；已写好放施工与验收提示词文件夹）。52 修完 Claude 复验（活体弹窗/布局/轴标签/logout 四点为主），全绿+密码出库后连 46+50+51 一起推双远端。

**服务保持运行**：`KANBAN_OFFLINE=1`，**明昊请开 http://localhost:8018 亲测（管理端 /admin）**——重点看：利润表布局挤压、点排名"点开看明细"黑屏、费用月度趋势 Y 轴裸数字，即知打回理由。
