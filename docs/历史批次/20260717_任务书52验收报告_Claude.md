# 任务书 52 验收报告（Claude 独立复测 · 2026-07-17）

**结论：通过（结论 a）——附 Claude 顺手修复 1 个 grok 未修的数字级缺陷后，全量绿。可待明昊亲测 8018 后推送**（push 前无遗留安全闸，密码已出库+已轮换）。

范围：stage51..HEAD（stage52=b484e42 + Claude 修复 commit）。main ahead origin 38+。

## 逐项亲测（不信报告）

| 项 | 结果 | 亲测证据 |
|---|---|---|
| F-1 弹窗 | ✅ | Playwright 真登录真点「点开看明细」：`.rkm-mask` computed **position:fixed top=0**；弹窗盒 560×720 落在视口 (top=90)；352 行内容；theme.css 524-533 规则在；两组件均 Teleport body |
| F-2 PL 布局 | ✅ | `.pl-name` 实测 **457×18px 横排**（此前 14×101 竖条）；grid `14px minmax(120px,1fr) auto`；375 媒体查询单列 |
| F-3 退出 | ✅ | curl 双会话：logout 后旧 cookie 重放 `/api/v1/vm/cockpit` → **401**（bump_session_version 密码版本+1，复用改密踢会话机制，MADR-0011） |
| F-4 面积图轴 | ✅ | VM `area_y_axis_ticks` 已接（`tickLabel` 同趋势/回款图）；活体 ticks=`0/200.0万/400.0万/600.0万`；`area_labels` 裁到 7 月（拖尾修了） |
| F-5 密码出库 | ✅ | 50 交付报告全文无三密码串；三账号已再轮换（新值仅在 `数据/看板账号.json`，与旧泄漏值全不同）。⚠ 守卫测试把**旧密码**硬编码进 test_task52_fixes.py 进了 git——因已轮换成死值，不构成活泄漏，下轮顺手改成读账号文件比对 |
| F-6 | ✅ | ledger 响应无 forbidden 字段（cockpit.py:197）；负载报告已更新；PL SHA 固化；像素截图 docs/pixel/vue/ 6 张 |
| run_verify | ✅ | 纯净复跑 **EXIT:0**（修复前后各一轮） |
| golden/算法 | ✅ | stage51..HEAD golden/_golden_data/profit/db/money 零 diff |

## 验收中发现并由 Claude 顺手修复的缺陷（grok 52 轮未覆盖）

**环形图混入 total 扇区、全部占比被稀释一半（数字级）**：`_pack_donut_by_period` 遍历 `expense` 字典只滤 `_` 前缀键，没滤合计键 `total` → total 既进分母又成扇区，五大类占比全部减半（营销真实 18.8% 显示 9.4%），图上还挂「total 50.0%」标签——50 号 C.1 点名项其实只修了中心文案没修数据（legacy 渲染用大类白名单天然没这毛病，仅 Vue VM 线有）。
修复：packers 跳过 `total` 键（对齐 legacy 白名单语义）+ 新增守卫测试 `test_donut_items_no_total_and_pct_sums_100`（扇区无 total、各周期 pct 合计≈100%）。修复后活体：`营销费用:18.8% / 管理费用:33.2% / 固定运营30.5% / 研发16.9% / 财务0.7%`，合计 100%。

## 小瑕疵（记账不拦）

1. CHANGELOG 未补 52 条目（本次 Claude 修复一并补记）。
2. test_task52_fixes.py 硬编码旧密码串（死值，无害，下轮改读文件）。

## 下一步

明昊开 **http://localhost:8018** 亲测（重点：排名弹窗、利润表布局、费用面积图轴、环形占比、退出）→ 说"推"后按第六节流程 push 双远端（只推 main，绝不 --tags）→ Ubuntu 26.04 部署。
