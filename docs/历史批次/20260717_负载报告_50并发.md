# 负载报告 · `/api/v1/vm/cockpit` · 50 并发

> **任务书52·F-6 更新（2026-07-17）**：下文以 **任务书51·B1 杀双管道之后** 实测为准；文首附 B1 前后对比。

## B1 前后对比（同机·已登录会话·50 并发 × 约 2 分钟）

| 指标 | B1 前（双管道 HTML+VM） | B1 后（vue 仅结构化） |
|------|-------------------------|------------------------|
| P50 | ~11077 ms | ~8453 ms |
| **P95** | **~12277 ms** | **~9307 ms** |
| P99 | ~12977 ms | ~10645 ms |
| 响应体约 | ~6.8 MB | ~4.4 MB |
| 全 200 | 是 | 是 |

说明：P95 实质下降约 24%，未达「数量级」——rankings 结构化载荷仍大；懒加载另立项。

---

## B1 后实测明细（采纳为当前基线）

- 路径：`GET /api/v1/vm/cockpit`
- 并发：50
- 时长：120s 级
- HTTP：全部 200
- **P95 ≈ 9.3 s**
- 脚本：`scripts/load_vm_50.py`（有则）

B1 前旧数字（P95 ~12.5s 档）仅作对比，不再作为当前发布基线。
