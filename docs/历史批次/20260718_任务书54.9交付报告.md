结论：美学终修完成,待明昊终审

# 任务书 54.9 交付报告 · 美学终修（全端无可挑剔）

- **日期**：2026-07-18  
- **tag**：`stage54p9`（只 commit 不 push）  
- **前置**：`stage54p8`  
- **范围**：纯视觉（theme/scifi-bridge/admin CSS · 静态 login HTML · echarts 轴 · dist · pixel/手册）；**零**口径/VM/golden 数值/算账逻辑  
- **工具**：Playwright 实测 computed style + WCAG 对比度公式；色值来源 `getComputedStyle`

证据根：`docs/pixel/vue54p9/`（`audit_measured.json` ≡ `audit_remeasure.json` 最新复测，**FAILS=[]**）

---

## 1. 分屏全绿审计表（规则 | 位置 | 实测值 | 判定）

### 1.1 看板登录 `login/after/dark_1440.png`

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| R6 主 CTA 色 | `button[type=submit]` | `linear-gradient(90deg, rgb(8,145,178), rgb(34,211,238))`（scifi 青，非 #8b5cf6） | **达标** |
| R1 间距 | `.card` padding | 32px；input margin 8/16 | **达标** |
| R4 字号 | label/hint | ≥12px | **达标** |

### 1.2 看板主页暗 1440 `home/after/dark_1440.png`

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| R1 KPI gap | `.kpi-grid` | **16px**；五卡同级一致 | **达标** |
| R3 KPI 大数 | `.kpi-v > b` | **32px** | **达标** |
| R4 无 <12 | `.kpi-tgt-h/.cock-ledger th/.ev-tab/...` computed | `[]` | **达标** |
| R4 对比度 | `.sec-t` 系 | 暗色 ≥4.5:1（实测见 json） | **达标** |
| R6 圆角 | `.scifi-panel` | 10px | **达标** |

### 1.3 亮色主页 `light/after/home_1440.png`

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| R4 对比度 | `.kpi-v b` | **16.49:1** | **达标** |

### 1.4 375 主页 `mobile375/after/home.png`

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| R7 触目标 | 全页 `button` 滚动 0~2200 | **lows=[]**（均 ≥44px） | **达标** |

### 1.5 BU 页（账号 `bu_only` / 营销）

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| 登录分流 | POST login | `redirect=/bu/营销` | **达标** |
| 页壳渲染 | GET `/bu/营销` | golden **OFFLINE** `bu_pages` 无「营销」预渲染 → API **404**（非样式） | **待明昊拍板**（真数据环境人审视觉；截图 `bu/after/dark_1440.png` 为 404 证据） |
| R1–R7 样式 | scifi/theme | BU 与主页共用 dist+theme，无独立破格 CSS | **达标** |

### 1.6 明细/台账区 `detail/after/ledger_area_1440.png`

| 规则 | 位置 | 实测值 | 判定 |
|------|------|--------|------|
| R4 th 字号 | `.cock-ledger th` | **12px**（已非 11.5） | **达标** |

### 1.7 管理端（after 全套）

| 页 | 截图 | R6 EP primary | 其它 |
|----|------|---------------|------|
| orderdept | `admin/after/orderdept_dark_1440.png` | rgb(34,211,238) **达标** | |
| manual | `admin/after/manual_dark_1440.png` | 同上 | |
| settings | `admin/after/settings_dark_1440.png` | 同上 | **R5** `.settings` maxWidth=**960px** width=960 **达标** |
| budget | `admin/after/budget_dark_1440.png` | 同上 | |
| detail | `admin/after/detail_dark_1440.png` | 同上 | |
| ledger | `admin/after/ledger_dark_1440.png` | 同上 | |
| unclassified | `admin/after/unclassified_dark_1440.png` | 同上 | |
| audit | `admin/after/audit_dark_1440.png` | 同上 | |
| admin 登录 | `login/after/admin_login_dark_1440.png` | 静态页主钮 scifi 青 | **达标** |

---

## 2. 源码级门禁（单测 `tests/test_task54p9_aesthetics.py`）

| 断言 | 说明 |
|------|------|
| theme + scifi-bridge **无** `font-size < 12px` | 全文件扫描，非抽查 |
| theme + scifi-bridge + admin **padding/margin/gap** 仅 4/8/12/16/24/32/48/64 | 全文件扫描 |
| `.admin-root .settings { max-width: 960px }` | R5 |
| `static/view_login.html` 主钮 `#22d3ee` 渐变 | R6（真实登录页） |
| echarts axisLabel ≥12 | R4 |

---

## 3. 前科五项

| # | 顽疾 | 自查 | 证据 |
|---|------|------|------|
| 1 | 文字眯眼 | 源码扫描 font-size&lt;12 = 0；台账 th 12px | 单测 + detail 实测 |
| 2 | 版面跳动 | 未改查询/布局逻辑 | diff 无业务 ts 行为 |
| 3 | EP 裸奔 | 主色青 + 斑马沿用 54.7 | admin 截图 primary |
| 4 | 截图不看图 | 每屏表带像素/色值 | 本报告 §1 |
| 5 | 修一处漏一处 | 全局 snap 间距/字号 + 静态 login 双份 | theme/scifi/static login |

---

## 4. 优于基准的偏离

| 项 | 规则 | 说明 |
|----|------|------|
| KPI 35.2→**32px** | R1/R3 8pt | 同步改 `test_task46` 期望 |
| 登录钮 **紫→青** | R6 | `static/view_login.html` + `admin_login.html` |
| gap/pad 非网格→**网格** | R1 | 全文件 snap |
| settings **960 居中** | R5 | `.admin-root .settings` |
| ReceiptsCard/图表 label **11→12** | R4 | 看端辅文与 ECharts 数据标签 |

## 4b. 待明昊拍板（正式清单）

| 项 | 现状 | 说明 |
|----|------|------|
| **BU 页视觉人审** | golden OFFLINE `bu_pages` 无「营销」预渲染 → GET `/bu/营销` **404** | 登录分流 `redirect=/bu/营销` 已达标；样式与主页同源 token。**真数据/正式环境**起服后人审 BU 截图签字。证据：`pixel/vue54p9/bu/after/dark_1440.png`、§1.5 |

样式规则本身无互斥硬改项；上表仅为环境预渲染缺口。

---

## 5. 红线

| 项 | 结果 |
|----|------|
| `run_verify` | **EXIT:0**（收口时重跑） |
| diff vs stage54p8 | 样式/static login/frontend dist/docs/pixel/tests；无 golden 数值、无算账 py |
| push | **否** |

---

## 6. 非目标

push · 部署 · 新功能/动画 · 改 run_verify 本体 · 改 golden 数字。
