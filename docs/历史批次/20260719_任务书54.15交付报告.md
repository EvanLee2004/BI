# 结论：口径统一完成,请明昊复审

> tag `stage55_rc8` · 版本 `2.0.0-rc8`

## 改动
- **R-30**：`domain.expense.chart_whitelist` 公共白名单；`_pack_expense_area` 与 HTML 趋势共用过滤；「成本」「非利润表」不进折线/热力；环形与利润表路径未改。
- **R-31**：热力图 tooltip.confine、grid.top=28、visMap bottom 留白。
- **R-32**：全站主要图表 tooltip confine；费用明细表头口径小字。

## 建议（热力图去留）
热力图在「月份×大类」扫异常上有价值，修缮后可读性明显提升；**建议保留**，若陆总觉得信息过载再下线为可选项（本单未删）。

## 验证
- `tests/test_task54p15_chart_whitelist.py` 通过
- `sh tests/run_verify.sh` EXIT:0
