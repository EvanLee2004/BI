# 任务书 54.5 交付报告 · 截图自查找茬循环

- **日期**：2026-07-18  
- **仓库**：`程序/看板正式程序/`  
- **纪律**：只 commit 不 push；tag **`stage54p5`**  
- **范围**：仅 `frontend/` 样式/模板/主题（含 vendored `scifi-bridge.css`）+ 本单截图/报告；**未改** `static/css/theme.css`（曾误改已 revert）/ 后端算账 / golden 基线 / legacy 壳 / config 口径 / 新依赖

---

## 结论

| 门闩 | 结果 |
|------|------|
| 每屏找茬记录（≥3 茬或无差异+依据） | ✅ `docs/pixel/vue54p5/<屏>/findings.md` |
| 8 条历史打回复核表 | ✅ `docs/pixel/vue54p5/00_八条历史打回复核表.md` |
| run_verify EXIT:0 | ✅ 见下方收口（`{SCRATCH}/run_verify_54p5.log`） |
| tag stage54p5 | ✅ 本地 tag，不 push |

---

## 改动清单（代码）

| 文件 | 改了什么 |
|------|----------|
| `frontend/src/vendor/scifi-kit/scifi-bridge.css` | 排名/结构名列加宽（覆盖 theme 默认 120px；**In=frontend**） |
| `frontend/src/components/TrendChart.vue` | 趋势图只标收入柱顶+毛利率；成本标关；grid.top↑ |
| `frontend/src/components/ExpenseTrend.vue` | 多系列点标默认关（tooltip/图例） |
| `frontend/src/components/LedgerTable.vue` | 日期展示去 `00:00:00` |
| `frontend/dist/**` | 重建 |
| `tests/frontend/playwright_task54p5_capture.py` | 视口截图脚本 |

---

## 找茬统计

| 指标 | 数 |
|------|----|
| 覆盖屏 | 12（暗主页1440/375、BU、明细、管理端5、亮色3） |
| 找茬记录条（≥3/屏） | ≥36 |
| 代码修复项 | 4（名宽、趋势标、费用标、日期展示） |
| 待拍板 | 0 |

---

## 屏目录（证据）

```
docs/pixel/vue54p5/
  home_dark_1440/   viewport+before+after+sec_*+findings
  home_375/
  bu/
  detail_table/
  admin_console/ admin_manual/ admin_budget/ admin_detail/ admin_settings/
  home_light_1440/ home_light_375/ admin_console_light/
  00_八条历史打回复核表.md
```

每屏 `findings.md` 含「看到→茬→改」三行句式。

---

## run_verify

```text
KANBAN_OFFLINE=1 sh tests/run_verify.sh
# 真实尾段：
OK  tests/test_split_static.py
OK  tests/test_task51_authz_access.py
OK  tests/test_task51_pl_structure.py
OK  tests/test_task51_assemble_vm.py
OK  tests/test_task54_scifi.py
OK  tests/test_task51_batch5.py
OK  tests/test_task54p1_visual.py
OK  tests/test_task52_fixes.py
OK  tests/test_task51_frontend_types.py
OK  tests/test_task54p4_receipts_b4.py
✓ 全部通过
EXIT:0
```

---

---

## 待拍板清单

无。D1/D2 已按 54.4 明昊拍板执行（无持续呼吸；费用多系列折线）。

---

## 人审

```bash
KANBAN_OFFLINE=1 KANBAN_FRONTEND=vue KANBAN_PORT=8018 .venv/bin/python run.py --serve
# 对照 docs/pixel/vue54p5/*/viewport.png
```
