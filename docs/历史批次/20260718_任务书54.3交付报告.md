# 任务书 54.3 交付报告（像素级+行为级复刻 legacy 前端 · CC 执行）

> 日期：2026-07-18 · 执行：Claude Code · tag `stage54p3` · **只 commit 不 push**
> 数据卫生：本单一切**含真实数据**的自测/对照截图**只留本机 scratch + `原始素材/…/interactions/`（仓库外）**，绝不进 git；`docs/pixel/vue54p3/` 内证据一律用 **`_golden_data` 合成数据**生成（合成人名/客户名，可进仓库）。
> **本报告严格区分「已达标 / 进行中 / 未做 / 待拍板 / 需后端」，无虚假打勾。**

## 一、本次实际完成（已验证）

### ① 阶段0：legacy 老前端实录成图纸 —— 已完成
- 起 legacy(8019)+vue(8018) 双服务，Playwright 逐屏+逐交互实录 legacy。
- 产出 `原始素材/legacy前端视觉基准_20260718/interactions/`（仓库外，含真实数据）：B-01 三态、周期面板/Q1/单月/自定义区间、费用四态、排名弹窗、利润表抽屉、密码弹窗、主题浅色、BU 页等 20+ 张。
- 产出 `原始素材/…/复刻清单.md`：每行=一状态/交互，三列 基准图|状态|证据。**这是进度真相表。**

### ② 阶段2 · B-01 按时间段查询版面跳动 —— 已达标（点名必修）
- **legacy 实录行为（最终基准）**：查询后**回款情况总图不消失、不挪窝**；累计摘要条不动；**只有下单/回款排名双卡原位切换**为区间结果并标注区间；返回默认（年）一键恢复。卡序不变、无版面重排。
- **vue 修前（明昊亲测 bug）**：点查询后 DailyQuery 面板内**新增**区间双卡、下方默认排名双卡**仍在**（两套并存）→ 页高 4713→**5278（+565px）**→版面下移重排、回款总图被挤。
- **修法（纯前端·frontend/src）**：
  - `stores/cockpit.ts`：新增 `dailyActive/dailyRange/dailyDual` + `setDaily/clearDaily`；`setPeriod` 切周期即 `clearDaily`。
  - `components/DailyQuery.vue`：**删除面板内自渲染的区间双卡**，查询成功写 `store.setDaily`、返回/失败 `clearDaily`；只保留查询控件+摘要。
  - `components/RankingsDual.vue`：`dailyActive` 时**原位**用**同一 `dualRankBarOption`** 渲染区间双卡、标题追加「· 区间 起~止」；否则默认排名。`data-source="rankings_view"` 保持静态（守卫测试），另加 `:data-daily`。
- **验证（合成数据，`docs/pixel/vue54p3/B01_行为对照.png`）**：默认/查询/返回三态**页高恒等**（真实数据 4713/4713/4713；合成 4452/4452/4452）=**无跳动**；回款总图三态同位；排名原位切区间带标注；**控制台零错**。

### ③ 阶段1 · 回款情况总图补金黄回款率线 —— 已达标
- 对照 legacy：回款总图除下单/回款双柱外，有**金黄回款率%折线（右轴0-100%）+ 点标签**。vue 修前只有双柱。
- **修法（纯前端·用现有 VM 字段 `receipts.ratio_pct_disp`）**：`components/ReceiptsCard.vue` 加第二 Y 轴(0-100% `{value}%`)+金黄线系列(`#fbbf24`·点发光·呼吸 effectScatter)，**几何取自后端已下发的百分比显示串、标签直接用该串——前端零金额/比率运算（铁律2）**；保留双柱。
- **验证**（`docs/pixel/vue54p3/receipts_gold_line.png` + 与 legacy 并排 scratch）：金线轨迹与 legacy 一致（真实数据 82.2/88.5/34.8/44.6/49.4/66.9/62.3% ≈ legacy 82/88/35/45/49/67/62%）；控制台零错。

### ④ 门闩
- `sh tests/run_verify.sh; echo EXIT:$?` = **EXIT:0**（60 文件全绿 + 32 周期回归红线一分不差 + vue-tsc --noEmit 通过）。
- 触及的守卫测试全绿：`test_task54p1_visual`（B-01 不变式已更新为「setDaily/dailyDual」）、`test_parity_structure`（data-source/无金额四则）、`test_daily`（legacy 壳未动）、`test_task54_scifi`、`test_receipts_dual_bars`（双柱保留）。

## 二、未做 / 进行中（如实列出，**未打勾**）

| 项 | 状态 | 说明 |
|---|---|---|
| 阶段1 逐卡像素复刻（KPI/趋势/环形/利润表/费用/结构/排名/明细/顶栏/登录/标题条） | **进行中·未逐卡收口** | stage54p2 已把整体气质做近；本单仅完成回款卡金线一项逐卡收口，其余卡未做「新旧并排到列不出差异」的完整自虐循环 |
| 阶段1 亮色/375/BU 页各一轮 | **未做** | |
| 阶段2 其余交互（周期联动/利润表抽屉/费用四态/排名弹窗/密码弹窗/亮暗/明细翻页筛选/BU） | **未做** | 仅 B-01 完成；基准图已备（复刻清单 C02~C09） |
| 阶段3 删前端废码 + bundle 体积记录 + 重复id扫描 + 375无横滚 | **未做** | |
| B03 收入·毛利趋势系列口径 | **未做·待核** | legacy 画「交付毛利/生产毛利」，vue 画「收入/成本」；待核 VM 是否有对应字段 |

## 三、需后端配合（前端-only 范围外）
- 回款情况：**月均预算虚线**（VM `receipts_budget` 空）、**累计与回款摘要条**（VM `summary_by_period` 空）——这两个字段 legacy 用服务端 HTML 出、vue VM 契约里**已存在但未被 packer 填充**。需后端 packer 填充这两个**已有**字段（非新增契约、非算账口径改动）后，前端才能复刻这两块。本单未动后端（54.3 In 范围=frontend/src）。

## 四、待拍板（书面指令冲突，不擅改）
- **费用月度趋势图型**：legacy=**堆叠面积图**；vue 现=**多系列发光折线**（明昊 54.1 V7 亲令从堆叠柱改折线）。「100% 复刻 legacy」与「明昊 V7 亲令」冲突——**未擅改**，请明昊裁定最终基准。

## 五、装库
- **无新增前端运行库**（B-01 与金线均用现有 ECharts + chart-fx + 现有 VM 字段实现）。
- 本机开发工具装 `Pillow`（拼图用，**不进 requirements.txt / 不进产品 bundle**，venv 已 gitignore）——非产品依赖。

## 六、结论
本单在 stage54p2 基础上，**完成阶段0 全量图纸 + 阶段2 点名必修 B-01（行为已与 legacy 等价、零版面跳动）+ 阶段1 回款卡金线**，全绿验证、证据用合成数据入库。**阶段1 其余逐卡、阶段2 其余交互、阶段3 减重尚未完成**，已在复刻清单与本报告如实标注，待续做与明昊人审（含 B06 费用图型裁定、回款摘要条后端填充）。
