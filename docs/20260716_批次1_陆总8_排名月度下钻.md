# 批次 1 · 陆总 #8 排名行 1~12 月下钻（2026-07-16）

## 需求
下单与回款板块（整体页 + BU 页）排名行点击后，展开/弹窗显示该主体 1~12 月各月下单、回款金额。与 `.rk-full`「其余」同一套 `rkModal` 交互。

## 实现

| 层 | 做法 |
|----|------|
| `profit.build_period` | 算 `rankings_monthly`：只挂排名出现的销售/客户（top+其余全量），年=期间起算年 |
| `profit.compute_name_month_totals` | 纯数矩阵 name×12 月 |
| `render.attach_monthly_to_dual` | 显示串 + 宽度% 挂到 dual items |
| `api_v1.rankings_view_for_period` | views 叶子带 `monthly[12]`；整体/BU 均 `embed_full=True` |
| `rankings.js` | 行上 `data-monthly=JSON`；`paintRankingMonthly` 只拼 DOM |
| `cockpit.js` / `cockpit-bu.js` | 点 `.rk-entity` → 弹窗 1~12 月；「其余」优先本地 `.rk-full` |

## 铁律
- 前端零金额运算（宽度/金额 Python 预算；JS 只 `toFixed` 拼 CSS / 拼已有显示串）
- BU 零新跨界 API；整体页也不加新 API
- 不倾倒全库：monthly 键 ⊆ 排名 full_items 名单

## 测试
`tests/test_ranking_monthly_a8.py`：结构、守恒、只挂排名主体、node≡python、点击字符串守卫、golden generate 抽查。

## 未做
- 不展开「签了哪些公司」
- 不改算账口径
