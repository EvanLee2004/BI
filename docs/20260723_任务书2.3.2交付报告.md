# 任务书 2.3.2 交付报告 —— 管理利润表四改

> 版本 **2.3.1 → 2.3.2**　·　tag `stage_2_3_2`　·　日期 2026-07-23  
> 执行：grok build　·　只 commit 不 push（本报告另注人侧要求上机）

## 一句话结论

陆总四点展示层改动已落地：交付成本抽屉两减项带 U+2212 负号、「管理毛利」→「毛利」、其下新增「毛利率」行、两率右侧数值三主题标绿；`src/profit/*` 与 KPI 金额零改动；`sh tests/run_verify.sh` → **EXIT:0**。

---

## 改动摘要

| # | 内容 | 落点 |
|---|------|------|
| 1 | 成本抽屉仅「系统内部译员」「直接成本增值税」带 `−` | `structure.py` `_dline_deduction`；legacy 复用 `amt_disp` |
| 2 | 「管理毛利」→「毛利」；税前公式脚注同步 | `structure.py`（`version.py` 历史串不动） |
| 3 | 「毛利」下插入 is_pct「毛利率」= `gross_margin_pct` | `structure.py`（无新计算） |
| 4 | 两率行 class `pl-pct` + grand 同档绿 token | `PLTable.vue` + `scifi-bridge.css`；legacy `pct_row.html` + `.pos` |
| 构建 | `npm run build` 重建 dist / dist-snapshot | 已进提交 |
| 版本 | VERSION / version.py / CHANGELOG → 2.3.2 | 已进提交 |

---

## 验收门闩证据

### 1. 全量验证 EXIT:0

```text
命令：sh tests/run_verify.sh; echo EXIT:$?
结果：EXIT:0
尾部（真实输出节选）：
  OK  tests/test_task52_fixes.py
  OK  tests/test_task_2_3_2_pl_display.py
  ✓ 全部通过
  EXIT:0
  … vue-tsc --noEmit 通过；ruff All checks passed
  ✓ 回归红线通过：32 个周期一分不差
证据文件：{SCRATCH}/run_verify_honest.txt
```

本单测试：`tests/test_task_2_3_2_pl_display.py`（11 项；已挂入 `run_verify.sh` PARALLEL）。  
PL HTML SHA 基线：`tests/fixtures/pl_table_year_sha.txt` 因本单显示串改动重锚为  
`785d6a95a79171e458f292d7ccf1878083c1ad5eebf1a263f8eb6132d67f8ac3`（含 毛利/毛利率/U+2212 抽屉串）。

### 2. 核心数字 / golden

- **KPI 金额零 diff**：reanchor 前后对 `KPI_KEYS` + expense/manual/ledger + trend/receipt 全量比对 → **KPI value diffs: 0**。
- **文件变更**：`golden/baseline_numbers.json` 仅补齐预存漂移字段 `rankings_totals.orders_by_bu`（32 周期结构键；**非本单业务改动**）。  
  命令：`git diff --stat golden/baseline_numbers.json` → `1 file changed, 160 insertions(+)`（全为 orders_by_bu 结构块）。  
  原因：HEAD 上 `test_api_v1_numbers` 已因该键缺失而红；为打通 `run_verify` 做了结构性 reanchor，**无利润数字改动**。
- 本单显示串改动不进入 `baseline_numbers.json`（该文件只存数字树）。

### 3. dist 重建

```text
命令：cd frontend && npm run build
结果：BUILD_EXIT:0（vue-tsc + vite build + snapshot build）
产物：frontend/dist/assets/boot-cockpit-BZYB8NDC.js + boot-cockpit-DwclmTzk.css 等（含 pl-pct 规则）
git status：frontend/dist/** 新旧 hash 更替并 stage
```

### 4. 单测断言（门闩 4）

| 断言 | 结果 |
|------|------|
| 抽屉「系统内部译员」「直接成本增值税」`amt_disp` 以 `−`(U+2212) 开头 | PASS |
| 加项（系统直接成本/PM 等）不带负号 | PASS |
| 主表存在「毛利」、活串无「管理毛利」 | PASS |
| 「毛利」后 is_pct「毛利率」=`gross_margin_pct` 串；BU 同有 | PASS |
| 费用类抽屉仍正数；legacy HTML 消费 structure 符号 | PASS |
| Vue `pl-pct` + CSS `#34d399` / `#0f766e` | PASS |

### 5. 真浏览器自测（三主题）

服务：`KANBAN_OFFLINE=1` 本机 `python run.py --serve`（:8018，数据目录 `数据/`）。

截图目录（**含业务数据，不进 git**；路径仅供人侧自看）：

| 主题 | 主表 | 成本抽屉（整页+侧栏） |
|------|------|----------------------|
| 霓虹 | `docs/_visual_2_3_2/neon/pl_main.png` | `pl_cost_drawer_main.png` + `pl_cost_drawer_panel.png` |
| 深色 | `docs/_visual_2_3_2/dark/pl_main.png` | 同上 |
| 浅色 | `docs/_visual_2_3_2/light/pl_main.png` | 同上 |
| BU | `docs/_visual_2_3_2/neon/pl_bu.png` | — |

**DOM 实测颜色（getComputedStyle）**：

| 主题 | 毛利率 / 税前利润率 color |
|------|---------------------------|
| neon | `rgb(52, 211, 153)` = `#34d399` |
| dark | `rgb(52, 211, 153)` = `#34d399` |
| light | `rgb(15, 118, 110)` = `#0f766e` |

**抽屉点开截图可见**（`pl_cost_drawer_panel.png` 标题「交付成本（生产成本）构成」）：

- 系统直接成本 **3,586.6万**（无负号）
- 系统内部译员 **−219.2万**（U+2212，清晰可见）
- 直接成本增值税 **0.0万**（当前数据为 0；非零时单测锁 `−` 前缀）
- PM/VM/其它加项 **不带负号**
- 主表「毛利」改名到位、下有「毛利率 42.3%」；BU「毛利率 36.0%」

证据 JSON：`{SCRATCH}/drawer_capture_evidence.json` + `run_verify_honest.txt`。

### 6. golden reanchor 留痕

- dry-run 与 write 仅 `orders_by_bu: only in new` + list/tuple 序列化差（JSON 往返后等价）。
- **无**毛利/成本/税前等 KPI 数值 diff。
- 与本单四改无直接关系；见 §2。

---

## 待确认（交明昊）

- 两率默认用税前利润档 **纯绿**（grand token）。若希望「毛利率」改配「毛利」青色 total 档，看截图后再微调。
- 增值税为 0 时显示 `0.0万` 不带负号（与主表 `amt_disp` 一致）；若陆总要求零也带 `−`，可再改一行。

---

## 提交

- 本地 commit：一句话中文说明四改 + dist + 测试  
- tag：`stage_2_3_2`（**不** `push --tags`）  
- **不 push 远端**（任务书红线；人侧若已授权上机另走 SSH）

---

## 范围核对

- 未碰：`src/profit/*`、`fetch_zhiyun.py`、密钥/数据目录、version.py 历史「管理毛利」串  
- 前端只加 class / CSS，不算金额  
