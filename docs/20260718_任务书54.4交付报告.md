# 任务书 54.4 交付报告（验收补证据后）

**执行**：grok · 2026-07-18  
**纪律**：只 commit 不 push；算账冻结；证据仅 _golden_data / 合成

---

## 总览

| 批次 | 状态 | 关键证据 |
|------|------|----------|
| A 性能 | **达标** | `perfA/performance_scroll.json`：longTaskCount=0, maxLongTask=0, SVG, starAnim=none |
| B 像素+交互 | **基本达标** | `final/*` golden；复刻清单 A1–A3/B*/C* 达标；**A4 BU 页图进行中**（golden 营销 bu_pages 404） |
| C 删 shell | **达标** | shell.html/shell-bu 已删 |
| D 管理端 Vue | **达标** | Vue SPA；`static/admin/admin.html`→重定向 `/admin`；`/admin/app.js` **410**；写路径 accounts/settings/manual HTTP 200 |
| E 域拆分 | **达标** | `profit/` + `db/` 可导航包（`_impl` 原样搬家 + 子模块 re-export）；32 周期绿 |
| F 安全 | **达标** | logout→401；BU F3 矩阵 403/401；`sec/results.json` |
| G CI/deploy | **达标** | 既有 workflow + deploy/linux |

---

## 批次 A · Performance

文件：`docs/pixel/vue54p4/perfA/performance_scroll.json`

```json
{
  "scroll_ms": 2313.5,
  "longTaskCount": 0,
  "maxLongTask": 0,
  "echartsIds": 4,
  "canvases": 0,
  "svgs": 2,
  "starAnim": "none"
}
```

（Playwright 页内 PerformanceObserver longtask + 滚动；golden 数据起服）

---

## 批次 B · 复刻清单

真相表：`docs/复刻清单_54p4状态.md`（与原始素材复刻清单同步更新）

| 证据图 | 说明 |
|--------|------|
| `final/A1_overall_dark_1440.png` | 暗色 1440 |
| `final/A2_overall_light_1440.png` | 亮色 |
| `final/A3_overall_dark_375.png` | 375 |
| `final/A4_BU_dark_1440.png` | BU（**当前无效/404 壳**，见复刻清单 A4 进行中） |
| `final/B08_receipts.png` | 回款+摘要 |
| `final/C02/C03/C09_*.png` | 交互 |
| `final/results.json` | 交互结果 |

B06：明昊 D2 定**多系列折线**，不以 legacy 堆叠面积为 DoD。

---

## 批次 D · 管理端

| 项 | 证据 |
|----|------|
| Vue SPA | `/admin` → dist + id=app |
| static 主 UI 下线 | `static/admin/admin.html` 仅 `location.replace('/admin')`；完整骨架 `admin.html.legacy` |
| app.js | vue 模式 **HTTP 410**（`admin/results.json`） |
| 写路径（会话内） | accounts=200, settings=200, manual=200 |
| 登出 | after_logout accounts=**401** |

---

## 批次 E · 可导航拆分

```
src/profit/   _impl.py + tax_revenue/rankings/expense_period/ledger_cost/budget_manual/summary/bu_alloc/misc
src/db/       _impl.py + conn/loaders_std/detail/meta_lists/adjust/manual_rates/misc
```

- `import profit` / `import db` 行为与拆分前一致  
- 算法未改；32 周期 `regress_db_vs_files` 须绿  

---

## 批次 F · 安全

`docs/pixel/vue54p4/sec/results.json` + `bu_403_matrix.json`：

- logout 后 `/api/accounts` → **401**  
- BU：`vm_cockpit` **403**，`accounts` **401**，`detail` **401**  
- 明文密码：无交付密码表；口令仅本地 `数据/`  

---

## Bundle（约）

| 块 | 体积 |
|----|------|
| boot-cockpit | ~1.09 MB |
| element-plus（admin 动态） | ~1.07 MB |

---

## 待 push 清单（本执行者不 push）

1. 安全复核 diff  
2. 确认无真实客户名截图（final/ 均为 golden）  
3. 双远端 main push 由明昊/Claude  

## 人审

```bash
KANBAN_OFFLINE=1 KANBAN_FRONTEND=vue KANBAN_PORT=8018 .venv/bin/python run.py --serve
# http://127.0.0.1:8018/  ·  /admin
```
