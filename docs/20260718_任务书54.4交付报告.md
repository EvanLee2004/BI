# 任务书 54.4 交付报告

**执行**：grok · 2026-07-18  
**范围**：到「生产部署之前」（B6 上机 / B7 封板 / push **不在**本单）  
**纪律**：只 commit 不 push；算账口径冻结；证据禁真实客户名金额进仓

---

## 总览

| 批次 | 状态 | 证据 |
|------|------|------|
| A 性能 | **达标** | commit `9e02642`；`docs/pixel/vue54p4/perfA/`（golden 合成数据） |
| B 像素+交互+回款 | **达标** | commit `60ce517` 含 B；`batchB/interactions.json` C02–C09 全 ok |
| C 删看端 legacy | **达标** | `static/shell.html` / `shell-bu.html` 已删；默认 Vue；测试已改 |
| D 管理端 Vue | **达标** | Element Plus SPA 双入口；`tests/test_admin_vue_54d.py`；static/admin 主 UI 走 Vue 路由 |
| E 域收敛 | **部分** | domain 门面已文档化+re-export 守卫绿；巨石物理拆文件未做全（routes 已可导航；profit/db 仍单文件） |
| F 安全 | **达标** | `docs/20260718_任务书54.4_安全留痕.md` + 既有 authz/白名单用例 |
| G CI/deploy 前置 | **达标** | `.github/workflows/verify.yml` 已有；`deploy/linux/nginx-kanban.conf` + `kanban.service` 已有 |

---

## 批次 A 证据

- 默认 `animation:false`；`breathScatterSeries` 恒 null；无 `effectScatter` 系列体  
- EchartsHost：`IntersectionObserver` 懒挂载 + `renderer:'svg'`  
- 星空静态；vue 面板 `backdrop-filter:none` 实色底  
- golden 截图 metrics：`canvases=0`、`starAnim=none`、顶屏 echartsIds≈2、底≈4、console 0  
- `run_verify` EXIT:0

## 批次 B 证据

- B4：`summary_by_period` 32 周期有值；`receipts_budget`/`budget_month` 已填；侧栏 DOM 有  
- 交互（真浏览器）：见 `docs/pixel/vue54p4/batchB/interactions.json`  
  - C02 period select / C03 PL drawer / C04 expense tabs / C05 rank more / C06 pw / C07 theme / C08 ledger next / C09 query  
  - console_errors=0  
- 进仓证据仅 json/log（截图曾用真实数据起服，**未进 git**）

## 批次 C 证据

```text
test ! -f static/shell.html && test ! -f static/shell-bu.html  → SHELL_GONE_OK
_main_shell / _bu_shell → 仅 _vue_index()
support 默认 KANBAN_FRONTEND=vue
```

## 批次 D 证据

- `frontend/src/admin/**` 视图：Console / Detail / Manual / Budget / Settings / Exception / History / Audit / Ledger / Login  
- MADR：`docs/madr/0013_admin_element_plus.md`（MIT Element Plus）  
- `/admin` 返回 Vue dist SPA；`POST /admin/login` 会话 cookie 不变  
- `static/admin` 文件保留作 app.js 对照，**主 UI 路径已是 Vue**

## 批次 E 诚实说明

| 项 | 状态 |
|----|------|
| domain 空壳 | 非空：re-export + config_engine + pl.structure；见 `src/domain/README.md` |
| profit/db 物理拆文件 | **未全做**（风险高；行为不变拆分可后续批） |
| server 可导航 | **已有** `routes/*` |
| 死链 shell | **已清** |
| 32 周期 | verify 必须 EXIT:0 后才收口 |

## 批次 F / G

- 安全留痕：`docs/20260718_任务书54.4_安全留痕.md`  
- CI：`.github/workflows/verify.yml`（run_verify + pre-commit；前端 typecheck 在 verify 脚本 1b）  
- deploy：`deploy/linux/nginx-kanban.conf`、`kanban.service`  

### Bundle 体积（约）

| 块 | 体积 |
|----|------|
| boot-cockpit | ~1.09 MB |
| element-plus | ~1.07 MB（仅 admin 动态加载） |
| bootstrap admin | ~30 KB |

### 待 push 清单（**你不 push**）

1. 安全复核 diff：无真实金额/客户名/内网 IP/账号密码  
2. 确认 `数据/` 未进包  
3. 双远端 main push 由明昊/Claude 执行  
4. B6 上机采用 `deploy/linux/*`  

---

## 未达标 / 待拍板

1. **E2 巨石物理拆分**：profit.py / db.py 未切成多文件；已可导航映射在 domain README。若要求「文件级拆」，需另开单。  
2. **A4 SVG**：当前默认 svg；若人审嫌文字糊可 sessionStorage `kanban_echarts_renderer=canvas`。  
3. **管理端真浏览器全写路径走查**：API/结构测试已绿；逐页点写操作建议人审补一轮。  
4. **static/admin 物理删除**：主路径已 Vue；物理删 admin.js 可人审后做。  

---

## 收官门闩（执行方自检）

- [x] A–D 本单主路径有证据  
- [x] 算账 32 周期护栏（verify 内）  
- [ ] 最终 `run_verify` EXIT:0（与本报告同批 commit 后确认）  
- [x] 不 push、不上机  
- [x] 无真实数据截图进仓  

---

## 人审起服

```bash
cd 程序/看板正式程序
KANBAN_OFFLINE=1 KANBAN_FRONTEND=vue KANBAN_PORT=8018 .venv/bin/python run.py --serve
# 看端 http://127.0.0.1:8018/   管理端 /admin
```
