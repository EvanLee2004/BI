# 任务书 54.13：工程完美收官——高内聚低耦合·复杂度·重复·死码·覆盖率 全面达标（跑完 54.12 后执行）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；收口 tag `stage55_rc4`。
> 你是执行机器兼架构师。本单把代码质量推到**可量化的完美**——明昊已拍板不省额度,标准一步到位。
> **总铁律（违反=整单作废）**:全程**只搬家、不改算法**——任何页面数字、任何 API 响应、任何 golden、32 周期红线**零 diff**;每个阶段收口跑全量 `run_verify` EXIT:0。有 stage54 domain 拆分"只搬家"成功先例,照那个纪律做。
> **前置**:stage55_rc3 存在且 54.12 交付报告结论为"二轮修复完成"。不满足则停下只写说明。

## §量化完美标准（收口时逐项出数字,一项不达不许打 tag）

| # | 指标 | 标准 | 测量方式 |
|---|------|------|----------|
| M1 | 后端单文件行数 | ≤800 行 | `wc -l src/**/*.py` |
| M2 | 前端单组件行数 | ≤400 行 | `wc -l frontend/src/**/*.vue` |
| M3 | 圈复杂度 | 每函数 ≤10 | ruff 开 `C901`(mccabe),超标即错 |
| M4 | 循环依赖 | 0 | import 图检测(脚本遍历 AST 或 pydeps) |
| M5 | 分层单向 | routes→domain→ingest/db 单向;domain 禁止 import routes/server | grep+import 图证明 |
| M6 | 重复代码 | 重复块<3% | `pylint --disable=all --enable=duplicate-code src/`(阈值默认)+前端肉眼抽查同类组件 |
| M7 | 死码 | vulture 报告逐条定性:真死→删;误报→白名单注明理由 | `vulture src/ --min-confidence 80` |
| M8 | 测试覆盖率 | `src/domain/` 行覆盖 ≥80%;缺口只许补**有断言价值**的测试,禁止凑数 | `coverage run/report` |
| M9 | ruff 全绿(54.12 已达)保持 | 0 错 | run_verify 内已含 |
| M10 | 核心模块 docstring | domain 每个子包+每个公开函数一句话说清职责 | 抽查 |

## 执行方式（按 agent 最佳实践:先计划后动手,小步可验证）

**阶段 0·蓝图先行(不写代码)**:产出 `docs/20260719_54p13_拆分蓝图.md`——render.py(1271 行)/server.py(1048 行)/SettingsView.vue(885 行)各自的拆分方案(拆成哪些文件、每个文件职责一句话、谁 import 谁),外加 M4/M5 现状依赖图。**蓝图里每个任务带勾选框和"完成判据"**,后续逐个勾。

**阶段 1·后端拆分**(一个目标文件=一批,每批:搬家→全量 verify EXIT:0→单独 commit):
- render.py → 按职责拆(导出渲染/历史快照/共用模板工具等),入口保持兼容(旧 import 路径给过渡 re-export 或全量改引用);
- server.py → 应用装配/中间件/生命周期/静态服务等分文件;
- 拆完 M1 达标、M4/M5 用依赖图证明。

**阶段 2·前端拆分**:SettingsView.vue 按功能卡片拆子组件(自动更新/备份/告警/智云配置/账号权限各一),AdminLayout/PeriodPicker 超 400 行的同规则处理;拆完 M2 达标,vue-tsc 绿,逐页截图与拆前像素级一致。

**阶段 3·复杂度与重复**:开 C901 跑出超标函数清单→逐个重构(提取子函数/早返回/查表替代 if 链);M6 重复块合并成共用函数/组件;每改一处跑定向测试+全量 verify。

**阶段 4·死码与覆盖率**:M7 vulture 逐条定性(交付报告列表:项|定性|依据);M8 补 domain 覆盖缺口(先出缺口清单,优先覆盖钱相关分支)。

**阶段 5·收官**:§量化标准 M1~M10 全表实测数字进交付报告;全量 run_verify+perf_check+console 零 error+抽 3 屏截图与 rc3 像素一致;架构图/概要设计/README/CHANGELOG 全部对齐拆分后现状(架构图 SVG 重画模块框);版本 `2.0.0-rc4` 三处一致;tag `stage55_rc4`。
交付报告 `docs/20260719_任务书54.13交付报告.md` 第 1 行:`结论：工程完美收官完成,待明昊终审`。

**阶段 6·全团队收尾（工程之外的角色活,一次做完）**:
1. **需求追溯矩阵**(需求工程师活):`软件工程文档/1_需求/` 新增 `需求追溯矩阵.md`——原始需求逐条(含 6-26 需求分析与历次陆总口径)→ 实现位置(模块/页面)→ 验证方式(测试文件/走查项)→ 状态(已实现/二期/已废止)。54 系列所有口径决策(工资全隐/时间选择器两段式/公共费用不分摊等)补进需求台账,如实记录别改写原话。
2. **二期规划草案**(产品经理活):`方案与文档/项目总览/` 新增 `20260719_二期规划草案_待陆总讨论.md`——半页:本期未含项(专项追踪投入产出比/现金流预判)各自的价值一句话、预估投入档位(小/中/大)、建议优先级;结论式、可直接转发,同事写岗位不写姓名。
3. **演示动线**(给明昊向陆总首秀用):`docs/上线交接包/` 新增 `演示动线_5分钟.md`——按"她最关心什么"排的点击顺序(总利润→BU 利润表→下钻构成→回款→手机扫码),每步一句讲词;含"她可能问的 5 个问题+答案"(数字口径类)。
4. **全部软工图终核**:拆分后 `2_设计/` 每张图(架构/模块组件/时序/ER/部署拓扑/登录分流)再过一遍与终态一致;schema 全表文档与实际库结构逐表核对。
5. **CI 终检**:`.github/workflows/` yaml 与拆分后路径/命令一致,本地 `act` 不可用则静态核对步骤,写明"push 后首跑需确认"进部署执行单。

## §grokbuild 执行效率条款（照做,能显著少走弯路）

- 开工先建 `docs/20260719_54p13_任务勾选.md`:所有阶段任务列成带勾选框清单(蓝图阶段产出),**每完成一项立即打勾并写一行证据路径**——这是你的外置记忆,上下文再长也不迷路。
- **每阶段收口自派一个 skeptic 子代理**只读复核该阶段(拆分零行为变化/指标真达标),复核意见进交付报告;子代理不许写代码。
- 长输出(依赖图/vulture 清单/覆盖率报告)落盘引用路径,不要贴进对话撑爆上下文。

## 防跑偏条款

1. **禁止顺手改行为**:拆分中发现"这逻辑好像不对"→记入「发现清单」上报,不许改(口径区=禁区,54.12 R-11 延续)。
2. **每批一 commit、批间全量验证**——绝不攒一堆一起交;commit 信息写清"搬了什么、零行为变化"。
3. 重构类改动**禁止引入新依赖**;检测工具(vulture/pylint/coverage)装进 venv 可以,不进 requirements 运行依赖(开发依赖单列 requirements-dev.txt)。
4. 上下文若不够跑完全部阶段:在阶段边界收口(该阶段 verify 全绿+commit+交付报告写明"完成到阶段 N"),**禁止跨阶段烂尾**。

## 非目标
新功能、视觉改动、push、数据库更换、口径任何变化。
