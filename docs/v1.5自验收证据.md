# v1.5.0-beta 自验收证据（2026-07-15）

基线：`393c04b`。实现提交：`c69fe07`（feat extract）+ 后续 docs。

## A 红线

| 项 | 结果 |
|----|------|
| A1 计算层 diff（profit/core/db/bu/periods/columns/ingest） | **空**（`layer_diff_summary.txt`） |
| A2 无 package.json / node_modules / jsx/tsx/vue | **无** |
| A3 requirements.txt / *.bat vs 基线 | **空** |
| A4 提交无真实金额/客户/密码 | golden 用 `_golden_data` 假数据；static 无业务数 |

## B 数字

| 项 | 结果 |
|----|------|
| 与 393c04b 同套 `_golden_data` 建库 summary 关键 KPI 32 周期 | **equal true**（`summary_equal.log`） |
| golden 骨架对照 test_admin_static | **OK** |
| `KANBAN_OFFLINE=1 sh tests/run_verify.sh` | **EXIT 0**（`run_verify.log`） |
| 逐文件 `tests/test_*.py` | **FAIL_COUNT=0**（`per_file_tests.log`） |

## C 活体 / 结构（TestClient + 静态）

- 未登录 `/admin` → 登录页  
- 静态：`/static/admin/admin.css` 200；`/admin/app.js` 含 showGroup 且无占位符  
- 生产路径（`_use_admin_static=True`）：登录后 HTML 含 `admin.css` + `/admin/app.js`，无 `<style>`  
- LEGACY 路径：含 `<style>`，无 admin.css 外链  
- 空 `admin_html` → 引导页（首次取数）  
- admin.html 无重复 id  

（本机 GUI 浏览器未强制；结构与 HTTP 路径已用 TestClient 覆盖；部署机 Ctrl+F5 后肉眼点页签。）

## D 更新链路

- static/ 已进 git（未 ignore）  
- updater / bat 未改  
- 部署：`git pull` → 看门狗重启 → 管理端 **Ctrl+F5**  

## E 回滚

```bat
git checkout 393c04b
:: 再双击 看门狗启动.bat
```

或：`set KANBAN_LEGACY_INLINE=1` 后重启（仅内嵌管理端 HTML）。
