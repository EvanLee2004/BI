# 人审指南 · 明昊 10 分钟清单（终局 2.0.0-rc9）

1. **登录看端** — 页签标题「甲骨易·经营罗盘」+ favicon 非灰球。
2. **总利润 KPI / 利润表** — 数字与印象一致；BU 切换正常。
3. **期间费用** — 四 tab 不跳版；环形无「万万」。
4. **费用折线 + 热力** — 图例无「成本」大类；悬停 tooltip 不被裁。
5. **费用明细** — 表头有「含成本/非利润表」说明小字；导出无工资列。
6. **管理端** — 人工填写 tab 可进；数据修正空态有引导；智云表 ID 走抽屉。
7. **设置** — 无「可见工资」开关；保存失败若断网见「服务暂时不可达」。
8. **错误页** — 浏览器开 `/不存在` 见友好 404，非裸 JSON。
9. **375 手机** — 顶栏与周期可点。
10. **签字** — 通过后告知 Claude push+部署。

## 热力图去留建议
修缮后建议**保留**（扫月份×大类异常有用）；若觉得过载再改可折叠。


## 附：B-5 前端错误日志（任务书57）

- 看端/管理端 JS 未捕获错误会 POST `/api/v1/client-error`，落 `数据/前端错误.log`（不进 git）
- `deploy/healthcheck.sh` 近 24h 有错误会打 **YELLOW** 行（不红灯）
- 统计接口仅管理员：`GET /api/v1/client-error/stats`
