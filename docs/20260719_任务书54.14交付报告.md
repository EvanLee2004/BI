结论：三轮修复完成,请明昊复审

# 任务书 54.14 交付报告 · v2.0.0-rc5 · stage55_rc5

- **日期**：2026-07-19  
- **tag**：`stage55_rc5`（**只 commit 不 push**）  
- **前置**：stage55_rc2（54.11）；54.12/54.13 任务书在仓但未形成 rc 封板  
- **环境**：`KANBAN_OFFLINE=1` · 本机 :8018 · 活体账号生产表 `123/123`（整体）  

证据根：`docs/验收证据/20260719_54p14/`  

---

## R-20 「万万」根因与漏 BU 检讨

**根因**：`ExpenseSection.vue` 环形中心把已含「万」的 VM 字段 `total_disp` 再拼一次 `` `${c.total_disp}万` ``。  
后端 `_donut_center_by_period` 约定 `total_disp = fmt_wan(tot)+"万"`。

**为何「修整体漏 BU」**：整体页与 BU 页**共用同一 SFC** `ExpenseSection`，并无第二条拼接路径。54.12 任务书点名 R-10 但仓库**无对应 fix commit**——代码从未真正改掉，人审若只看到某一周期中心为空/未滚到图，会误判「整体已好」。本单按「组件×页×主题×375」清单一次清完，并加 `withWanUnit` 幂等与自动化守卫。

详单：`验收证据/20260719_54p14/R20_万万位置清单与根因.md`

---

## R 清单收口

| ID | 摘要 | 关键改动 | 证据 |
|----|------|----------|------|
| R-20 | 万万清剿 | `ExpenseSection` + `utils/disp.ts` + 测试 | unit + live body 无万万 |
| R-21 | 主题零刷新 | `utils/theme.ts` · ThemeToggle · Admin · ECharts option 依赖 themeMode | live theme_after_toggle.png |
| R-22 | 周期弹层可点 | topbar z-index token；pp overlay | live period_*.png · panelA=0.97 z=60 |
| R-23 | 图裁切/比例 | containLabel + 容器高度 | 源码 + 结构测试 |
| R-24 | 比率线不冲顶 | `ratioAxisBounds` | unit live（负/0/超100） |
| R-25 | 浅色黄条 | `--banner-warn-*` 双套 | theme.css + admin.css |
| R-26 | 费用热力图 | `ExpenseHeatmap.vue` 挂整体+BU | live heatmap.png |
| R-00 | 零新增 P0/P1 | 切换态纳入走查 | 结构守卫全绿 |

---

## 全量验证

| 项 | 结果 |
|----|------|
| `vue-tsc --noEmit` | EXIT:0 |
| `tests.test_task54p14_r20_no_double_wan` + `r21_r26` | 16 OK |
| 活体 Playwright | OK（`live/live_report.json`） |
| 性能（生产账号） | 看端 idle 1462ms；管理端 464ms；orderdept 1089ms；热力图节点=1 | 见 `perf_check.json` |
| 口径/domain | **零改动** |
| 新增组件行数 | ExpenseHeatmap 164 ≤400 |

全量 `run_verify.sh` 在本报告提交后跑；以最终日志为准。

---

## 版本三处

| 位置 | 值 |
|------|-----|
| `VERSION` | `2.0.0-rc5` |
| `frontend/package.json` | `2.0.0-rc5` |
| `/api/version` / 管理端 pill | 读 VERSION → 发布候选 |

---

## 文档联动

- 用户手册：费用热力怎么看 + 周期/主题说明  
- CHANGELOG_stage54 系列补记 rc5 节  
- 上线交接包封板报告追加 rc5  
- 本交付报告  

---

## 非目标（诚实）

- 热力图点击联动筛选明细表：未做（任务书允许列待拍板）  
- 未 push 远端  
