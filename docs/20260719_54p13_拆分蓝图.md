# 54.13 拆分蓝图（只搬家、不改算法）

## 目标行数
- M1 后端单文件 ≤800
- M2 前端单组件 ≤400

## render.py (1271) → 拆分

| 新文件 | 职责 | 迁入内容 |
|--------|------|----------|
| `src/render_charts.py` | 趋势/费用环形/堆叠图 HTML | 板块②-1 趋势、费用环形、expense trend pack |
| `src/render_pl.py` | 管理利润表 HTML | `_pl_structure_to_html`、PL 相关 |
| `src/render_receipts.py` | 回款卡 | `render_receipts`、insight totals |
| `src/render_rankings.py` | 双血条排名 | dual merge/attach、`render_rankings`、daily dual |
| `src/render_assemble.py` | 整页/BU 碎片组装 | `build_dashboard_fragments`、`build_bu_dashboard_fragments` |
| `src/render.py` | 兼容 re-export 入口 + 薄包装 | 对外 import 路径不变 |

完成判据：`from render import *` 行为同前；`wc -l` 各文件 ≤800；run_verify EXIT:0。

## server.py (1114) → 拆分

| 新文件 | 职责 |
|--------|------|
| `src/settings_io.py` | `save_settings` / zhiyun 读存 / schedule 相关纯函数 |
| `src/audit_diff.py` | `_audit` / `_diff_*` 配置变更留痕 |
| `src/app_factory.py` | `create_app` 主体（中间件/会话/favicon/错误页/注册路由） |
| `src/server.py` | 兼容 re-export + `serve()` 入口 |

完成判据：`import server; server.create_app` 可用；≤800；verify 绿。

## SettingsView.vue (887) → 拆分

| 组件 | 职责 |
|------|------|
| `SettingsVersionCard.vue` | 版本与更新 |
| `SettingsScheduleCard.vue` | 自动更新时间 |
| `SettingsBackupCard.vue` | 备份保留 |
| `SettingsAlertCard.vue` | 飞书告警 |
| `SettingsZhiyunCard.vue` | 智云账号+抽屉 |
| `SettingsAccountsCard.vue` | 账号权限 |
| `SettingsBuCard.vue` | BU 归属 |
| `SettingsSourcesCard.vue` | 数据来源 |
| `SettingsView.vue` | 编排壳 <400 行 |

完成判据：M2≤400；vue-tsc 绿；设置页功能同前。

## 其它超标
- AdminLayout 436、PeriodPicker 417 → 阶段 2 内压到 ≤400（抽 composable/子组件）。
