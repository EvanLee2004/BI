# 任务书36 交付报告 · 上线终检（gzip + Windows 部署链路 + 从零手册 + 2027 + 分离证明）

> **分支**：`chore/deploy-audit-2027`（worktree `看板worktree36/`）  
> **纪律**：只本地 commit、不合并不 push；由 Claude 验收后合 main。  
> **日期**：2026-07-16  

---

## 摘要

| 批次 | 结果 |
|------|------|
| A gzip | `GZipMiddleware(minimum_size=1000)`；`tests/test_gzip_fragments.py` 锁 `content-encoding: gzip` + PNG 魔数 |
| B Windows 审计 | 5 个 `.bat` 逐文件表；修 `更新看板` venv、`开启内网访问` 防火墙、`注册每日更新`/`_schtask_command` 显式 cd |
| C 从零手册 | `docs/Windows部署手册.md` 重写为 **D:\\** checklist |
| D 非法日 | `parse_date_parts`/`_valid_ymd` 用 `datetime.date`；2/30→None + 体检黄路径 |
| E 2027 | 缺 sheet 清晰 KeyError；`zhiyun_since=auto`；合成 2027 小基线；**不动** 2026 golden |
| F 分离 | `test_split_static` 相对 API + 异端口 static；MADR 写入 `docs/softeng/09_部署架构说明.md` |

---

## A · gzip 证据

- 实现：`src/server.py` `create_app` → `app.add_middleware(GZipMiddleware, minimum_size=1000)`（Starlette 内置，无新 pip）。
- 测试：`tests/test_gzip_fragments.py` 驱动真实 `create_app` + TestClient。
- 桩 fragments 实测（TestClient 自动解压 body，`content-length` 为 wire 压缩长）：

```text
[gzip-evidence] plain=2918 content-length(gzip)=265 ratio=9.08%
```

- 真实大数据体积随业务变；上线后可在浏览器 Network 对 `/api/v1/cockpit/fragments` 再录一帧（期望 encoding=gzip，约 10%～20% 体量级）。
- PNG：`/export.png` 在 Accept-Encoding:gzip 下 body 仍以 `\x89PNG` 开头、`content-type=image/png`。

---

## B · Windows 部署链路审计表

> 开发环境 macOS：schtasks/防火墙/看门狗热重启**未在 Windows 实跑**；以静态审计 + 脚本正确性为准，部署机按手册后验。

| 文件 | 编码 | 路径 / cwd | 权限 | 结论 |
|------|------|------------|------|------|
| `看门狗启动.bat` | UTF-8 + `chcp 65001` | `cd /d "%~dp0"`；PY=`%~dp0.venv\Scripts\python.exe` | 普通用户可 | **无需改**（回滚/42 逻辑完整） |
| `启动看板服务.bat` | UTF-8 + `chcp 65001` | `cd /d "%~dp0"`；venv 优先 | 普通 | **无需改** |
| `更新看板.bat` | UTF-8 + `chcp 65001` | `cd /d "%~dp0"` | 普通 | **已修**：原固定 `python`，现优先 `.venv\Scripts\python.exe` |
| `注册每日更新.bat` | UTF-8 + `chcp 65001` | `cd /d "%~dp0"`（仅注册脚本自身）；`/TR` 绝对路径 `"%PY%" "%~dp0run.py" --scheduled`（**禁止** `cd /d "%~dp0"` 进 TR：尾 `\` 转义引号） | **管理员** | **已修**：venv PY + 绝对路径 TR；与 `server._schtask_command` 一致；`findstr` 仅英文任务名 |
| `开启内网访问.bat` | UTF-8 + `chcp 65001` | `cd /d "%~dp0"` | **管理员** | **已重写**：旧版 `http.server` 共享 output（无鉴权、过时）→ `netsh advfirewall` 放行 8018；`findstr /i IPv4` 仅英文 |
| `server._schtask_command` | — | 与 bat 对齐显式 cd | 管理端保存同步 | **已修** 与 bat 同语义 |
| Python `os.replace` / Path | — | 进料口/备份用 pathlib，根目录 `__file__` | — | **无需改**：无 POSIX-only 硬编码；Windows 文件占用 PermissionError 属 OS 限制，备份路径已滚动命名 |
| WAL/busy_timeout | — | 本地盘 | — | **无需改**（任务书33 已落地） |

**数据从零清单**：见手册 §4；空库引导页路径（F-02）回归点在手册 §5。

---

## C · 从零部署手册

- 路径：`docs/Windows部署手册.md`
- 结构：装 Python → clone（`D:\看板\看板正式程序`）→ venv+清华镜像 → 数据清单 → 看门狗 → 防火墙 → 注册计划任务 → 三账号 → 回滚 → 安全 checklist（改初始密码、智云全量只读号）。
- 所有示例路径 **`D:\…`**。

---

## D · 非法日历日（行为变化 · CHANGELOG）

**变更前**：`parse_date_parts("2024-02-30")` → `(2024, 2, 30)`（仅 1–12 / 1–31 松校验）。  
**变更后**：经 `datetime.date` 校验 → `None`；行不计入任何月；体检黄「有 N 行日期解析不出…」（既有 `_scan_*_issues` 路径）。  
**兼容**：合法闰日 `2024-02-29` 仍有效；`2027-01-01` 有效。  
**测试**：`test_hygiene_b.TestTimeBoundaries.test_illegal_calendar_day_is_none`、`test_year2027.TestIllegalDateHealthPath`。  
**红线**：合法源数据数字不变；仅脏日期从「误入」改为「剔除+报警」。

---

## E · 2027 跨年

| 项 | 行为 |
|----|------|
| 台账缺 `2027` sheet | `KeyError` 文案含「找不到『2027』sheet」「新一年的 sheet 还没建」——`test_year2027` |
| `zhiyun_since` | 出厂 **`auto`** = 当年元旦；写死 `YYYY-MM-DD` 兼容；空串=全量。`resolve_zhiyun_since` + filter 前一天逻辑不变 |
| 周期 | `today=2027-01-15` → 键含 `2027年` / `2027年1月`；合成下单/交付进汇总 |
| golden | **未改** 2026 基线 |

---

## F · 分离能力

- 静态检查：`static/**/*.{js,html}` 无 `http://host:port` 类 API 源（用户提示「服务器:端口」文案除外）。
- `tests/test_split_static.py`：另一端口 `ThreadingHTTPServer(directory=static)` GET shell + page.js 200；shell 内 fragments 仍为相对 `/api/...`。
- **边界**：不测跨域登录；真跨域需 HTTPS+SameSite=None（MADR 已记）。
- 文档：`docs/softeng/09_部署架构说明.md` §部署形态 MADR。

---

## 测试与验证

新增/扩展：

- `tests/test_gzip_fragments.py`
- `tests/test_year2027.py`
- `tests/test_split_static.py`
- `tests/test_hygiene_b.py` / `tests/test_fetch_zhiyun.py` 增量

全量：`KANBAN_OFFLINE=1 sh tests/run_verify.sh` → **EXIT:0**（见验收证据目录）。

---

## 配置

- `config.json`：`zhiyun_since` 默认 `"auto"`（出厂；不脏部署机本地覆盖语义）。

---

## 非目标确认

- 未 push / 未合 main  
- 无新 pip 运行时依赖  
- 未改利润口径与前端视觉布局  
- Windows 实机 schtasks/防火墙后验留给部署日  
