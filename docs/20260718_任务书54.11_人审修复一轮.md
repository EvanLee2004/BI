# 任务书 54.11：明昊人审修复单·第一轮（rc1 人审发现,修完复验重封板）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；收口 tag `stage55_rc2`。
> 你是执行机器:只修本单 R 清单,禁止顺手改别的;每条修复附「复现→修复→复验证据」;全部修完重跑封板复验关键项。
> 明昊在 8018 真实人审发现以下问题——这些都是此前批次自查漏掉的,修复标准以"明昊视角"为准,不以"技术上说得通"为准。

## R 清单（按严重度排序）

### R-01 BU 页入口全部消失（最高优先级·先定性再动手）
- 现象:看端页面上找不到任何进入各 BU 页的入口。
- 已知线索(Claude 初诊):`frontend/src/components/BuNav.vue` 依赖 `store.buNames`,空则整条隐藏;本地 `config.json` 无 BU 页配置。
- **你的任务——先定性,二选一**:
  a) 数据链回归:对照 `git log stage54p3..HEAD` 里动过 buNames 来源链(库配置层→VM→store)的 commit,若链断了→修复,并补一条"buNames 配置存在时导航必渲染"的测试;
  b) 环境无配置(链没断):在本地合成数据环境把 BU 页配置配起来(管理端 BU 数据归属,用合成销售名),让明昊人审时**能看到并进入 BU 页**;同时 BuNav 在"有 BU 配置但当前名单为空"时不应完全无声——给管理员一句可见提示(如体检面板加一条)。
- 收口标准:8018 上明昊能看到 BU 入口并点进 BU 页;定性结论(回归/环境)+证据写进交付报告。

### R-02 全局时间选择器重设计（体验灾难级）
- 现象:下拉是 40+ 项平铺列表(年/季/单月/所有跨月区间组合混排),找一个周期要滚很久,超出视口。
- 修复方案(按此做,不再自由发挥):改为**两段式选择**——第一段:粒度 tab(年 / 季 / 月 / 自定义区间);第二段:按粒度出选项(季→Q1~Q4;月→12 个月网格;自定义→起止月两个下拉)。默认仍=全年;VM 周期枚举与后端零改动,纯前端把平铺列表映射成分组交互。
- 收口标准:任何周期 3 次点击内可达;下拉不超视口;深浅色/375 都可用;周期切换后数字与旧版选同周期完全一致(抽 3 个周期核对数字)。

### R-03 弹层/抽屉透明度过高(看不清)
- 现象:"查看构成"抽屉等弹层背景太透,底层卡片文字数字透上来与内容叠字。
- 修复:全站弹层(抽屉/弹窗/下拉/tooltip)统一到主题 token:内容面板背景不透明度 ≥0.95(近实色深空底),弹层下必须有半透明遮罩(rgba 黑 0.4~0.5)隔离底层;**全局排查所有弹层组件一次修完**(前科条款:修一处漏一处零容忍)。
- 收口标准:每类弹层打开状态截图,叠字现象清零;文字对比度仍达 4.5:1。

### R-04 复查明昊未点名但同源的问题(观察项,逐条核,不是必修)
- 顶栏体检黄条文字被截断("local_fallback…")——悬停可见全文或点开可读。
- 选择器/下拉在管理端 iframe 场景下的定位与裁剪。
- 其他弹层同款透明问题(按 R-03 全局排查覆盖)。

## 收口流程

1. R-01~R-03 修复+复验(每条单独 commit `fix(54p11): R-xx 一句话`);R-04 逐条核并写结论。
2. 重跑封板关键项:`sh tests/run_verify.sh; echo EXIT:$?` EXIT:0;perf_check 达标;console 零 error;受影响页截图更新 `docs/pixel/` 与用户手册。
3. 更新 `docs/上线交接包/`:封板复验报告追加"rc2 修复轮"节(R 清单+证据);人审指南补充 BU 页与新时间选择器的"重点看什么"。
4. 版本号升 `2.0.0-rc2`(三处一致);tag `stage55_rc2`;只 commit 不 push。
5. 交付报告第 1 行:`结论：人审修复轮完成,请明昊复审` 。

## 非目标
新功能、其他视觉再优化(明昊没点名的不动)、push。
