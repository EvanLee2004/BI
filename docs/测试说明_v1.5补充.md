# 测试说明补充 · v1.5.0-beta（程序仓副本）

> 全量历史说明仍在项目本地 `方案与文档/软件工程文档/3_测试/05_测试说明_回归红线与验收.md`（已同步本数字）。本文件保证**公开代码仓**也能看到 v1.5 用例登记。

## 当前规模（2026-07-15）

| 项 | 值 |
|----|-----|
| 产品版本 | **1.5.0-beta** |
| run_verify 登记 unittest 文件 | **26** |
| `def test_` 合计 | **426** |
| 回归红线 | 32 周期 库算==文件算 |
| 判绿 | 只认真实退出码（铁律20，禁 `\| tail`） |

## v1.5 新增

| 文件 | 例数 | 覆盖 |
|------|------|------|
| `tests/test_admin_static.py` | 11 | static/admin 在位；JS 标记；无重复 id；禁 toFixed/parseFloat；css/js 与 `_ADMIN_CONSOLE` 逐字节同文；未登录 /admin；静态壳；`/admin/app.js` 注入；unittest 内嵌路径；golden 骨架对照 |

已登记 `tests/run_verify.sh` 末尾：`$PY tests/test_admin_static.py`。

## 跑法

```bash
KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo $?
for f in tests/test_*.py; do .venv/bin/python "$f" || echo FAIL $f; done
```
