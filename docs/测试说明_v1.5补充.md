# 测试说明补充 · v1.5.0-beta（程序仓副本）

> 全量历史说明仍在项目本地 `方案与文档/软件工程文档/3_测试/05_测试说明_回归红线与验收.md`（已同步本数字）。本文件保证**公开代码仓**也能看到 v1.5 用例登记。

## 当前规模（2026-07-16 批次0 后）

| 项 | 值 |
|----|-----|
| 产品版本 | **1.5.0-beta** |
| run_verify 登记 unittest 文件 | **29**（+`test_ranking_monthly_a8`） |
| `def test_` 合计 | **425**（+排名月度 5 例） |
| 回归红线 | 32 周期 库算==文件算 |
| 判绿 | 只认真实退出码（铁律20，禁 `\| tail`） |
| HTTP 测试入口 | `tests/run_test.py`（先 `support`：`SERVE_SHELL=False`） |

## v1.5 新增 / 收口

| 文件 | 例数 | 覆盖 |
|------|------|------|
| `tests/test_ranking_monthly_a8.py` | 8 | 陆总#8/任务书34：mkey+页面级字典/守恒/无行级大 JSON/node≡python/paint 查表/多周期共享/golden 抽查 |
| `tests/test_login_cache_p0.py` | 3 | 会话态文档路由 `Cache-Control: no-store`；未登录→登录→同 URL body 翻转；shell 401→/login 守卫 |
| `tests/test_admin_static.py` | 11 | static/admin 在位；外链；无重复 id；禁 toFixed/parseFloat；未登录 /admin；`/admin/app.js` 注入；golden 骨架 |
| `tests/test_serve_shell.py` | 2 | `SERVE_SHELL=True` 返回 shell；False 直出 HTML |
| `tests/test_profile.py` | 2 | 守卫：无 executive / data-profile；管理端无精简开关 |

已登记 `tests/run_verify.sh`：经 `run_test.py` 跑全部 `test_*.py`（含 admin_static、serve_shell）。

## 跑法

```bash
KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo $?
for f in tests/test_*.py; do .venv/bin/python tests/run_test.py "$f" || echo FAIL $f; done
```

## 业绩目标矩阵（2026-07-15）

| 文件 | 变化 |
|------|------|
| `tests/test_budget.py` | +矩阵 UI 守卫；+budget_batch 全公司/BU 写读互不串 |
| `tests/test_admin_static.py` | +toolbar sticky CSS 守卫 |

## 期间费用构成「按部门」（2026-07-15）

| 文件 | 变化 |
|------|------|
| `tests/test_expense_views.py` | 四态守卫；按部门守恒含未分类；BU 页仍无按部门 |
| `src/render.py` | `render_expense_views` 第四态 `dept`，数据 `expense_by_department` |

## C 阶段验证并行（2026-07-15）

- `sh tests/run_verify.sh`：写库/服务态用例串行；轻量用例默认 4 并行。
- `KANBAN_VERIFY_JOBS=1` 全串行；B 组装测试见 `test_b_*`。
