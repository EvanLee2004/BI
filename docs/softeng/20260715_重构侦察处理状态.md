# 重构侦察报告处理状态（阶段 C · 2026-07-15）

对照：`方案与文档/软件工程文档/2_设计/20260715_重构侦察报告_后端结构债.md`

| 条目 | 状态 | 说明 |
|------|------|------|
| `app_state.py` 抽出 `_state`/`_LOCK`/`_EXPORT_LOCK`/cookie 常量 | **已处理** | `src/app_state.py`；server 从中 import 同一 dict |
| `auth_session.py` 抽出 HMAC token | **已处理** | `src/auth_session.py`；server 别名兼容 |
| `refresh_pipeline.py` 抽出 publish/full/recompute | **已处理** | `src/refresh_pipeline.py`；`start_refresh_async` 留 server 以便测打桩 `_do_full` |
| `export_png.py` 截图内核 | **已处理** | `src/export_png.py`；`_screenshot_png` 别名 |
| `server.py` 目标 <800 行 | **部分** | 现约 1780 行（原 ~1918）；路由簇仍在 `create_app`，下批再拆 `routes_*` |
| `routes_*` APIRouter 拆分 | **明确不做（本批）** | 鉴权闭包耦合重；无上线压力，记台账下批 |
| `settings_ops.py` 调度/智云凭据 | **明确不做（本批）** | 同上，行为零变优先 |
| 测试并行提速 | **已处理** | `run_verify.sh`：写库/服务态串行 + 轻量用例并行（`KANBAN_VERIFY_JOBS` 默认 4） |
| F-02 类直读 config 残余 | **已处理（前批）** | 注册 bat 已读合并配置 |
| 杂项 TODO/FIXME 台账 | **明确不做（本批）** | 无阻塞试运行项 |

完成标准说明：侦察报告核心「state/auth/refresh/export」已落地；`server.py` 仍厚因路由未拆，**不阻塞** B 分离交付与试运行。
