# 12 · 风险与技术债台账

> 产品以根 **`VERSION`（当前 3.7.7）** 为准 · 诚实列「现在能接受 + 何时必须升级」。**不是 bug 清单**。  
> **3.3.1 已清偿/降级**（历史）：DEBT-1 分摊展示 float 旁路 → int 分；orphan 测书面处置 + 有效测进 `run_verify`；文档 VERSION 漂移对齐。3.7.x 另见 CHANGELOG（凭据不下发、候选预热等）。  
> 3.3.0：管理端用户统计。3.0+：物理删 render。2.7.x：写路径/探活 v1；sid-only；双榜分域；单 worker。

| # | 边界/债 | 当前可接受的理由 | 触发升级的条件 |
|---|---------|------------------|----------------|
| T1 | 账号密码**明文**存 `看板账号.json` | 内网 5 人、管理员可见明文是业务要求；实现成本低 | 人数扩大/需 SSO/审计强制哈希 |
| T2 | 智云**内部译员**依赖专用账号/行数护栏 | 权限不足账号会降级不中断管道 | 陆总给全量任务账号后关黄 |
| ~~T3~~ | ~~预渲染 HTML 体积~~ | **3.0+ 已删 render 装运**；看端 VM | — |
| T4 | **SQLite 单文件**单机 + **单 worker `_state`** | 零运维；2.7.0 明文书写 | 多写并发/HA → Phase3 Postgres + 状态外置 |
| ~~T5~~ | ~~legacy cookie 至 2026-08-15~~ | **2.7.1 已硬删读路径**；登录 delete 旧名 | — |
| T6 | 登录页仍小体积**内嵌 server.py** | 体量小、非主路径 | 要统一主题构建时再抽 |
| T7 | shell 会话过期跳转 | **已缓解观察**（authRequired/LoginView 路径）；日常少碰 | 用户再报白屏/死链则复核 |
| T8 | 无公网、无 HTTPS 默认 | 内网信任边界 | 跨网段/合规要求 TLS |
| T9 | 手填/分摊/去税 **靠人记** | 业务就是人工口 | 缺月漏填导致决策错 → 加强强制校验/提醒 |
| T10 | 附加税费措辞 vs 陆总口头「收入×6%×12%」 | 代码用不含税基数与税法一致 | 陆总明确改基数则改 profit |
| T11 | 公共费用分摊比例 **业务细则**仍靠手填 | 机制已上线；**展示挪归属 3.3.1 已 int 分** | 陆总定 100% 分光/全年一套等再收紧校验 |
| T12 | 测试活体浏览器 **未进 CI** | 本地 optional；**有效测须进 run_verify**（3.3.1 铁律） | 再出 dTbl 类假绿 → 加 headless 冒烟 |
| T13 | `parse_date_parts` **不校验非法日历日**（如 2/30） | 坏行多半进不了合法周期；历史数据兼容 | 明昊拍板是否严校后改解析 |
| T14 | 预算表 **比率与金额共用「分」存** | 数值 round-trip 一致；schema 简单 | 比率列语义混淆时拆列 |
| ~~T15~~ | ~~部分 POST 写路径仍 `/api/*`~~ | **2.7.2 已清**：adjust/refresh/my_passwd/update/health 均 v1 | — |
| ~~T-API-双榜~~ | 曾疑似双源 | **2.7.0+**：profit vs full 分域；读写 v1 | — |
| ~~DEBT-1~~ | ~~分摊展示 float 旁路~~ | **3.3.1 已清**：`alloc_amounts_by_period`/`apply_alloc_to_pc_view` int 分 | — |

## 升级排序建议（明昊视角）

1. 配置类黄灯清零（销售 BU 归属、译员账号）— 业务价值高、改码少  
2. 业务手填/公共分摊比例补齐 — 业务价值高  

3. 密码明文存储（MADR-0020 产品接受）/飞书 SSO — SSO 见生产化路线图 Phase3；明文缓解=0600+内网+防爆破+12h  
4. CI 真实退出码 + 冒烟 — 防假绿  
