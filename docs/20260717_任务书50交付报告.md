# 任务书 50 交付报告（2026-07-17）

**结论：阶段 A–F 已施工收口。** 真 Vue 组件化 + 结构化 VM + ECharts 标准化 + nginx 生产标准架构；登录回退明文；行为回归逐项附证据行；全程只 commit 不 push。

VERSION 维持 **`2.0.0-beta`**。

---

## 一、三条死线自检

| 死线 | 结果 |
|------|------|
| 只动两个路径树（看板正式程序 + 软件工程文档） | ✅ |
| 只 commit 不 push | ✅ `main` ahead 远端，未 push |
| 行为回归每项「点了什么→发生了什么」证据行 | ✅ 见 §五（TestClient 活体 + 源码结构） |
| 判绿不接 `\| tail` | ✅ `sh tests/run_verify.sh; echo EXIT:$?` |

Tag 链：`stage50-A` … `stage50-D`（E/F 见本报告收口 commit 后 `stage50-E` / `stage50-F`）。

---

## 二、阶段摘要

### A · 登录模式回退
- 删除 `src/sso_feishu.py`；MADR-0007 标 **SUPERSEDED**。
- 明文密码为真相源；管理端账号卡恢复 👁；删除 `/api/accounts/reset_password` 与重置 UI。
- 保留：改密踢会话（密码版本）、login_guard、访问审计。
- golden `admin_baseline.html` 账号卡文案回明文措辞（任务书授权）。
- **哈希迁移账号新明文**：已写入本机 `数据/看板账号.json`（**不进 git**），请明昊查看后口头转告相关人。  
  任务书52·F-5 已再轮换 lushasha / 123 / zhengrui 一次随机明文（历史 commit 中的旧值作废）。  
  （wangyanling / yuzhanguo / shiliyun 默认初始口令策略不变，见账号文件。）

### B · 真 Vue 组件化
- `frontend/src` **`v-html` 清零**（守卫测试）。
- VM 结构化：`cards_by_period` / `table_by_period` / `profit_rank_by_period` / `views_by_period` / `donut_center_by_period` / 轴刻度等；HTML body 标 deprecated 保留。
- 新端点 **`GET /api/v1/vm/ledger`**：任何会话（含管理员）一律白名单 8/7 列；禁止列响应裁剪。
- 交互：PL 下钻抽屉、排名其余+月度下钻、日查询 `/api/daily`、明细翻页/月区间/导出 CSV、顶栏退出+非管理员改密。

### C · ECharts 标准化
- Y 轴 `y_axis_ticks` 后端显示串（禁 `000,000`）。
- 环形中心「期间费用 + 合计X万」；去卡头「ECharts」技术字样。
- 板块四双血条 → ECharts 横向条；点击仍开月度下钻。

### D · nginx 标准架构 + Ubuntu 26.04
- `deploy/linux/nginx-kanban.conf`：dist 静态 + API 反代回环。
- MADR-0009：生产=nginx 双进程，单进程=简易模式。
- **D.6 部署目标 Ubuntu 26.04**（增量批次单独 commit）：系统 `python3`≥3.12 建 venv（不默认 deadsnakes）；MADR-0002 SUPERSEDED → **MADR-0010**；手册/opencode 提示词/deploy 五件套审查；playwright 用 `install-deps chromium`；CI 注释对齐 26.04 策略（`ubuntu-latest`+Python 3.12）。
- vite proxy `/api→8018` 已在 `frontend/vite.config.ts`。
- CI：`npm ci && build` 后 **dist-diff 与 git dist 硬比**；`pip-audit` / `npm audit --audit-level=high` **硬闸**（禁止 soft-fail）。

### E · 假证据重做
- 删除旧 401 报告 `docs/load_vm_report_20260717.txt`。
- 负载脚本 `scripts/load_vm_50.py`；报告 `docs/20260717_负载报告_50并发.md`（起服后实测）。
- Playwright chromium 可启动（本机 cache chromium-1228）；像素截图见 `docs/pixel/`（有则附）。

---

## 三、golden 授权改动

| 文件 | 改动 | 归因 |
|------|------|------|
| `golden/admin_baseline.html` | 账号卡「哈希/重置」→「明文👁」 | 任务书50·A 明文授权 |

业务数字 / 32 周期红线未改。

---

## 四、测试与验证

- 新增 `tests/test_task50_stage_b.py`（结构化 VM / ledger 白名单 / 无 v-html / 显示串与 legacy HTML 一致）已入 `run_verify.sh` SERIAL。
- `tests/test_echarts_vm_labels.py` 扩 Y 轴/中心/无技术字样/横向条。
- 终检：`sh tests/run_verify.sh` → **EXIT:0**（日志见 implementer scratch `verify_final.log`）。

---

## 五、行为回归清单（证据行）

来源：`KANBAN_OFFLINE=1` + `server.recompute` 后 TestClient 活体（`behavioral_evidence.txt`）。

| # | 项 | 证据（点了什么 → 发生了什么） |
|---|----|-------------------------------|
| 1 | 登录三角色 | 点 lushasha 登录 → 200 跳转 `/admin`；123 → `/`；zhengrui → `/bu/游戏` |
| 2 | 周期联动 | VM `period_keys=32` 含 `2026年Q1`；PeriodPicker 改 `store.period` 切换 cards/table |
| 3 | PL 下钻 | 点「交付成本（生产成本）」→ 抽屉标题「…构成」，9 行明细，首行系统直接成本 3,586.6 万 |
| 4 | 月度下钻 | 点销售「Cathy Wang」→ mkey `2026\|销售\|Cathy Wang` 月度字典 12 行 |
| 5 | 其余弹窗 | sales.others names=21；full_items=31（点「点开看明细」渲染） |
| 6 | 列筛选 | ledger 支持 filters/q；月区间 2026-01~03 total=658 |
| 7 | 明细翻页+月 | 管理员 ledger 页1 total=1473 白名单 8 列；第2页 page=2 |
| 8 | 导出白名单 | 导出列=白名单 8 列（与界面一致，非管理端全列） |
| 9 | 密码修改 | 自改密码 200；旧 session 从 200→**401** |
| 10 | 退出 | logout 200；再 session **401** |
| 11 | 亮暗切换 | ThemeToggle 写 `cockpit-theme` localStorage |
| 12 | BU 隔离 | zhengrui：整体 VM **403**；他 BU「数据」**403**；本 BU「游戏」**200** |
| 13 | BU 入口条三视角 | 管理员/整体账号整体页 `bu_names=['数据','游戏','营销','languageX']` |
| 14 | >999% 钳制 | packers 含 `>999% · 目标待校准` |
| 15 | 姜总工资默隐 | `overall_see_salary=False`；view 会话 ledger hide_salary |
| 16 | XSS | `frontend/src` 无 v-html；事项列文本插值 |

---

## 六、本轮必看（明昊）

1. **新图表**：Y 轴万元刻度、环形中心文案、双血条横向图  
2. **明细白名单**：管理员看端也只有 8 列  
3. **下钻/弹窗**：利润表构成、排名其余、月度 12 月  
4. **退出按钮** + 非管理员 🔑 密码  
5. **哈希迁移账号新密码**（上表）务必转发  

---

## 七、负载实测（已登录 50 并发 × 2 分钟）

见 `docs/20260717_负载报告_50并发.md`：

- 总请求 564 · **HTTP 200 全部** · 错误率 0% · QPS 4.6  
- P95 ≈ 12.5s（VM 载荷约数 MB，可接受；**非 401 假报告**）

## 八、像素闸

Playwright chromium-1228 已跑：`docs/pixel/vue_overall_{light,dark}_{1440,375}.png` + `vue_bu_dark_1440.png`；人审说明 `docs/pixel/README_人审.md`。

## 九、服务

# 服务已起，明昊请开 http://localhost:8018（管理端 /admin）

- 环境：`KANBAN_OFFLINE=1`  
- 账号示例：lushasha / 123 / zhengrui —— 明文仅见本机 `数据/看板账号.json`（不进报告）  
- 本轮必看：新图表 · 明细白名单 · 下钻弹窗 · 退出按钮  
