# 任务书 54.10：封板复验 + 上线交接包（最终单·跑完 54.9 后执行·本单收口=可上线）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；最终 tag `stage55_rc1`（发布候选）。
> 你是执行机器兼最终质检:54.7 的终验结论核的是 54.8/54.9 改动**之前**的状态,本单在一切尘埃落定后**重验关键项**,然后把明昊人审和部署需要的一切打包成「上线交接包」。
> **本单原则上零修改**——只验证、只打包;若复验发现 54.8/54.9 引入的回归,按 R 级修复（当场修+单独 commit+复验）,但禁止任何新功能新优化。

## 前置自检（不满足=停下只写说明）

`git tag` 中 stage54p6/54p7/54p8/54p9 齐全;54.7 与 54.9 交付报告结论行分别为"上线就绪"与"美学终修完成";工作区干净。

## 一、封板复验（54.9 大改视觉后,以下结论必须重新拿一遍）

1. **红线**:`sh tests/run_verify.sh; echo EXIT:$?` EXIT:0（禁 `| tail`）;`KANBAN_OFFLINE=1 python run.py` EXIT:0;vue-tsc 绿。
2. **性能复测**（54.9 改了大量样式,防性能回归）:跑 54.8 固化的 `tests/perf_check.py` 全项达标;375 视口快速走查无卡顿。
3. **视觉抽验**:看板主页(深/浅/375)+管理端 3 个代表页,截图对照 54.9 的审计表结论仍成立;console 零 error。
4. **交互抽验**:登录三分流/周期切换/下钻/R-00a 那个 99+ 入口/数据调整斑马纹页——各真点一遍无回归。
5. **全站死链与文档一致性**:54.8 归整后的索引/文档地图逐链点验零死链;README 跑法照做能跑通;用户手册截图与 54.9 后的真实界面一致（不一致=重截）。
6. **发布安全终扫**:`git diff stage54p3..HEAD` 全量 grep 无真实客户名/金额/内网 IP/账号/密码;`git status` 干净;dist 与源码一致（重建对比）。
7. **版本封板**:VERSION、页面显示、CHANGELOG 三处统一升至 `2.0.0-rc1`;CHANGELOG 汇总 54.4~54.10 全部批次条目。

## 二、上线交接包（新建 `docs/上线交接包/`,给两个读者,一个不懂技术一个是 AI）

1. **`人审指南_明昊10分钟清单.md`**（给明昊）:起服命令(8018);按序要看的页面清单与每页"重点看什么"（一句话一条);手机扫码怎么测;五单「待拍板」清单**全量汇总**（从 54.5~54.9 各交付报告收集,一条不漏,每条附上下文链接）;发现问题怎么记（大白话+截图即可）。
2. **`部署执行单_给Claude.md`**（给部署方）:push 前安全检查项;push 命令与目标(GitHub+Gitee main);部署机步骤（git pull/venv/systemd/nginx/healthcheck cron/备份 cron/BIOS 来电自启提醒）;冒烟清单;回滚命令(`git checkout stage54p3` 等各锚点说明)。
3. **`发布说明_v2.0.0-rc1.md`**:本次发布全部变化的用户视角汇总（从 CHANGELOG 提炼,零术语）,供上线公告定稿时取材。
4. **`封板复验报告.md`**:第 1 行必须是 `结论：封板通过,交接明昊人审` 或 `结论：封板失败,原因如下`;正文=上面复验 1~7 每项的命令+真实结果+证据路径;若有 R 级修复,列清单。

## 收口交付

- [ ] 复验 1~7 全绿（或 R 修复后复验全绿）
- [ ] 上线交接包四份文件齐全,人审指南让"没跟过项目的人"也能照做
- [ ] 待拍板汇总零遗漏（逐单核对 54.5~54.9 交付报告）
- [ ] tag `stage55_rc1`;只 commit 不 push;交付报告=封板复验报告（不另写）

## 非目标

push（人审签字后 Claude 执行）、部署机操作、公告定稿、任何新功能/新样式（发现"还能更好"的想法写进交接包附录「后续建议」,不许动手）。
