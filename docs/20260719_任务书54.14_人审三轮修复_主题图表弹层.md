# 任务书 54.14：人审三轮修复——主题切换·图表显示·弹层图层·费用明细图（含一条回归追责）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；收口 tag `stage55_rc5`。
> 你是执行机器：只修本单清单+R-00 自查发现，禁止顺手改别的；每条附「复现→修复→复验证据」，证据落 `docs/验收证据/20260719_54p14/`。
> 验收尺度=54.12 §完美标准五条（可点性/正确性/稳定性/一致性/卫生）继续有效;口径计算区仍是禁区（数字变一分=回滚）。

## 前科条款（本单重点盯这两条）

1. **回归追责（R-20 详述）**:54.12 R-10「万万」点名修过,明昊三轮人审发现 **BU 页仍显示"36.0万万"**——你修了整体页漏了 BU 页,这正是被零容忍的"修一处漏一处"。本单每条修复收口前,**必须列出该问题可能出现的全部位置清单并逐个核**（整体页/各 BU 页/管理端/深浅色/375）,清单进证据。
2. 上一轮弹层透明修过（54.11 R-03）,本轮周期选择器弹层又被遮挡——说明当时没把"所有弹层"扫全。同族问题必须真正全局清。

## R 清单（按严重度排序,每条单独 commit `fix(54p14): R-xx 一句话`）

### R-20 「万万」回归清剿（最高优先·回归追责）
- BU 页期间费用中心仍"36.0万万"。先查 54.12 R-10 当时改了哪、为什么 BU 页没覆盖（同组件不同数据路径?还是另一处拼接?）,把根因写进报告;然后按 54.12 定的"VM disp 字段单位约定"全局 grep 所有 `}}万` / `+'万'` 拼接点,整体页+全部 BU 页+管理端逐屏核,一次清干净;补一条自动化断言(渲染结果不含"万万")进测试。

### R-21 深浅色切换半生效（明昊实报·体验硬伤）
- 现象:管理端点「浅色」只有顶栏变,内容区必须整页刷新才变;反向同样。
- 排查方向:主题 class/CSS 变量只在挂载时读取、组件未响应主题状态变化(或 ECharts 实例主题没跟着 setOption 重建)。
- 收口标准:任意页面点亮/暗切换,**全部区域(含所有 ECharts 图表、Element Plus 组件、banner)零刷新即时切换**;看端+管理端全部页面 Playwright 各切换一次截图对比留证。

### R-22 周期选择器弹层被遮挡+点不到（明昊实报·功能不可用级）
- 现象:「月」的月份网格、「自定义区间」的起止选择,弹层图层在 KPI 卡之下被遮,自定义区间根本点不到;月份增多(8月/9月…)会更严重。
- 修复:弹层 z-index 置于全部卡片之上(用全站弹层 token 统一,不许写死魔法数字);父容器 overflow 不得裁剪弹层;弹层背景按 54.11 R-03 的不透明度标准(≥0.95+遮罩可选);**预演 12 个月全满的布局**(改系统时间或 mock 12 个月数据)确认不遮挡不溢出。
- 收口:Playwright 真点「月→12个月每一个」「自定义区间→选起止→应用」全链路成功;深浅色+375 各验一遍。

### R-23 图表上下显示不全+比例失衡（明昊实报）
- 「费用月度趋势·按报表大类」与「回款情况」两图:上下内容被裁(图例/轴标签),回款情况内容不满容器、留白比例失衡。
- 修复:两图容器高度按内容自适应(参考 54.3 修"月份轴被裁"的方案);图与容器的填充比例对齐 54.9 R5(无空洞无拥挤);**全站所有图表逐个核一遍同族问题**(含 BU 页、375)。

### R-24 折线冲出图表顶部（Claude 截图发现）
- 游戏 BU 毛利率线 7 月冲出绘图区;回款情况回款率线 1 月/6 月同款。
- 修复:比率轴上限自适应数据最大值(或夹持+超限标注),线永远画在绘图区内;负值/0/超 100% 三种边界各造一组数据 Playwright 截图验证。

### R-25 浅色主题体检黄条不可读（Claude 截图发现）
- 浅色下多条黄条文字对比度崩(黄底浅字)。修复:banner 深浅色双套 token,对比度 ≥4.5:1 实测;顺带把浅色下全部提示/告警类元素扫一遍。

### R-26 费用明细区新增统计图（明昊需求·产品项）
- 要求:与现有图型(柱/环/折线/条形)**都不同**、但视觉气质统一的图——**方案定死:月份 × 报表大类 的「热力格子图」(ECharts heatmap)**,横轴 12 个月、纵轴报表大类、格子颜色深浅=金额,悬停显示金额,点击格子联动筛选下方明细表(联动做不了就先不做联动,列待拍板)。
- 深空主题配色(青→金渐变),浅色适配,375 可横滚;数据全部来自现有 VM/接口,**前端零金额运算**;若需后端新聚合接口,按 VM 契约规范加只读端点并补测试。
- 收口:深浅色+375 截图;数字抽 3 格与费用明细表筛选结果核对一致。

## R-00 自查自修循环（继续执行）

按 54.12 R-00 同法:全站(重点=深浅色两套×整体/BU/管理端×1440/375)再走查一轮,发现新问题续编号当场修,循环到零新增。**本单特别关注主题切换后的每个页面状态**——R-21 修完后,切换态就是新的走查维度。

## 收口交付

- [ ] R-20~R-26 全修+R-00 零新增;每条含「全部可能位置核查清单」
- [ ] 全量:`sh tests/run_verify.sh; echo EXIT:$?` EXIT:0(含 ruff 关);perf_check 达标(新增热力图不得拖慢首屏);console 零 error
- [ ] 文档联动:CHANGELOG/用户手册(新热力图要新增一段怎么看+截图)/54.13 架构图若组件结构变了同步/上线交接包封板报告追加 rc5 节/演示动线若受益于热力图可更新
- [ ] 代码整洁度保持:54.13 的 M1~M10 不许退步(新增组件 ≤400 行、复杂度 ≤10、ruff 0 错)
- [ ] 版本 `2.0.0-rc5` 三处一致;tag `stage55_rc5`;只 commit 不 push
- [ ] 交付报告 `docs/20260719_任务书54.14交付报告.md` 第 1 行:`结论：三轮修复完成,请明昊复审`;含 R-20 根因分析与「万万」为何漏 BU 页的检讨

## 非目标
新功能(热力图除外)、口径改动、push、性能之外的重构。
