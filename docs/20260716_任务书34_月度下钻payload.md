# 任务书 34 · 月度下钻 payload 归一（2026-07-16）

**基线**：`456d2dd`（任务书 30+33 已验收）  
**仓库**：`程序/看板正式程序/`  
**纪律**：**只 commit 不 push**（Claude 验收后再推）  
**本地 tip**：`be5679f`（docs）← `2914e33`（fix）；`origin/main` 仍 `456d2dd`（ahead 2）

## 问题
陆总#8 实现把 12 个月 JSON 以 `data-monthly` / items[].monthly 嵌在每行、每周期 → fragments 实测 ~16MB 级不可接受。

## 修法
- 页面级 `rankings_monthly_data`：`{ "2026|销售|甲": [12 月显示串…], … }`
- 排名行仅 `mkey` / `data-mkey`
- `paintRankingMonthly` 查表拼 DOM（与改前 dual-month 同形）
- 显示串仍 Python 预算（铁律2）

## 实测（golden `_golden_data`，`core.generate` → `cockpit_fragments`）

| 指标 | 值 |
|------|-----|
| fragments JSON 字节 | **2 989 429**（≈ **2.85 MB**） |
| 3MB 上限 | 3 145 728 |
| 是否 ≤3MB | **是** |
| 周期数 | 24 |
| 月度字典键数 | 257 |
| 扫描排名行 | 6561 |
| 仍带 `monthly` 大数组的行 | **0** |
| 带 `mkey` 的行 | 6561 |

证据目录：implementer scratch `fragments_size.txt` / `payload_shape.txt`。

## 测试
`tests/test_ranking_monthly_a8.py`（8 例）+ `run_verify` 全绿。

## 未做
- 不 push 远端；不重录 golden 数字基线；不改业务口径/弹窗视觉
