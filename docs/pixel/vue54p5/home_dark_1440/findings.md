# 找茬记录 · 主页暗色 1440

对照：legacy 整体页_暗色_1440（只许看）+ 54.1 V1–V8；视口内截图 `viewport.png` / `before.png` / `after.png` / `sec_mid.png` / `sec_lower.png`。

## 茬 1 · 客户名截断（V6 复发）

- **我看到了什么**：板块③「收入·按客户」行名显示为 `示例客户012有限…`，末字被 ellipsis 吃掉，需悬停 title 才见全名。
- **挑出了什么茬**：`.ev-name`/`pr-grid` 固定 120px 过窄，与 54.1 排名「行名不截断」口味冲突。
- **改了什么**：名列加宽写在 **`frontend/.../scifi-bridge.css`**（In 范围；theme.css 已 revert 不动）。复测 DOM：`示例客户012有限公司` 完整、无省略号（见 `after_names.png`）。

## 茬 2 · 趋势图柱顶标签三重叠

- **我看到了什么**：收入/成本柱顶数字 + 毛利率黄字叠在柱体中部，1–7 月区需眯眼才能分清。
- **挑出了什么茬**：V6「图表文字清晰」不达标；三重 label 同时 show。
- **改了什么**：`TrendChart.vue` 只保留收入柱顶标 + 毛利率线标（金色、distance 8）；成本标关（数值走 tooltip）；`grid.top` 40→48。

## 茬 3 · 费用月度多系列点标拥挤

- **我看到了什么**：费用折线各点堆数字（149.9/174.9…），线密时字糊在一起。
- **挑出了什么茬**：多系列折线（D2）可保留，但常显点标与「可读」冲突。
- **改了什么**：`ExpenseTrend.vue` 默认 `label.show=false`；合计/分项靠 tooltip + 可点击图例隔离；`grid.top` 略收。

## 复核（修后视口）

- V1 区块标题编号方块+装饰线：仍高对比，未回归。
- V2 五卡一排：`kpi-5` 保留。
- V3 利润表：行铺满、查看构成仍在。
- V4/V5：按 54.4 D1 无持续呼吸；静态网格星点不脏底。
- V7 多系列折线 tag 仍在。

**结论**：本屏三茬已修；并排对照后可挑不出上述三类问题。证据：`after.png` / `sec_lower.png`。
