# 找茬记录 · 主页暗色 375

对照：legacy 整体页_暗色_375 + V2/V8；视口 `viewport.png`（375×812）。

## 茬 1 · 横向溢出？（复核）

- **我看到了什么**：顶栏标题与「2026年 / 亮暗 / 退出 / 密码」折成两行，高度约 87px。
- **挑出了什么茬**：疑似窄屏顶栏挤；用 `scrollWidth===clientWidth` 复核 → **无横向溢出**（V8 过）。
- **改了什么**：本屏无代码改；判定「折行是允许的 wrap，不是溢出 bug」。

## 茬 2 · KPI 是否仍五卡一排（不该）

- **我看到了什么**：KPI 单列堆叠，视口内可见下单/交付/毛利/税前，回款在滚动区。
- **挑出了什么茬**：对照 V2「窄屏应降列」— 单列符合 theme `@media` 断点，**非 bug**。
- **改了什么**：无。DOM 计 `kpiCount=5` 齐全。

## 茬 3 · 字号/对比

- **我看到了什么**：KPI 大数字 ~45,943.1 与副文案对比清晰；区块「一 基本情况」编号方块实底。
- **挑出了什么茬**：并排对照后无「眯眼才看清」；无图表裁切（本视口未入图区）。
- **改了什么**：无。沿用 1440 屏对 theme/Trend 的全局修。

**结论**：375 视口并排对照后无差异需返修；证据 `viewport.png` + 溢出度量。
