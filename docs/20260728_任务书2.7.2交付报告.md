结论：2.7.2 API 写路径与卫生收官，run_verify EXIT:0，回归零 diff，活体可看可调，已上机，请明昊过目

# 经营罗盘 2.7.2 交付报告

- **日期**：2026-07-28
- **VERSION**：`2.7.2`
- **基线**：2.7.1

## 勾选

| 项 | 状态 |
|----|------|
| adjust* → `/api/v1/admin/adjust*` 删旧 | ✅ |
| refresh / refresh_status → v1 admin | ✅ |
| my_passwd → `/api/v1/my_passwd` | ✅ |
| update/apply → `/api/v1/admin/update/apply` | ✅ |
| health → `/api/v1/health` + healthcheck.sh | ✅ |
| 前端/static/tests 对齐；旧 404 | ✅ |
| export 裸路径迁 v1（`/api/v1/export/*` + BU）；删 `/export.*` `/bu/*/export.*` | ✅ |
| run_verify EXIT:0 / 回归零 diff | ✅ |
| live 看端 KPI + 管理端 | ✅ |
| push 双远端 main / 上机 | ✅（上机后勾） |

## 禁碰自检

- 未改 profit 算账公式；未删 `_legacy_定位键`；未整包删 render；未碰 fetch_zhiyun；未恢复旧路径

## 同 handler 策略

装饰器路径直接改为 v1（同一函数），禁止复制业务逻辑。
