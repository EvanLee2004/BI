# 任务书 51 交付报告（2026-07-17）

**结论：结构性整改已收口为「单一建造链」。** vue 路径不再双管道生成 HTML；利润表/KPI 共用 `pl_structure`；VM 组装 `_assemble_vm`；明细鉴权 `resolve_expense_view_access`；ledger 真导出 xlsx + 周期月区间 + 轴刻度 meta；前端 `types/vm.ts` + `vue-tsc` 入 verify。纯结构重构——数字与 32 周期红线/golden 未改。只 commit 不 push。

VERSION 维持 **`2.0.0-beta`**。Tag：`stage51`。

---

## 一、死线自检

| 死线 | 结果 |
|------|------|
| 只动 `看板正式程序/` + `软件工程文档/` | ✅ |
| 只 commit 不 push | ✅ |
| 32 周期红线与 golden 一分不动 | ✅ 未改 golden 数字基线 |
| 页面像素与数字零变化（结构等价） | ✅ PL HTML 重构前基线 SHA 对齐；VM 字段快照对齐；vue-tsc 绿 |
| 判绿真实退出码 | ✅ `sh tests/run_verify.sh; echo EXIT:$?` |
| 批次 1 最先 + 负载 P95 前后对比 | ✅ 见 §三 |

---

## 二、批次摘要

### B1 杀双管道
- `frontend=vue`：`_vue_core_views` 只建 period keys + rankings_view，**不执行** render HTML。
- `frontend=legacy`：仍走完整 HTML views。
- Commit：`af51fb4`

### B2 利润表/KPI 单一结构
- 新 `src/domain/pl/structure.py`：`pl_structure` / `structure_for_vm` / `kpi_peak_for` / `kpi_target_bar`。
- `render_pl_table` / `render_bu_pl_table` 与 `pack_pl_by_period` 均消费结构。
- 重构前基线（golden 数据）：overall HTML SHA `15b9ec93d8d7fa9c`、BU HTML `e80ac57800883dcc`、pack JSON 全等。
- Commit：`1ea2ef4`

### B3 合并 VM 组装
- `_assemble_vm(summary, views, *, scope, …)` + cockpit/bu 薄包装。
- Commit：`8916d6c`

### B4 明细鉴权统一
- `authz.resolve_expense_view_access(..., force_whitelist=)`。
- `/api/detail` → False；`/api/v1/vm/ledger` → True。
- Commit：`c5170f4`

### B5/B6/B7 分支归位
- **导出**：`GET /api/v1/vm/ledger/export`（xlsx，openpyxl，同源白名单策略）；前端删 DIY CSV。
- **周期**：VM `ledger.period_months[key]={month_from,month_to}`；前端只赋值。
- **轴刻度**：`y_axis_min/max/interval` + ticks；前端 `tickLabel` 精确匹配，删最近扫描。
- Commit：`5413158`

### B8 前端契约类型
- `frontend/src/types/vm.ts` 对齐 pydantic；store/组件去 `as Record`。
- `npm run typecheck`（`vue-tsc --noEmit`）入 `tests/run_verify.sh`。
- Commit：`a1d8dd9`

---

## 三、负载 P95 前后对比（B1）

环境：`http://127.0.0.1:8018`，账号 `123` 已登录 cookie，**50 并发 × 120s**，路径 `/api/v1/vm/cockpit`，全部 HTTP 200。

| 指标 | B1 前 | B1 后 | 变化 |
|------|-------|-------|------|
| 总请求 | 567 | 752 | +32.6% |
| QPS | 4.6 | 5.9 | +28% |
| 响应体（约） | 6.8 MB | 4.4 MB | −35% |
| **P50** | 11077 ms | 8453 ms | −24% |
| **P95** | **12277 ms** | **9307 ms** | **−24%** |
| P99 | 12977 ms | 10645 ms | −18% |
| HTML 字段 | 满填（kpi/pl/donut/rank…） | 全空（vue 不生成） | 结构目标达成 |

说明：P95 有实质下降且 payload 变瘦，但未到「数量级」——rankings 结构化仍大；若需继续压延迟应拆 rankings 懒加载（超本任务范围）。

原始报告：implementer scratch `load_before_report.md` / `load_after_report.md`。

---

## 四、像素 / 数字等价证据

| 证据 | 结果 |
|------|------|
| overall `render_pl_table` vs 重构前基线 | 逐字节相等（SHA `15b9ec93…`） |
| BU `render_bu_pl_table` + tag_note | 逐字节相等 |
| `pack_pl` overall/BU vs 重构前 JSON | 逐字段相等 |
| KPI pack vs 重构前 | 相等 |
| 32 period_keys | 仍为 32（`close_vm_snapshot.json`） |
| vue 路径 HTML body | 空 dict / 空串 |
| `vue-tsc --noEmit` | EXIT 0 |

---

## 五、测试清单（本任务新增）

- `tests/test_task51_pl_structure.py`
- `tests/test_task51_assemble_vm.py`
- `tests/test_task51_authz_access.py`
- `tests/test_task51_batch5.py`
- `tests/test_task51_frontend_types.py`
- 契约快照：TrendVM / ReceiptsVM / LedgerVM / CockpitVM / BUPageVM schema 同步新字段

全量：`sh tests/run_verify.sh` → **EXIT:0**（2026-07-17 收口实测，含 vue-tsc + 32 周期红线 + 串行/并行全文件 + task51 五套新测）。

---

## 六、行为回归（结构相关）

| 点了什么 | 发生了什么 | 证据 |
|----------|------------|------|
| 默认 vue 打开 cockpit VM | 无 legacy HTML 字段、有结构化 cards/table | B1 测试 + vm_keys_after |
| legacy 模式 | 仍可生成 HTML bodies | `frontend_mode` 分叉 |
| PL 下钻 / 金额串 | 与重构前同一结构源 | B2 基线全等 |
| 管理员看端 ledger | 仍白名单列 | test_task50_stage_b |
| 导出 Excel 按钮 | 下载 xlsx（非 CSV） | 路由 + LedgerTable.vue |
| 切周期 | month_from/to 来自 VM | period_months |
| 趋势/回款 Y 轴 | min/max/interval + 精确标签 | tickLabel |

---

## 七、文档与版本

- CHANGELOG / 概要设计补记 / 测试说明：同步本批次。
- 本报告：`docs/20260717_任务书51交付报告.md`
- Tag：`stage51`（本地，未 push）
- 服务：`KANBAN_OFFLINE=1 python run.py --serve` → **:8018**
