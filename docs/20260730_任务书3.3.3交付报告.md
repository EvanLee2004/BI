3.3.3 去掉 KPI「目标待校准」：超目标显示真实%/ >999% 软顶；run_verify EXIT:0；双远端；生产 tip 对齐。

# 交付报告 · 经营罗盘 3.3.3

**日期**：2026-07-30  
**基线**：VERSION 3.3.2 · tip `dcba777`  
**目标**：VERSION **3.3.3** · 本地 tag `stage_3_3_3`（不推 tag）

---

## 一句话

看板 KPI 目标进度条彻底去掉「目标待校准」；超目标显示真实完成率%（极端值软顶 `>999%`）；算账/目标数值不动。

---

## In 变更

| 路径 | 变更 |
|------|------|
| `src/domain/pl/structure.py` | 新增 `format_target_pct_disp`；`kpi_target_bar` 两处 pct 展示串按新规则 |
| `src/viewmodels/__init__.py` | `_attach_year_budget_bars` 复用同一展示规则 |
| `tests/test_task_2_6_1_budget_pct_display.py` | 断言改为 `>999%` / 正常% |
| `tests/test_task_2_6_1_rankings_full_and_scroll.py` | 同上 |
| `tests/test_task57_b6_domain_coverage.py` | pretax 超极端 → `>999%` |
| `tests/test_task_3_3_3_no_target_calibration_label.py` | 守卫：src 无该五字 + 150% / >999% |
| `VERSION` / `CHANGELOG.md` / `src/version.py` / `Agent.md` / `docs/Runbook.md` | 3.3.3 |

### `pct_disp` 规则

| 条件 | 展示 |
|------|------|
| pct is None | `—` |
| 0≤pct≤100 | 现有格式（margin `.0f%`，金额 `.1f%`） |
| 100&lt;pct&lt;1000 | 同格式真实进度% |
| pct≥1000 | 仅 `>999%` |
| bar_w | 仍 `min(max(pct,0),100)` |

---

## Out 遵守

- 未改 profit 算账、预算 API、目标默认值、fetch_zhiyun、定时、会话、端口、nginx conf  
- 未发明「已超目标」等新说教文案  
- 未 force push、未推 tags、未发飞书  

---

## 门禁

- `KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?` → **EXIT:0**
- 回归红线：32 周期 + 趋势/回款/体检 **逐数字一致**
- `rg '目标待校准' src` → **0 命中**

---

## 证据

- 本地（gitignore）：`docs/验收证据/3_3_3/`（baseline / rg_before·after / run_verify.log / pct_disp_function_cases / live 记录）
- 函数级活体：真实 `kpi_target_bar` pct=150→`150.0%`；pct=5000→`>999%`；无待校准字样

---

## 部署

- push：`origin` + `gitee` **main**（禁 force / 禁推 tags）
- 上机：ff-only pull + reload/restart；核 tip=本地、VERSION=3.3.3、health 200
