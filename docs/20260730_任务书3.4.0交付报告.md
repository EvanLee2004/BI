3.4.0 重点客户分析：自然年下单预估六档 S–E + 双饼 + 四底列表/月钻；整体+BU；run_verify EXIT:0；回归旧数字零 diff。

# 交付报告 · 经营罗盘 3.4.0

**日期**：2026-07-30  
**VERSION**：3.4.0  
**本地 tag**：`stage_3_4_0`（不推）  
**push/上机**：本单默认只 commit 不 push

## 交付摘要

在整体页与各 BU 页「四、下单与回款」最底部（RankingsDual 之后）交付**重点客户分析**：

- 口径：自然年 · 下单预估本币 · 每年清零
- 档：S≥200 · A[80,200) · B[30,80) · C[10,30) · D[3,10) · E(0,3) 万（ytd>0 才进）
- 左：六档列表，S/A/B 默认展开，C/D/E 可点开（懒加载）；点客户 → 1～12 月下单
- 右：级分布双饼（个数+占比 / 金额到万+占比）
- 跨 BU = 本 scope 销售名下；别名不合并；日查不带动本块；切月不重算等级

## 架构

```
domain/key_customers（纯函数）
  → summary["key_customers"]（每年一次，非 period）
  → packers.pack_key_customers → CockpitVM/BUPageVM
  → Vue KeyCustomersPanel
可选：GET /api/v1/key-customers/tier（鉴权同 rankings/full）
导出：embed_key_customers_full=True 全档 items
```

**未改**：利润公式、`fetch_zhiyun`、rankings/full、orders_by_customer。

## 验证

- `tests/test_key_customers_3_4_0.py`：10 passed
- `tests/test_frontend_arch_guards.py`：绿
- `KANBAN_OFFLINE=1 sh tests/run_verify.sh` → EXIT:0
- 证据：`docs/验收证据/3_4_0/`

## DoD

- [x] 整体 + BU 均有；位置=四底
- [x] 六档阈值测锁；双饼与列表同源
- [x] S/A/B 默认开；C/D/E lazy 可拉
- [x] 月钻；切月不改级；日查不改本块
- [x] 导出 embed；守卫绿；run_verify EXIT:0；旧回归零 diff
- [x] VERSION=3.4.0；证据+文档齐
