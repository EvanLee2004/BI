# 任务书 55：终局总攻——四阶段一次跑完,到「人审签字即上线」的完美态

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；终点 tag `stage55_final`,版本 `2.0.0-rc9`。
> 你是执行机器兼总工。这是一个**长任务**:四个阶段,每阶段有独立规格文件、独立收口 tag。明昊已授权跑多久都行——**质量一步到位,禁止任何"差不多"**。
> **前置自检**:`git tag` 含 `stage55_rc5` 且工作区干净;不满足停下只写说明。

## §断点续跑协议（本单最高优先级规则,防上下文丢失,严格执行）

1. **开工第一件事**:建 `docs/55_总控勾选.md`——四阶段全部任务列成勾选清单(任务|完成判据|证据路径|对应 tag),这是你的外置大脑。
2. **每完成一项立即**:打勾+写一行证据路径进总控文件;**每个阶段收口立即**:commit+打该阶段 tag+在总控文件记「阶段N完成@tag」。
3. **恢复规则**:本 goal 无论何时被重跑(新会话/中断续跑),第一步=读 `docs/55_总控勾选.md` + `git tag`,**从第一个未完成阶段继续;已打 tag 的阶段严禁重做**。
4. **上下文纪律**:长输出(依赖图/审计表/vulture 清单/覆盖率)一律落盘只引用路径;阶段之间不靠记忆传递信息,一切以磁盘(总控文件/tag/交付报告)为准。
5. **阶段边界=安全点**:若上下文吃紧,在当前阶段收口(verify 全绿+commit+tag+总控更新)后干净停下,在总控文件末尾写「续跑指引:从阶段N+1开始」——明昊重贴本 goal 即无损续。

## 四阶段（按序执行,每阶段规格=对应任务书文件,执行前先通读该文件全文）

| 阶段 | 规格文件（必读,按它干活） | 内容一句话 | 收口 tag |
|---|---|---|---|
| **A** | `docs/20260719_任务书54.12_人审二轮修复与代码卫生.md` | 全站自查自修循环+人审二轮 16 项(R-00~R-15)+ruff 清零 | `stage55_rc6` |
| **B** | `docs/20260719_任务书54.13_工程完美收官.md` | 十项量化工程指标(M1~M10)+胖文件拆分+团队收尾五件 | `stage55_rc7` |
| **C** | `docs/20260719_任务书54.15_费用口径统一与热力图修缮.md` | 费用两图口径统一(剔成本大类)+热力图遮挡+tooltip 全站复扫 | `stage55_rc8` |
| **D** | 本文件 §阶段D | 终局封板复验+交接包终版 | `stage55_final` |

各阶段规格文件里的前置自检以**本单的阶段顺序为准**(A 的前置=rc5;B=rc6;C=rc7);规格文件内的版本号/交付报告要求照做。**已被 54.14(rc5)顺手修好的项:亲手复测后在报告标「已由 54.14 修复·复测通过」勾过,不重复施工**(A 阶段文件里有同款说明,遵守)。

## §阶段 D·终局封板（A/B/C 全收口后执行）

1. **全量复验**(A/B/C 改动大,一切结论重新拿):`sh tests/run_verify.sh; echo EXIT:$?` EXIT:0(禁 `| tail`);`KANBAN_OFFLINE=1 python run.py` EXIT:0;perf_check 全项达标;console 零 error;三内核冒烟;深浅色×整体/BU/管理端×1440/375 抽 8 屏截图。
2. **零散补漏**(此前人审提过但散落的,一并收掉):① 设置保存失败提示把 `TypeError: Failed to fetch` 原文翻译成「服务暂时不可达,请稍后重试」类友好文案(全站 fetch 错误提示统一);② 热力图修缮后与折线图并排截图,在交付报告给明昊出「热力图去留」建议(只建议,不删)。
3. **文档终态**:CHANGELOG 汇总 A~D 全部条目;全部软工图/README/接口清单/schema 文档/用户手册与终态零出入;需求追溯矩阵覆盖到本单;`git diff` 抽查文档声明与磁盘一致。
4. **上线交接包终版**:人审指南(按终态重写要看什么,含热力图/新拆分/口径变化点)、部署执行单、发布说明、封板复验报告(首行 `结论：终局封板通过,交接明昊人审` 或如实写失败原因);五阶段「待明昊拍板」全量汇总零遗漏。
5. **版本封板**:`2.0.0-rc9` 三处一致;tag `stage55_final`;工作区干净;只 commit 不 push。

## 总铁律（全阶段有效）

- 口径计算区=禁区(阶段C 的两图白名单过滤是唯一授权例外);golden/32 周期红线零未授权 diff;数字变了先回滚再想。
- 真实数据/密钥/内网 IP 不进 git;判绿只认真实退出码;每交互/视觉项附 Playwright 真证据。
- 每阶段收口自派 skeptic 子代理只读复核,意见进该阶段交付报告。
- 发现规格文件之间矛盾:以本文件为准,矛盾点记入交付报告,不停工。

## 非目标
push(明昊人审签字后 Claude 执行)、部署机操作、新功能、删热力图。
