# 批次 4 · 质量地基（2026-07-16）

对照 `方案与文档/软件工程文档/2_设计/20260716_软工规范落地包_配置与依据.md` 落地。

## 已提交

| 项 | 路径 | 出处 / 版本 |
|----|------|-------------|
| ruff format 全库基线 | `ruff.toml` + 全库 format | ruff **0.15.21** MIT |
| ruff lint 安全 fix | `--fix` 清 228 项；余 **87** 项记债（F841/B904/B905 等） | 同上 |
| gitleaks 定制 | `.gitleaks.toml` | gitleaks **v8.30.1** MIT；铁律6 UNC allowlist `config.json` |
| pre-commit | `.pre-commit-config.yaml` | pre-commit 4.6.0；ruff/gitleaks/hooks/conventional |
| CI 防假绿 | `.github/workflows/verify.yml` | 缺 `tests/fixtures/ci_data` 显式 fail；播种后 `run_verify` |
| 合成 fixtures | `tests/fixtures/ci_data/*.xlsx` | 6 源极小合成表，全假人名金额 |
| coverage | `.coveragerc` | coverage **7.15.1** Apache-2.0；不设 % KPI |
| AGENTS.md | 仓库根 | https://agents.md 精简同步 CLAUDE |

## 开发机安装（不进运行时）

```bash
python -m pip install pre-commit==4.6.0 ruff==0.15.21 coverage==7.15.1 \
  -i https://pypi.tuna.tsinghua.edu.cn/simple
pre-commit install
pre-commit install --hook-type commit-msg
```

## 覆盖率实测（示例）

```bash
KANBAN_OFFLINE=1 .venv/bin/python -m coverage run --source=src -m unittest discover -s tests -p 'test_*.py'
.venv/bin/python -m coverage report --skip-empty | tee output/coverage_report.txt
```

（html 报告勿提交含真实数路径。）

## 未做 / 债

- ruff 余 87 项不在本批强清（避免行为风险）
- pre-commit 需开发机本地 `install`；部署机不装 hook
- gitleaks 二进制未钉进仓库（CI 经 pre-commit 钩子拉取）
