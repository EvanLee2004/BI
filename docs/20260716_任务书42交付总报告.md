# 任务书42 交付总报告

> 仓库：`程序/看板正式程序/` · 基线 `3f55670` · 产品 **v1.6.0-beta**  
> 纪律：只 commit 不 push · 每阶段 `KANBAN_OFFLINE=1 sh tests/run_verify.sh` EXIT:0 · 32 周期红线一分不动

---

## 总览

| 阶段 | 内容 | 主 commit | 证据 |
|------|------|-----------|------|
| 一 | Ubuntu 22.04 平台移植（任务书40 A–F） | `3ddd4be` | `tests/test_linux_deploy` + bash -n + 手册/MADR |
| 二 | 看端明细口径 + 尺寸美化（任务书41 A–F） | `a37cd66` | `tests/test_task41` + 相关守卫改写 |
| 三 | 版本 1.6.0-beta + 文档全量刷新 | （本报告所在提交） | VERSION + version.py + README/CHANGELOG/CLAUDE… |
| 四 | 全库 code review 循环 | 见下节 | 连续 2 轮零新缺陷终止 |
| 五 | 终检 + 起服 | — | localhost:8018 200 |

---

## 阶段一 · Ubuntu 移植

### 交付物
- `deploy/linux/kanban.service` · `start_with_rollback.sh` · `register_schedule.sh`
- `server.sync_schedule` Linux cron 哨兵分支（Windows schtasks 保留）
- `docs/Ubuntu部署手册.md` · `docs/opencode部署提示词_Ubuntu_20260716.md`
- `docs/madr/0001_cron_vs_timer.md` · `0002_python_version_ubuntu22.md`
- `docs/20260716_平台假设审计_任务书40.md`
- Windows 手册标 **legacy**

### 业务零变化
- profit / render 业务算账 / ingest 清洗未改口径；fetch 仅补 POSIX 提示
- 阶段一后 golden/32 红线原样绿

### 仅部署机可验
1. `systemctl enable --now kanban` 与 journald  
2. 真实 CIFS `mount -a` 与掉挂载体检黄  
3. 用户 crontab 到点 `--scheduled`  
4. `systemd-analyze verify`  
5. ufw 外访 8018  
6. Playwright 中文 PNG  

---

## 阶段二 · 看端口径与美化

### 需求 × 实现

| 条目 | 实现 | 测试 |
|------|------|------|
| A KPI 放大 10~15% | `--fs-kpi:32px`（原 28）+ pad | theme 守卫 |
| B 回款卡头预算小字删 | `_budget_tag` 恒空 | test_task39 / test_budget |
| C 45/55 双血条 | `.dual-grid` 0.45fr/0.55fr | test_task37_ui |
| D 白名单+业务员 | `VIEW_EXPENSE_*` + audience | test_task41 |
| E 起止月真筛 | `month_from`/`month_to` BETWEEN | test_task41 |
| F 美化梯度 | CSS 变量统一 | 参数表见下 |

### 基线变化 ↔ 需求归因（UI golden 允许重录）

| 变化点 | 对应需求 | 数字红线 |
|--------|----------|----------|
| 回款卡头无「回款年预算…」小字 | B | 不动（侧栏年目标仍在） |
| KPI 字号/内边距 | A/F | 不动 |
| dual-grid 比例 | C | 不动 |
| 看端明细列集合 | D | 不动（展示列，非算账） |
| 月筛控件双下拉 | E | 不动 |

**活体浏览器**：本机以 TestClient+DOM/结构断言覆盖可验项；**活体留给 Claude**（管理员+BU：五卡一行、小字消失、客户卡更宽、列=白名单、Q1 真筛 1~3 月、亮暗+375px）。

### F 改前/改后参数对照

| 变量/选择器 | 改前 | 改后 |
|-------------|------|------|
| KPI 主数字 | 28px | `--fs-kpi: 32px`（+14%） |
| KPI padding | 16px 16px 14px | `--pad-kpi: 18px 18px 16px` |
| 板块顶距 `.sec` | 26px | `--gap-sec: 28px` |
| 卡间距 | 14px | `--gap-card: 14px`（统一变量） |
| dual-grid | 1fr 1fr | 0.45fr 0.55fr |
| 辅助 mut2（浅色） | #5a6a7d | #4a5a6e（对比度） |
| 表行 padding | 分散 | `--row-pad-y: 8px` |

---

## 阶段三 · 版本与文档

- `VERSION` = `1.6.0-beta`
- `version.py` 用户更新日志大白话一条（登录修复/月度下钻/整数分/gzip/费用明细与堆叠/联动筛选/Ubuntu）
- README · CHANGELOG（软工）· CLAUDE（铁律 2b/2c）· 07 HTTP · 测试说明重数 · 需求台账业务员澄清
- schema：无强制变更（08 无需改表）

---

## 阶段四 · code review 循环

### 第 1 轮 · 正确性
| 发现 | 证据 | 处置 |
|------|------|------|
| （施工中已修）BU 看端断言仍读「业务BU」列 | test_a5 / test_task37_expense_perm | 改测跟白名单，非产品缺陷 |
| 月区间参数非法 | KeyError→400 | 已有 |
| 无新增确凿缺陷 | 全量 run_verify 绿 | 进入第 2 轮 |

### 第 2 轮 · 安全（铁律10/12/16）
| 抽查 | 结果 |
|------|------|
| BU 越权 detail bu=他BU | 403（test_a5） |
| 看端不落提单人/定位键 | 白名单守卫 |
| 配置变更不落密码 | 既有 C3 测试 |
| 无新增确凿缺陷 | **连续第 1 轮零缺陷** |

### 第 3 轮 · 资源/健壮性
| 抽查 | 结果 |
|------|------|
| fetch 不可达降级 | test_linux_deploy + 既有 banner |
| cron/schtasks 失败不打断保存 | best-effort note |
| 无新增确凿缺陷 | **连续第 2 轮零缺陷 → 循环结束** |

### 待明昊拍板（汇总）
1. 部署日 CIFS 账号与最终挂载子路径（凭证永不进 git）  
2. 附加税费「收入」含税/不含税口头确认（历史遗留，本批未动）  
3. 是否要在看端导出按钮对 BU 页也默认露出（整体页已有 mlExport）  

---

## 阶段五 · 终检与起服

见报告末尾起服段（执行后填写）。

### 终检清单
- [x] run_verify EXIT:0（`run_verify_stage5.log` · EXIT:0 · 32 周期红线通过）  
- [x] 逐文件独立跑 PER_FILE_FAILS=0（`per_file_tests.log`）  
- [x] 测试文件/方法数已重数（**60** / **602**，与 `tests/test_*.py` + run_verify 登记一致）  
- [x] git 中文 commit · **不 push**  
- [x] 三种账号 TestClient 全流程（`tests/test_task42_final.py`）  
- [x] `/api/health` 无红：result=**黄**（开发机预期，见下）  
- [x] gzip：首页响应 `content-encoding: gzip`（TestClient 抽查）  
- [x] 起服 http://localhost:8018 → **200**

### `/api/health` 黄项解释（无红）
| 原因 | 说明 |
|------|------|
| 收单台账/智云四源 local_fallback | 本机不在公司内网、未挂 CIFS → 用数据目录本地副本，管道不中断 |
| 19 组定位键重复 | 源表重复行审计提示；写调整拒/重放标过期疑似，非本批引入 |

---

## 未做项（明确）
- 不 push（Claude 验收后统一推）  
- 不删 Windows 资产  
- 不引 Docker  
- 真实 systemd/CIFS/cron 活体（仅部署机）  
- 浏览器活体截图（**活体留给 Claude**；本批已用 TestClient+DOM 断言覆盖）  

### 主 commit 列表
| hash | 说明 |
|------|------|
| `3ddd4be` | feat(deploy): Ubuntu 22.04 平台移植 |
| `a37cd66` | feat(view): 看端白名单与月区间 + 尺寸美化 |
| `388a3b9` | docs(v1.6.0-beta): 版本升档 + 终检测试 |
| （本提交） | 交付总报告终检/起服段补全 |

---

## 服务交付

# 服务已起，明昊请开 http://localhost:8018 （管理端 /admin）测试

| 角色 | 账号名（不写密码） | 重点看 |
|------|-------------------|--------|
| 管理员 | `lushasha` | `/admin` 数据调整**全列**、设置、一键更新 |
| 整体 | `123`（姜总）/ `wangyanling` | `/` 费用明细**白名单**、起止月、Q1 联动、KPI 略放大、回款卡头**无**预算小字、45/55 排名卡 |
| BU | `zhengrui`（游戏）/ `yuzhanguo`（营销）/ `shiliyun`（营销） | 仅本 BU、明细无「业务BU」列、越权 403 |

本轮重点页面：`/` 板块一~五 · 板块五费用明细起止月 · `/admin` 数据调整·费用明细对照 · 各 BU 链接。

起服日志：scratch `kanban_serve.log` · PID 见 `kanban_serve.pid` · **起服后未再改业务代码**。
