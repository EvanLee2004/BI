结论：日期选择统一完成,请明昊过目

# 任务书 58 交付报告 · 2.0.0-rc12 / stage58_ui

## 做了什么

| 编号 | 内容 | 结果 |
|------|------|------|
| R-50 | 费用明细：月起/月止 → 日历起止（按**收单日期**日级）+ 查询/本月/返回本年；默认本年全年；只筛明细表；导出注明区间+口径 | ✅ |
| R-51 | 下单与回款·按时间段查询：在「返回默认（年）」左边加「本月」（当月1日～今天并查询） | ✅ |
| 收口 | VERSION 三处 2.0.0-rc12；run_verify EXIT:0；console 零 error；证据截图；用户手册；CHANGELOG；tag `stage58_ui`（不 push） | ✅ |

## 技术要点

- 后端 `query_detail` / `/api/v1/vm/ledger[+export]` 新增 `date_from`/`date_to`（YYYY-MM-DD）；与归属月 `month_from`/`month_to` 并存（管理端/其它调用兼容）。
- 收单日期库内可能是 `20260105` 或 `2026-01-05`：比较时统一压成 8 位 YYYYMMDD。
- 前端 `LedgerTable.vue` 独立日期状态，**不联动**热力/费用折线；`DailyQuery.vue` 仅加「本月」调既有 `runQuery`。

## 验证摘要

- 单元/API：`tests/test_task58_ledger_date_range.py` 全绿（同月跨日分行集、导出口径说明含区间）。
- 活体 Playwright：`tests/frontend/playwright_task58_dates.py` — 全年 1348 行 → 1/1–1/15 共 94 行 → 返回本年恢复；本月控件正确；pageerror/console error 为空。
- `sh tests/run_verify.sh` → EXIT:0（见 scratch `run_verify.log`）。
- 证据：`docs/验收证据/20260719_58/`（深/浅 × 1440/375，费用明细日期条 + 下单与回款本月）。

## 非目标（本单未做）

- push / 部署上机
- 热力/折线与明细日期联动
- 其它 UI/口径/利润计算改动
