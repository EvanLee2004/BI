# 任务书 58：费用明细日期选择统一 + 「本月」快捷按钮（上线前最后 UI 单）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；收口 tag `stage58_ui`,版本 `2.0.0-rc12`。
> **前置自检**:`git tag` 含 `stage57_gold` 且工作区干净;不满足停下只写说明。
> 你是执行机器:只做本单两项,零其他改动;明昊拍板的交互方案照做,不自由发挥。

## R-50 费用明细日期选择区改造（对齐「下单与回款」的样式）
- 现状:费用明细区是「月起/月止」两个月份输入框。
- 改为与「下单与回款·按时间段查询」**同款**:日历起止日期选择(起/止)+「查询」+「**本月**」+「返回本年」按钮(按钮顺序:查询 · 本月 · 返回本年);默认=本年全年。
  - 「本月」=当月 1 日~今天;「返回本年」=本年 1/1~12/31;
  - 筛选按`收单日期`字段真实到日;**只影响费用明细表本身**(上方热力图/折线仍按全年月度,不联动);
  - 「显示全部台账记录」口径开关与关键词筛选保持原有并存;**导出 Excel 跟随当前日期区间+口径开关**,文件内注明区间与口径;
  - 分页/行数统计随区间刷新。
- 测试:Playwright 真点(选区间→行数变化;本月→只剩当月行;返回本年→恢复;导出内容抽验与页面一致);深浅色+375 截图。

## R-51 「下单与回款·按时间段查询」加「本月」按钮
- 在「返回默认(年)」左边加「**本月**」:一键=当月 1 日~今天并触发查询;**只影响本板块**(现状语义不变);样式与现有按钮一致(全站 token)。
- 测试:真点「本月」→ 双卡区间标注为当月、数字与手选同区间一致;返回默认恢复。

## 收口
- run_verify EXIT:0(禁 `| tail`);console 零 error;两处深浅色+375 截图落 `docs/验收证据/20260719_58/`;用户手册费用明细一段补两句(新选择器+本月);CHANGELOG(仓库镜像) rc12 条目;版本三处一致;tag `stage58_ui`;只 commit 不 push。
- 交付报告 `docs/20260719_任务书58交付报告.md` 首行:`结论：日期选择统一完成,请明昊过目`。

## 非目标
push(Claude 做)、其他任何 UI/口径改动、部署。
