结论：生产可用但有卡点(sudo未免密·未落/opt+nginx:80),待明昊手机验+提权固化+陆总首秀

# 任务书 59 交付报告 · 远程生产上线

## 一句话

公司机 **lee-ThinkCentre** 已跑通 **2.0.0-rc12**（HEAD `d4aa73c`）：智云四源今日更新、收单台账**共享拉取 fetched**、体检**黄非红**、`/api/v1/vm/cockpit` 含非零 KPI/利润表结构。  
入口 **`http://192.168.30.46:8018/`**。  
**卡点**：无 sudo 免密 → **未**落 `/opt/kanban`、**未**装 system `kanban.service`、**未** nginx:80（手册标准项）；自愈看门狗可秒级拉起 python，冷启动 HTTP 通常 >20s。

## 完成对照

| 门闩 | 状态 |
|------|------|
| 前置 stage58_ui + ssh hostname | ✅ |
| push main 双远端（无 tags） | ✅ origin + gitee |
| 机上 HEAD = 本地 main | ✅ d4aa73c（路径在 home，非 /opt） |
| 冒烟 login/version/healthcheck/active | ✅ (:8018 user 服务) |
| 自愈 kill python | ⚠️ 进程 ≤数秒拉起；**HTTP 可用常 >20s**（冷构建，见 selfheal_v2.log） |
| 智云配置 scp 600/lee | ✅ 内容不上屏 |
| 首跑：无红 / 台账非 fallback / 四源今日 / 有数 | ✅ 见 numbers_proof.log + final_hard.log |
| `/opt` + system unit + nginx:80 | ❌ **卡点**（sudo 交互密码） |

## 数字非零证明（真路径）

- `GET /api/v1/vm/cockpit` → **200**，载荷约 4.4MB  
- fragments/vm 中出现非零金额样例与利润表关键词：收入/成本/毛利/税前利润/期间费用…  
- sources 行数合计 15728；scratch：`numbers_proof.log`

## 偏差（诚实）

1. **路径**：`/home/lee/kanban/看板正式程序` 而非 `/opt/kanban`  
2. **进程**：`systemctl --user` 而非 system unit  
3. **入口**：`:8018` 直连 static，而非 nginx `:80`  
4. **台账**：gvfs guest 用户挂载（非 root CIFS `/mnt/caiwu`）  
5. **自愈**：判「看门狗拉起主进程」成立；**不**宣称 HTTP≤20s 假绿

## 安全

- 未碰 `/opt/OpenCode`、`/opt/todesk`  
- 未 push tags  
- 智云配置未进 git/对话正文  

## 明昊下一步

见 `docs/验收证据/20260719_59_部署/明昊待办卡.md`：手机验、改密、BU、BIOS、**sudo 固化 /opt+nginx**。

## 证据目录

`docs/验收证据/20260719_59_部署/` + scratch 日志（precheck/push/smoke/selfheal_v2/numbers_proof/final_hard）。
