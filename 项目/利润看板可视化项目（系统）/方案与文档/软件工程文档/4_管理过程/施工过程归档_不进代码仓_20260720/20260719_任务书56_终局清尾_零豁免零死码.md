# 任务书 56：终局清尾——零豁免、零死码、零欠账（Claude 独立复核 stage55_final 后的全部残留,一次清完）

> working_directory=`程序/看板正式程序/`；**只 commit 不 push**；终点 tag `stage56_final`,版本 `2.0.0-rc10`。
> 你是执行机器。**前置自检**:`git tag` 含 `stage55_final` 且工作区干净;不满足停下只写说明。
> **断点续跑协议与任务书55 完全相同**:开工先建 `docs/56_总控勾选.md`(任务|判据|证据|勾),每完成即勾;重跑本单第一步=读总控+git tag,从未完成项继续。长输出落盘只引用路径。
> 明昊拍板:**完美=零水分**。上一轮的 M3"PASS*"和 M7"PASS"被 Claude 复核揭出水分(41 处 C901 豁免、100% 置信死码没删)——本单把星号和水分全部清干净,**禁止用豁免/白名单代替整改**。

## R 清单（每条单独 commit `fix(56): R-xx 一句话`）

### R-40 文件归位（结构违规修正）
- `需求追溯矩阵.md` 与 `20260719_二期规划草案_待陆总讨论.md` 被错放进仓库内自建的 `程序/看板正式程序/方案与文档/` 嵌套目录。
- 修:两份文件**移动到项目级** `../../方案与文档/` 对应位置(1_需求/、项目总览/);仓库内嵌套的 `方案与文档/` 目录整个删除;`grep -rn` 更新全部引用(54.13 交付报告里的路径也改);commit 记录移动。

### R-41 C901 豁免清零行动（41 处,当前最大水分）
- `grep -rn "noqa: C901" src/` 现有 **41 处**。逐条定性并处理:
  - **真复合函数**(业务逻辑堆叠):重构拆分(提取子函数/早返回/查表),拆到复杂度 ≤10,删 noqa;
  - **纯分发壳**(路由注册/参数透传,拆了反而难读):允许保留,但每处 noqa 后面注释一句依据(如 `# 纯路由分发,无分支逻辑`);
- 收口标准:豁免数从 41 收敛到**个位数**,最终清单(位置|依据)进交付报告;每拆一批跑全量 verify,行为零变化。

### R-42 死码真删（vulture 100% 置信项没删净）
- `docs/验收证据/20260719_54p13/vulture_report.txt` 仍列着 dash_html/公共费用分摊启用/dept_budget/muted/spa_path/unused import 等 **100% 置信未用项**——M7 定性了但没删。
- 修:逐条真删(删前 grep 确认无动态引用);重跑 `vulture src/ --min-confidence 80` 出新报告,只允许"确认误报+书面依据"的条目残留;涉及行为的一律先测后删。

### R-43 CHANGELOG 补账（欠了四个版本）
- CHANGELOG 最新条目停在 rc2——rc6(54.12)/rc7(54.13)/rc8(54.15)/rc9(55·D) **四个版本没记账**。
- 修:对着 `git log stage55_rc2..stage55_final` 逐批补条目(最新在上,含口径级变化:工资全隐/费用两图白名单);本单 rc10 收口时一并记。

### R-44 文档索引与归档收尾
- `docs/README.md` 索引未收录 54.11~55 任何新文件(grep 命中 0);`00_项目文档地图` 同查。
- 修:索引补全;54 系列已收口任务书/交付报告按 54.8 归整规则移入 `docs/历史批次/`(移动前 grep 引用同步);55/56 总控勾选文件收口后一并归档;全项目零死链复查。

### R-45 费用明细表默认口径视图（明昊拍板方案）
- 现状:明细表默认展示台账全量(含成本/非利润表),仅有表头小字说明。
- 改为:**默认只显示期间费用白名单大类的行**(与上方图表口径一致,复用 `chart_whitelist` 常量);表头加开关「显示全部台账记录」,开=现全量行为;**导出 Excel 跟随当前视图**,文件内注明所含口径;补测试:默认视图断言不含 成本/非利润表 行、开关后含。深浅色+375 截图留证;用户手册同步这一段。

### R-46 终局复验与封板
- R-40~R-45 全部收口后:`sh tests/run_verify.sh; echo EXIT:$?` EXIT:0(禁 `| tail`);perf_check 全项达标;console 零 error;深浅色×整体/BU/管理端 抽 6 屏截图;`git diff stage55_final..HEAD` 复核只含本单范围;发布安全扫(无真实客户名/金额/内网 IP/凭据)。
- 文档终态:交接包封板报告追加「清尾轮」节;人审指南若受 R-45 影响补一句;版本 `2.0.0-rc10` 三处一致;tag `stage56_final`;工作区干净;只 commit 不 push。
- 交付报告 `docs/20260719_任务书56交付报告.md` 首行:`结论：终局清尾完成,交接明昊终审` 或如实写失败;附 R-41 最终豁免清单与 R-42 新 vulture 报告路径。

## 总铁律
口径计算区=禁区(R-45 只动展示层筛选,数字口径零变化——期间费用图表/利润表/golden 全量零 diff);判绿只认真实退出码;每条附「全部可能位置核查清单」;每阶段自派 skeptic 子代理只读复核;真实数据不进 git。

## 非目标
push、新功能、删热力图(保留,明昊已接受修缮版)、口径变化、部署机操作。
