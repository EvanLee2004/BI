# 任务书 59 · 部署总控（断点续跑）

> 版本锚点：**2.0.0-rc12** / **stage58_ui** · HEAD `d4aa73c`  
> 远程仅 `ssh kanban-home` · 实际产品树：`/home/lee/kanban/看板正式程序`（**卡点**：sudo 阻塞故未用 `/opt/kanban`）

## 前置
- [x] `git tag` 含 `stage58_ui`
- [x] `ssh kanban-home hostname` 含 `lee-ThinkCentre`
- [x] 证据目录已建

## 步骤
- [x] 1 机况检查（磁盘/内存/时区/python≥3.12/nginx 已装）
- [x] 2 push main 双远端（diff 快扫）+ 机上 clone/对齐 HEAD（home 路径）
- [x] 3 venv + deps + playwright chromium
- [x] 4 数据目录初始化（无合成进生产）
- [x] 5 user systemd + 看门狗 enable/now（**非** system unit）
- [ ] 6 nginx 80 + nginx -t + reload — **未做·卡点 sudo**
- [x] 7 crontab（日更 + healthcheck + 备份）
- [x] 8 冒烟 D 清单（login 200 / 版本 rc12 / healthcheck 0 / is-active）— 基于 **:8018**
- [x] 9 自愈演练（kill python；进程秒级拉起；HTTP 冷启动常 >20s，见 selfheal_v2）
- [x] 10 scp 智云配置（600/lee，内容不上屏）
- [x] 11 真实「更新数据」首跑（黄/fetched/今日/vm 有数）
- [x] 12 防回归快照 + 明昊待办卡 + 交付报告（**卡点结论首行**）
- [ ] 13 `/opt/kanban` 迁移 + system kanban.service — **未做·卡点 sudo**

## 卡点记录
- sudo 无 NOPASSWD → 不能写 `/opt`、不能装 system unit、不能改 nginx:80；已用 home + user systemd + :8018 等价可用。
- 台账：gvfs guest 挂载成功；刷新后 **fetched**（非 local_fallback）。
- 自愈：HTTP 冷构建通常 >20s，不以 is-active 父 bash 冒充 HTTP 恢复。
