# 任务书 35 · 智云边界日 bug + 批次 0.5 护栏补做（2026-07-16）

**纪律**：只 commit 不 push；golden/红线不动。

## 1. 边界日（P0）

| 项 | 内容 |
|----|------|
| 根因 | `filterType=13` 实测=**严格大于** value，旧实现 value=since 丢掉 since 当天 |
| 证据 | 陆总号：项目明细 2026-01-01→5201、前一天→5205；任务 23062→23083 |
| 修法 | `value = since − 1 day`（`datetime`）；docstring 注明实测语义 |
| 测试 | `TestDateSinceFilter` + `test_since_boundary_day_included_in_filter_and_rows` |

## 2. 批次 0.5（任务书 **30 漏做 · 本次补做**）

| 护栏 | 行为 |
|------|------|
| 总条数对账 | 首页 `notGetTotal=false` 取 `count`/`total`；`len(out)≠total` → RuntimeError → 该源 fallback 失败（不静默残缺） |
| 行数骤降 | 相对 `数据/智云抓数上次行数.json`；降幅 > `zhiyun_row_drop_ratio`（默认 0.3）→ `warnings` + 管道黄，仍 fetched 写盘 |
| 测试 | `test_total_mismatch_raises`；`test_row_drop_warns_yellow_not_block` |

## 3. 同名控件观察

日期列同名 >1 → warnings（黄观察），过滤/取值仍用第一个。

## 4. 台账空行

`norm_ledger` 跳过全部业务字段空的格式化行；`TestNormLedgerEmpty`。

## 未做

- 未在本机陆总号重跑实网四表对账（验收环境再跑）；不 push 远端。
