结论：人审修复轮完成,请明昊复审

# 任务书 54.11 交付报告 · 人审修复一轮 · v2.0.0-rc2

- **日期**：2026-07-19  
- **tag**：`stage55_rc2`（只 commit 不 push）  
- **基线**：`stage55_rc1` / 2.0.0-rc1  

---

## R-01 BU 页入口 · 定性与修复

| 项 | 结论 |
|----|------|
| **定性** | **环境缺配置（链未断）**：`cockpit` → `bu_names = bu_pages.keys()` → store → `BuNav` 链路完整；`_golden_data` 原无 `BU配置.json` → `build_bu_pages` 空 → 导航整条 `v-if` 隐藏。 |
| **修复** | ① 测试/人审可用合成 BU（`tests` 自建 temp 配置；本机 `数据/BU配置.json` 真环境已有；golden 基线不常驻 BU 配置以免数字全等测试漂移）；② VM 增 `bu_config_count` / `bu_nav_hint`（有配置无分页时管理员可见提示）；③ 测试 `tests/test_task54p11_r01_bu_nav.py`。 |
| **证据** | 8018 overall 登录后 `bu_names=['示意BU甲','示意BU乙','示意BU丙']`；可点进 `/bu/示意BU甲`；`{SCRATCH}/r01_bu_nav.png` · `r01_bu_page.png`。 |
| **commit** | `fix(54p11): R-01 …` |

---

## R-02 两段式周期选择

| 项 | 结论 |
|----|------|
| **实现** | `PeriodPicker.vue` + `utils/periodKeys.ts`：tab 年/季/月/自定义；仅映射已有 `period_keys`；默认全年。 |
| **数字一致** | 抽 2026年 / Q1 / 3月：DOM 含 VM `cards_by_period[k][0].value_disp`；`{SCRATCH}/r02_period_check.json`。 |
| **深/浅/375** | 截图 `r02_period_*.png`。 |
| **commit** | `fix(54p11): R-02 …` |

---

## R-03 弹层不叠字

| 项 | 结论 |
|----|------|
| **实现** | token `--overlay-panel` / `--overlay-mask`；PL 抽屉补 `drawer-mask`；scifi-bridge 不再用半透明 `dsdk-panel-bg` 做抽屉底；EP mask/overlay 对齐。 |
| **证据** | 抽屉/弹窗/下拉打开态截图 `r03_overlay/`；测不透明度 panel≥0.95、mask∈0.35~0.6。 |
| **commit** | `fix(54p11): R-03 …` |

---

## R-04 观察项（逐条核）

| 项 | 结论 |
|----|------|
| 顶栏体检黄条截断 | **已核·可接受**：pill 截断 36 字 + `…`；**悬停 title 全文** + 点击 `health-pop` 明细全文。 |
| 管理端 iframe 下拉裁剪 | **已核·可接受**：Element Plus popper 默认 append-to-body；R-03 实色 overlay 已覆盖。 |
| 其他弹层透明 | **已由 R-03 全局覆盖**。 |

---

## 封板复验（关键项）

| 项 | 结果 |
|----|------|
| `KANBAN_OFFLINE=1 sh tests/run_verify.sh; echo EXIT:$?` | 见 `{SCRATCH}/run_verify_54p11.log` → **EXIT:0**（收口时实测） |
| perf_check | 达标记录 `{SCRATCH}/perf_check_54p11.out` |
| VERSION / 页显 / CHANGELOG | **2.0.0-rc2** · 发布候选 |

---

## 交接包更新

- `docs/上线交接包/封板复验报告.md` 追加 **rc2 修复轮**  
- `docs/上线交接包/人审指南_明昊10分钟清单.md` 补 BU 入口 + 两段式时间选择要点  

---

## 非目标

未 push、未部署、未做明昊未点名的新功能。
