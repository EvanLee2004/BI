# 任务书 54.6：全项目文档审计与补全（排队单·跑完 54.5 后执行）

> working_directory=`程序/看板正式程序/`（文档可写范围含上级 `方案与文档/软件工程文档/`，见 In/Out）；**只 commit 不 push**；tag `stage54p6`。
> 你是执行机器：按序做、不猜、不发挥。**本单只写文档，一行代码/测试/配置都不许改。**
> **诚实红线（本单第一铁律）**：文档必须**如实反映磁盘上的现状**——已建的写已建、未建的写未建、待拍板的写待拍板；**禁止把计划写成现状、禁止编造数字/日期/测试数**。每个写进文档的事实（行数、测试数、端点数、版本号）必须现场用命令查证并可复核。

## 前科条款

1. 历史交付报告出现过**把"计划做"写成"已做"**——本单每个"已完成"表述旁必须能对应磁盘证据（文件路径/命令输出），验收方会抽查。
2. 禁止为了"显得完整"而新造文档体系——**先补旧账，不开新篇**；确实缺的文档类型才新建，且要在交付报告里说明"为什么现有文档覆盖不了"。

## 必读输入（按序）

1. 项目根 `CLAUDE.md` + 根仓库 `甲骨易实习/CLAUDE.md` 维护规则 #8（软工文档清单）
2. `方案与文档/软件工程文档/` 现有四类全部文件名（先盘点再动手）
3. `git log --oneline stage54..HEAD`（54~54.5 全部批次=本次要补记的账）
4. `方案与文档/00_项目文档地图_从这里开始.md`

## 范围 In / Out

**In（只许写这些）**：`方案与文档/软件工程文档/` 四类；`程序/看板正式程序/docs/`（CHANGELOG、README、测试说明）；`方案与文档/00_项目文档地图`。
**Out（碰=作废）**：任何 `.py`/`.vue`/`.ts`/`.sh`/`config.json`/tests 代码；`_golden_data`；`progress.md` 与根 `工作日志.md`（那是 Claude/明昊的账本，你别动）；真实客户名/金额/内网 IP 不得写入任何文档。

## 执行步骤（有序勾选）

1. **盘点**：列出维护规则 #8 清单要求的每类文档 × 现状（存在/缺失/过期），产出一张审计表（进交付报告）。
2. **CHANGELOG 补账**：把 stage54 → stage54p5 每个批次一条（最新在上），内容对着 git log 和各交付报告写，不许凭记忆。
3. **测试说明更新**：`3_测试/05_测试说明_回归红线与验收.md` 对齐当前真实测试面——用例文件数、串行/并行分池、红线项，全部现场数（`ls tests/*.py | wc -l` 等命令贴证据）。
4. **概要设计"现状边界"**：`2_设计/02_概要设计_系统架构说明.md` 补 54 系列变化：SciFi kit vendor、Vue 管理端（54.4）、legacy 双壳现状、KANBAN_FRONTEND 开关。只写已发生的。
5. **README 对齐**：`程序/看板正式程序/README.md` 按"GitHub 项目总览"标准核一遍——跑法、架构图引用、当前版本号、frontend 构建步骤是否与磁盘一致；不一致的改到一致。
6. **接口清单核对**：`2_设计/07_HTTP接口清单_全端点.md` 与 `src/routes/` 实际端点逐一对照，缺的补、废的标废（不删记录，标注废止日期）。
7. **架构图全量对齐（一张也别拉下）**：`方案与文档/软件工程文档/2_设计/` 下**每一张图逐一核对现状**——系统架构、模块与组件、关键流程时序、ER 图、部署拓扑、登录权限分流、小白版运行逻辑等：54.4 已删看端 legacy 壳、管理端换 Vue SPA、domain 拆分、routes 拆模块,图里凡是画着旧结构的**必须重画/修改**（SVG 直接改源文件）;与 SVG 同名的 jpeg 导出版若无法同步更新,文件名加 `_已过期以svg为准` 后缀;每张图核对结果（改了/无需改+理由）列进审计表。
8. **文档地图更新**：`00_项目文档地图` 把本单新增/更新的文档挂进索引。
9. **每完成一步 commit 一次**（中文一句话说清补了什么账），最后打 tag `stage54p6`。

## 验收门闩

- [ ] 审计表：清单项 × 补前状态 × 补后状态，全量无遗漏
- [ ] 每个新写的数字有命令证据（贴在交付报告）
- [ ] `sh tests/run_verify.sh; echo EXIT:$?` 仍为 EXIT:0（证明没碰代码；禁 `| tail`）
- [ ] `git diff stage54p5..stage54p6 --stat` 只含 .md/.svg 文档路径
- [ ] 交付报告 `docs/20260718_任务书54.6交付报告.md`；tag `stage54p6`；只 commit 不 push

## 待拍板 / 非目标

- 发现文档之间口径互相矛盾（如两处版本号不一致）→ 以磁盘现状为准修正，并在交付报告列"矛盾清单"供人复核。
- 非目标：progress.md、工作日志、周报、给陆总的汇报件（人写）；任何代码改动。
