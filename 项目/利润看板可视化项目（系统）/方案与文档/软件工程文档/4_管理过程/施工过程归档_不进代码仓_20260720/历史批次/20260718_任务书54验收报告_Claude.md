# 任务书 54 验收报告（Claude 独立复测 · 2026-07-18）

**结论：通过（结论 a）——待明昊人审签字（= 重跑总计划 B0 门闩）后进入 B1。** 未 push（main ahead 50）。

范围：stage53..HEAD（stage54 + 补刀 commit）。判定依据：54 号 v2 任务书 + 54 号验收提示词。

## 逐项亲测

| 项 | 结果 | 证据 |
|---|---|---|
| run_verify | ✅ EXIT:0（纯净复跑，先 pkill 服务） | /tmp/v54.log |
| 逐文件（经 run_test.py） | ✅ 60+ 文件全绿 | 后台循环 EXIT:0 |
| 32 周期红线 / profit 层 | ✅ 红线绿；profit/db/money/periods 零 diff | git diff stage53..HEAD |
| golden 归因 | ✅ 唯一改动=admin_baseline.html -23 行，内容 100%=口径配置卡（caliber×6/setCardCaliber/口径 计数核对） | 纪律4 |
| A 口径卡 | ✅ 卡+JS+API 下线；引擎内核保留（红线绿即证）；MADR-0012 | grep 无 caliberLoad |
| B kit vendor | ✅ LICENSE+NOTICE 在；main.ts 引 kit css；echarts-theme.ts 从 kit 变量派生；**kit 图表组件零使用**（数据图全 ECharts） | grep |
| C 风格统一 | ✅ 整页 kit 面板壳统一（亮/暗截图目检）；费用月度=**堆叠柱+柱顶合计、只画 1~7 月** ✅；排名双条+行尾金额 ✅；PL 占满格 ✅ | docs/pixel/vue54/ + 活体 |
| C 环形占比 | ✅ 18.8%/33.2%/30.5%/16.9%/0.7%（52 补刀不回退，合计 100%） | 亮色截图+VM |
| D 去 Windows | ✅ 无 .bat；schtasks/win32 仅存守卫断言；Windows 手册删；**退出码 42+.update_rollback+start_with_rollback.sh 保留**；铁律 2c 已改写 | ls+grep |
| E 废码 | ✅ 删除清单抽核（模板碎片 grep 零引用）；禁删清单完好（legacy 壳/golden/备份/测试基建） | stage_e_deletes.txt |
| 52 修不回退 | ✅ 弹窗 fixed 视口内（boxTop=90）；PL 下钻真开；重复 id=0 | Playwright 活体 |
| legacy 保底壳 | ✅ `KANBAN_FRONTEND=legacy` 8019 起服，/login 与 / 均 200 | curl |
| 前后端分离 | ✅ vue 模式 VM 纯 JSON（51 结构未回退） | 静态+既有守卫 |
| 375px | ✅ **直载正常**（scrollWidth=375，canvas 自适应 321px） | Playwright 375 直载 |
| 发布安全 | ✅ diff 无真实密码/凭据（当前账号密码逐一比对 False）；`git ls-files 数据/` 仅 README | 精确串扫描 |

## 澄清两处"假警报"（grok 截图伪影，页面无病）

1. **收入·毛利趋势卡在 vue54 截图里空白**：活体实拍（/tmp/trend_live.png）柱/线/数字标签全部正常——整页截图时 canvas 动画未完成的时机伪影。
2. **顶栏出现在截图中部**：活体 computed `position:sticky; top:0` 正常——full-page 截图对 sticky 元素的绘制伪影。

## 小瑕疵（不拦，55 顺手修）

1. **EchartsHost 缺 resize 监听**：大窗加载后再缩窗，canvas 保持旧宽导致横向溢出（375 直载无此问题，真机不受影响）。修法=ResizeObserver 调 chart.resize()。
2. grok 截图脚本建议等 `animationDuration` 结束再拍（避免下轮再出"空图"假警报）。
3. KPI 下单主数较 7/17 快照有小幅变化（8,293.2→8,326.0 万）——`数据/` 本地真实数据被线上抓新过（数据现象非代码问题），提醒：起服务验收纪律=KANBAN_OFFLINE=1（本轮验收全程遵守）。

## 待明昊拍板（原样转述 grok 报告）

1. 历史交付报告/v1.4·v1.5 文档是否进归档夹；2. `body_by_period` deprecated 字段何时移出契约（55 删壳后顺理成章）；3. **像素主观"比 legacy 更好"= B0 人审项，等你签字**。

## 下一步

明昊 8018 人审（对照 legacy 在 8019）→ 满意则工作日志记「B0 人审通过，准许 B1」→ 按重跑总计划开 B1（任务书 55 删 legacy 壳）。
