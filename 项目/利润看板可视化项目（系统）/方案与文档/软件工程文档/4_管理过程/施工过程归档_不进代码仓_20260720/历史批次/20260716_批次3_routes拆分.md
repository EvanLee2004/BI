# 批次 3 · server.py 拆 routes（2026-07-16）

## 目标
`server.py` 只留 app 组装、会话/中间件、共享状态；HTTP 处理按域挂到 `src/routes/`。纯搬家，行为零变化。

## 行数对比（拆后）

| 文件 | 行数 |
|------|------|
| `src/server.py`（搬家前约 1896） | **733** |
| `src/routes/auth.py` | 238 |
| `src/routes/cockpit.py` | 226 |
| `src/routes/admin_pages.py` | 107 |
| `src/routes/data_api.py` | 327 |
| `src/routes/export.py` | 163 |
| `src/routes/config_api.py` | 221 |
| `src/routes/manual.py` | 431（略超 400，预算/分摊/去税同域） |
| `src/routes/register.py` | 15 |
| routes 合计 | ~1730 |

## 组装方式
`create_app` 建鉴权闭包 + `SimpleNamespace` 依赖，调用 `routes.register_all(app, d)`。

## 登录/shell 模板
- 两个登录 static（`view_login.html` / `admin_login.html`）**未合并**：文案与默认账号不同，强行合易误导。
- shell / shell-bu 仍分文件（整体 vs BU fragments URL 不同）。

## 死 SSR
- `_bu_view_html`、`_main_with_nav`：grep 零调用方 → 另 commit 删除（见下）。
