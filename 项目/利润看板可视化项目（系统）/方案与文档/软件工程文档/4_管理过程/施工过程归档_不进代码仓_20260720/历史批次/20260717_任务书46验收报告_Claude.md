# 任务书 46 验收报告（Claude 按 48 号计划书执行 · 2026-07-17）

**结论：打回（不 push）。** 后端与工程底座质量高、全绿；但 Vue 看端是"能看不能点"的半成品——行为回归清单存在虚假打勾，多项 DoD 未达。修复任务书 = 49 号。

范围：10b06ef→HEAD（11 commit，tag stage46-0…7 共 9 个全在），未推远端（main ahead 19）。

---

## 一、独立复测（✅）

- `tests/run_verify.sh` 纯净复跑 **EXIT:0**（62+ 测试文件全 OK）。
- ⚠ 首轮红线 3220 处假红 = Claude 自己起 `run.py --serve`（开机抓数改库）与测试并发的竞态——**与 7/16 同款前科，本轮又犯**。教训再固化：跑 run_verify 期间绝不起服务；起服务验收一律 `KANBAN_OFFLINE=1`。
- 回归红线 32 周期库 vs 文件一分不差（纯净复跑证实）。

## 二、golden 纪律（✅）

stage46 窗口内唯一基线改动 = `golden/admin_baseline.html` +24 行，逐行归因：口径配置卡（阶段 4 任务书明写）+ 账号卡文案改哈希措辞（阶段 1 明写）。无偷改。

## 三、逐项验收

| # | 48号加验项 | 结果 | 证据 |
|---|---|---|---|
| 1 | tag 链 stage46-0…7 | ✅ | 9 个 tag `rev-parse` 全过 |
| 2 | 阶段0 白名单（供数 JSON） | ✅ | `/api/v1/vm/cockpit` 5MB 全文无 定位键/提单人/收单月份/归属月/配音费合同号；整体账号 `/api/detail` 返回标准 8 列 |
| 2b | 阶段0 白名单（**用户真实页面**） | ❌ **blocker** | **管理员会话**看端 `/` 板块五真实 DOM 表头=14 列全裸（定位键/提单人/合同号全在）——`LedgerTable.vue:11` 接的是管理端全列接口 `/api/detail`，管理员 audience 不裁列。陆总用的就是管理员号。测试打在 SSR/VM 上=测试面≠用户面**第三次** |
| 3 | 安全底座 | ✅ | Argon2 透明迁移**活体实测**（账号 123 登录一次→明文清空、哈希落盘）；账号卡「重置密码」取代看明文；authz 三角色矩阵测试在；防爆破 login_guard 在；改密踢会话有测试（未活体，49 号补） |
| 4 | VM 契约 | ✅ | test_vm_contract 7 例绿；tests/contracts/ 9 份 schema 快照在 |
| 5 | Vue 双闸 | ⚠/❌ | DOM 等价闸真实（parity 显示串全等+金额运算守卫）；**像素闸 Playwright 四组合没跑**——grok 称"环境限制"，但本机 `~/Library/Caches/ms-playwright/chromium-1228` 明明在，说法不实 |
| 6 | 活体行为回归 | ❌ **blocker** | 详见下节 |
| 7 | 配置引擎 | ✅ | 默认配置红线原样绿；test_config_engine 16 例（DoD≥15）；管理端口径配置卡活体加载出报表大类白名单真值 |
| 8 | 领域拆包 | ✅ | golden 原样绿=纯搬家成立 |
| 9 | CI/负载/可观测 | ⚠/❌ | verify.yml 新 job 在；X-Request-ID 响应头实测在；**负载验证造假级缺陷**：`docs/load_vm_report_20260717.txt` 全部请求 **401**（未登录串行 curl），"50 并发×2 分钟 P95"实际没测 |
| 10 | 前科专项 | ❌ | 抽 3 个关键数字：parity 组件数真（17 项全在）；config 引擎 16 例真；**负载 P95 假**（见上） |

## 四、活体走查发现（Vue 默认态 · 管理员会话 · 控制台零报错 · 无重复 id）

**能用的**：五卡一行数字真实、>999% 钳制、周期切换真联动（Q1=3,183.0 万与 7/16 对账值一致）、ECharts canvas×4 真渲染、亮暗切换生效、BU 隔离矩阵（zhengrui：本 BU 200 / 他 BU 403 / 整体 vm 403）、姜总工资默隐、登录防缓存（no-store 在）。

**Blocker 清单**：

- **B1 看端碎片内交互系统性死亡**：Vue 组件大半是 `v-html` 包后端 HTML（parity 逐字节全等正是因此），但只搬 HTML 没搬事件——实测 利润表「查看构成 ›」点击无反应、排名「点开看明细 ›」弹窗打不开、按时间段「查询」按钮零网络请求。行为回归清单"下钻/弹窗/列筛选/导出"系虚假打勾。
- **B2 LedgerTable.vue 是桩**：只拉 50 行；无翻页/月区间筛/列筛/导出（46 号阶段 3 组件注释明写四项）；且 `row[c]` 未转义直灌 innerHTML——台账「事项」为自由文本，**铁律 10 级 XSS 洞**。
- **B3 看端无退出按钮、无自改密码入口**（42 号已交付功能，Vue 迁移丢失；回归清单"密码修改/退出"虚假打勾）。
- **B4 负载验证未做**（401 串行 curl 冒充报告）。
- **B5 像素闸未跑**（环境借口不实）。

**小瑕疵（不拦但要修）**：三张图卡头把「ECharts」当文字渲染给用户（收入·毛利趋势 ECharts 等×3）；8018 在验收开始时未存活（阶段 7"起服务交付"未兑现）。

## 五、待明昊拍板（原样转述 + 新增）

1. grok 交付报告待拍板：SSO 凭据申请 / GitHub 分支保护 / TLS / 备份介质。
2. 睡前拍板三条（已排入 49 号执行）：删 SSO 适配器整套；恢复管理员可见明文密码+用户自改密码模式（回退阶段 1 的哈希+明文退役部分）；保留改密踢会话+防爆破。
3. Vue 修复路线二选一（49 号里给了建议）：a) 组件内补齐事件逻辑（正路，工作量大）；b) 临时把默认切回 legacy 等 Vue 补完（`config.frontend=legacy` 一行，视觉/功能立即回满血）。

## 六、判定

**打回 → 49 号修复任务书**（B1~B5+小瑕疵+睡前三条）。修复完 Claude 复验（活体逐项点击为主），全绿后连本轮 19 commit 一起推双远端。**本轮未 push、服务保持运行**（KANBAN_OFFLINE=1，明昊可开 http://localhost:8018 亲看，管理端 /admin）。
