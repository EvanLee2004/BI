# 任务书54.1 交付报告 · B0 人审打回项视觉整改二轮（V1–V8）

- **日期**：2026-07-18
- **仓库**：`程序/看板正式程序/`
- **范围**：只 commit 不 push；tag **`stage54p1`**
- **红线**：纯前端+主题；禁后端算账/profit/golden 口径；禁改 legacy；禁引新 JS 库；管理员端 UI 不在本单

---

## 结论

| 项 | 结果 | 说明 |
|----|------|------|
| V1 区块标题对比 | ✅ | scifi-bridge 覆盖 kit 青字压青底 → 实底+深/白字+装饰线 |
| V2 五卡一排 | ✅ | `KpiCards` 挂 `kpi-5`；1440 同行，断点降列 |
| V3 利润表铺满 | ✅ | `.pl-card` flex 拉伸 + 行高加大 |
| V4 图表呼吸发光 | ✅ | `chart-fx.ts` effectScatter/shadowBlur/pieEmphasis；reduced-motion 关 |
| V5 星光背景 | ✅ | body::before 星点闪烁 + ::after 细网格；亮主题弱化 |
| V6 图表文字清晰 | ✅ | 字号≥11、对比色、数字描边；排名 left=128 |
| V7 费用多系列折线 | ✅ | ExpenseTrend 换 line；数据仍 area_*；tag「多系列折线」 |
| V8 resize+截图 | ✅ | ResizeObserver；`playwright_task54p1_pixel.py` ANIM_WAIT=1100 |
| run_verify | ✅ | 见收口证据（真实 EXIT:0） |
| 新库 | ✅ 无 | package.json 无 gsap/three 等；动效=ECharts+CSS |

---

## 逐条证据行（`docs/pixel/vue54p1/`）

| 条目 | 修改前（`before/`，= stage54 vue54 拷贝） | 修改后 | 观察 |
|------|------------------------------------------|--------|------|
| V1 | `before/v1_v5_overall_*`（标题偏浅） | `vue_overall_dark_sec.png` / `vue_overall_light_sec.png` | 编号方块实底高对比 + 装饰线 |
| V2 | `before/v2_kpi_*`（非五列） | `vue_overall_dark_kpi.png` / `compare_vue_kpi.png` | 5 卡同行；`v8_resize_after_900` 窄屏降列 |
| V3 | `before/v3_pl_*` | `vue_overall_dark_pl.png` / `compare_vue_pl.png` | 行高铺满、下钻「查看构成 ›」仍在 |
| V4 | `before/v4_v6_trend/donut/receipts/rank_*` | `vue_overall_dark_trend.png` 等 | 柱/线/环外发光可见 |
| V5 | `before/v1_v5_overall_*`（无星点层） | `vue_overall_dark_1440.png` | 网格+星点层；亮主题不脏底 |
| V6 | 同上 before 图表卡 | trend/rank/receipts 卡截图 | 轴标签与数据标签清晰可读 |
| V7 | `before/v7_expense_trend_stacked_bar_*` | `v7_expense_trend_line.png` / `vue_overall_dark_expense_trend.png` | 多系列折线+点呼吸；非堆叠柱 |
| V8 | （改前无 resize 专证） | `v8_resize_before_1600.png` / `v8_resize_after_900.png` | 缩窗后无横向溢出 |
| legacy 对照 | — | `compare_legacy_*` + `compare_vue_*` | 标准「只许比它好」目检达标 |
| 整页 | `before/overall_*_375` | `vue_overall_{dark\|light}_{1440\|375}.png` | 亮暗×宽窄 |

脚本：`tests/frontend/playwright_task54p1_pixel.py`（等动画结束再拍）。  
自测笔记：scratch `pixel_review_notes.txt`；控制台错误 0。

### 迭代截图（任务书「自测硬性要求」）

- **`iter_1/`**：首轮（亦最终轮）修后关键卡/整页拷贝。本单一次改码 + 一次 Playwright 全量截图即目检通过，**无第二轮视觉回改**，故无 `iter_2/`。
- 最终版证据仍以根目录 `vue_overall_*` / `v7_*` / `v8_*` 为准；`before/README.md` 说明基线来源。

---

## 改动文件（摘要）

| 路径 | 用途 |
|------|------|
| `frontend/src/chart-fx.ts` | V4/V6 共享动效与文字样式 |
| `frontend/src/echarts-theme.ts` | 轴/图例对比提高 |
| `frontend/src/components/charts/EchartsHost.vue` | V8 ResizeObserver |
| `frontend/src/components/KpiCards.vue` | V2 kpi-5 |
| `frontend/src/components/{Trend,Expense*,Receipts,RankingsDual}*.vue` | V4/V6/V7 option |
| `frontend/src/vendor/scifi-kit/scifi-bridge.css` | V1/V3/V5 CSS |
| `frontend/dist/**` | 重建进 git |
| `tests/test_task54_scifi.py` | 堆叠柱断言→多系列折线 |
| `tests/test_task54p1_visual.py` | V1–V8 结构守卫 |
| `tests/frontend/playwright_task54p1_pixel.py` | 像素证据 |

**未动**：`src/profit.py`、golden 数字、legacy 静态壳、后端算账、管理员端 UI、package 新依赖。

---

## 验证

```text
sh tests/run_verify.sh; echo EXIT:$?
# → EXIT:0
# 完整输出：scratch run_verify.log（含 OK  tests/test_task54p1_visual.py）
# 本机路径示例：…/implementer/run_verify.log 行含 test_task54p1_visual + ✓ 全部通过 + EXIT:0
```

---

## 下一步

- 明昊人审 `docs/pixel/vue54p1/` + 活体 :8018（对照 :8019 legacy）
- 通过后开 B1 / 任务书55（删 legacy 壳）按重跑总计划

## 人审补刀（同日 · stage54p1 后续）

| 问题 | 修复 | 证据 |
|------|------|------|
| PL 抽屉名竖排 | `.pl-drow` 两列横排 + `pl-name` | `iter_2_livefix/pl_drawer_open.png` |
| 回款图不全/缺月 | 年轴 pad 1~12；Y 轴盖峰值 | `iter_2_livefix/receipts_full.png` |
| 查询后排名样子不同 | DailyQuery 共用 `dualRankBarOption` ECharts | `daily_query_echarts.png` |
| 销售/客户表太小 | `rank-chart-host` ≥420~480px | `rankings_dual_large.png` |

