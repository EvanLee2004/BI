# 任务书40 · 全库平台假设审计表

> 扫描：`sys.platform` / `win32` / `schtasks` / 反斜杠路径 / GBK / `os.startfile` / 盘符。  
> 业务层 profit/render/ingest 清洗：**零改动**（fetch 仅补 POSIX 提示与注释）。

| 位置 | 假设 | Linux 行为 | 处置 |
|------|------|------------|------|
| `src/server.py` `_win_sync_schedule` / `SCHTASK_NAME` | Windows schtasks | 不调用 | **保留 legacy**；旁路 `sync_schedule` → Linux cron |
| `src/server.py` `sync_schedule` | 平台分支 | linux→cron；其它提示 | **本批新增** |
| `src/ingest/fetch.py` `ledger_share_path` | UNC 或路径 | Path.exists；CIFS 挂载后 POSIX | 文档+提示；降级路径回归测 |
| `config.json` `ledger_share_path` | 出厂 UNC | 设置页覆盖 POSIX | 不改出厂值（铁律19） |
| `看门狗启动.bat` 等 `*.bat` | Windows 启动/计划任务 | 不用 | **保留 legacy**；Linux 用 `deploy/linux/*` |
| `注册每日更新.bat` | schtasks 多任务 | 不用 | **保留**；Linux `register_schedule.sh` |
| `updater.py` git/pip | 平台无关 | 正常 | 无改 |
| `server.serve` 清 `.update_rollback` | 平台无关 | 正常 | 无改 |
| 读文件 `encoding=utf-8` | 显式 UTF-8 | 正常 | 抽查通过，无 GBK 硬编码 |
| `tests/bench_scale_100k.py` `darwin` | 本机性能 | 仅测试 | 不动 |
| `os.startfile` | — | **未使用** | — |
| 盘符 `D:\` | 仅 Windows 手册 | 手册 legacy | Ubuntu 手册用 `/opt/kanban` |
| Playwright/导出 PNG | 系统库+中文字体 | 需 apt | 手册 §2 清单 + `fonts-noto-cjk` |
| CI `.github/workflows/verify.yml` | ubuntu + 3.12 | 已对齐 MADR 0002 | 保持 3.12 |
| gitleaks / UNC | 出厂允许 UNC | Linux 填 POSIX | 无需为 `//ip/share` 特判放行（路径在覆盖层） |

## MADR

- `docs/madr/0001_cron_vs_timer.md` — cron 非 timer  
- `docs/madr/0002_python_version_ubuntu22.md` — Python 3.12 via deadsnakes  

## 仅部署机可验

真实 systemd / fstab CIFS / 用户 crontab 到点 / ufw 外网访问 / Playwright 中文渲染。
