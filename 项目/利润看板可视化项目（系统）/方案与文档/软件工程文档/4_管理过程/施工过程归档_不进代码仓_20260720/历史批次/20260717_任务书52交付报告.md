# 任务书 52 交付报告（2026-07-17）

**结论：50+51 验收打回项 F-1～F-6 已修。** Playwright 活体证据齐；logout 旧 cookie 401；密码出库；只 commit 不 push。tag **`stage52`**。服务：`http://localhost:8018`。

---

## 死线

| 项 | 结果 |
|----|------|
| 只动 看板正式程序/ + 软件工程文档/ | ✅ |
| 不 push | ✅ |
| 32 周期 / golden 数字 | ✅ 未改 profit/money；PL HTML SHA 固化 |
| 真退出码 | ✅ `run_verify` / `test_task52_fixes` |
| UI 证据 | ✅ Playwright 真点 DOM + 截图（非 TestClient） |

---

## F-1 弹窗 CSS

- **改**：`theme.css` 补 `.rkm-mask{position:fixed;inset:0;…flex 居中}` + 亮色 scrim；`.rkm` 面板样式。
- **Teleport**：`RankingsDual` / `ProfitStructure` 已 `to="body"`。
- **Playwright**（账号 123 登录）：点「点开看明细 ›」→  
  `position=fixed, top=0, 1440×900, parent=BODY`；截图 `docs/pixel/vue/f1_others_modal.png` / scratch 同名。  
  ESC 后 mask count=0。

## F-2 管理利润表布局

- **根因**：Vue `.pl-row` 仅「名+金额」两子节点，却套 legacy `grid 14px 1fr auto` → 名列被压成 **14px** 竖排。
- **改**：`.pl-table .pl-row { grid-template-columns: minmax(140px,1fr) auto }`；375 单列。
- **Playwright**：pl-name 宽 1440≈457px、1280≈297px（>>14）；截图 `f2_pl_1440/1280/375.png`。

## F-3 退出失效

- **改**：`accounts.bump_session_version`（「密码版本」+1 写盘）；`/api/v1/logout` 与 `/admin/logout` 调用。MADR-0011。
- **测试**：`test_view_logout_old_cookie_401` / `test_admin_logout_old_cookie_401`：login → session 200 → logout → 旧 cookie session **401**。
- 日志：`{SCRATCH}/f3_logout.log`、`test_task52_unit.log`。

## F-4 费用面积图

- **改**：`ExpenseTrend.vue` 接 `area_y_axis_ticks` + min/max/interval（`tickLabel`）；`_pack_expense_area` 裁到最后有数月份。
- **测试**：轴标签含「万」；labels≤12；前端无 `String(v)`。
- **截图**：`docs/pixel/charts/f4_area_axis.png`。

## F-5 密码出库

- 任务书50 交付报告密码表改为「见本机 `数据/看板账号.json`」。
- lushasha / 123 / zhengrui **再轮换** 12 位随机明文（只写账号文件，**不进 git/报告**）。
- 守卫：`test_docs_no_active_passwords`（轮换口令交叉 docs）。

## F-6 小瑕疵

- 负载报告更新 B1 后 P95 ~9.3s 前后对比。
- ledger JSON 去掉 `forbidden` 元数据。
- `tests/fixtures/pl_table_year_sha.txt` + 守卫。
- 像素：`f6_overall_light_1440.png`、`f6_overall_375.png`。

---

## 验证

- `python -m unittest tests.test_task52_fixes` → **OK**
- Playwright `tests/frontend/playwright_task52.py` → **PW_EXIT:0**
- `sh tests/run_verify.sh` → 见收口日志 EXIT:0（入链含 test_task52_fixes）

## 服务

`KANBAN_OFFLINE=1` → **http://localhost:8018**
