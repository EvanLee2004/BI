# 任务书 59 · 部署实录

> 逐步命令 + 真实退出码。凭据步骤只记「已 scp + 权限」，**不落文件内容**。  
> 版本锚点 **2.0.0-rc12** / **stage58_ui** · HEAD `d4aa73c`。

## 0 前置

| 检查 | 结果 |
|------|------|
| `git tag -l stage58_ui` | 有 |
| `ssh kanban-home hostname` | `lee-ThinkCentre-M755e-D182`（含 lee-ThinkCentre）EXIT:0 |
| 工作区 | 仅任务书59/部署文档未提交，可施工 |

scratch：`precheck.log`

## 1 机况

| 项 | 实测 |
|----|------|
| 磁盘 | 468G 总 / 430G 可用 |
| 内存 | 14Gi 可用约 10Gi |
| 时区 | Asia/Shanghai NTP active |
| python3 | 3.14.4 ≥3.12 |
| nginx | 1.28.3 已装，默认 Welcome 占 :80 |
| /opt | 有 OpenCode、todesk（**未碰**）；无 /opt/kanban |
| 内网 IP | **192.168.30.46** |
| sudo | lee 在 sudo 组但 **无 NOPASSWD**；`sudo -n` 失败 → **无法写 /opt、改 system nginx/system systemd** |

scratch：`machine_survey.log`

## 2 push + 取码

| 步 | 命令摘要 | 退出码 |
|----|----------|--------|
| 快扫 | `git diff origin/main..main` 关键词：无真实客户名/密码进库（UNC/种子账号为允许项） | 0 |
| push origin | `git push origin main` → `3f55670..d4aa73c` | 0 |
| push gitee | `git push gitee main` → 同 | 0 |
| **未** | `git push --tags` / 其他分支 | — |
| 机上 clone | `git clone` Gitee → `/home/lee/kanban/看板正式程序` | 0 |
| HEAD | `git rev-parse HEAD` = `d4aa73c…` 与本地一致 | 0 |

**偏差**：因无 sudo，产品目录为 `/home/lee/kanban/看板正式程序`（非 `/opt/kanban`）。OpenCode/todesk 未触碰。

scratch：`push.log` `clone.log` `user_deploy_1.log`

## 3 环境

| 步 | 结果 |
|----|------|
| venv | `python3 -m venv .venv` + pip 清华源装 `requirements.txt` | OK |
| playwright | `playwright install chromium` | 已装浏览器到用户缓存 |
| 数据目录 | `数据/` 初始化；**未**拷 golden/合成库 | OK |

## 4 服务与自愈

| 项 | 结果 |
|----|------|
| 服务形态 | **user systemd** `~/.config/systemd/user/kanban.service` + `start_with_rollback.sh`，`Restart=always` |
| 绑定 | `0.0.0.0:8018` + `KANBAN_SERVE_STATIC=1`（无 root nginx 反代） |
| `systemctl --user is-active kanban` | **active** |
| nginx :80 生产反代 | **未做**（需 sudo 改 sites-enabled） |
| crontab | `register_schedule.sh` → 09:30 日更；每小时 healthcheck；03:30 数据备份 |
| 自愈 | kill `run.py` 后看门狗**立即**拉起新 python（同 cgroup）；冷启动 HTTP 约 2–3min（首次管道） |

scratch：`service_setup.log` `selfheal.log` `smoke.log`

## 5 凭据

| 步 | 结果 |
|----|------|
| scp | 本机 `数据/智云配置.json` → 机上同名 | 完成 |
| 权限 | `-rw------- lee lee`（600） | OK |
| 内容 | **未**进对话/实录/git | OK |

## 6 冒烟 D

| # | 项 | 结果 |
|---|-----|------|
| 1 | login 200 | **200** |
| 2 | VERSION | **2.0.0-rc12**（`/api/version` + 文件） |
| 3 | `bash deploy/healthcheck.sh` | **EXIT:0**（stat 比较有 bash 告警噪声，最终 OK） |
| 4 | is-active | **active** |

## 7 真实首跑硬标准

触发：`POST /api/refresh` → `last.status=ok` built_at=`2026-07-20 00:15:40`

| 标准 | 结果 |
|------|------|
| 体检无红 | **黄**（非红）— 手填为空黄、译员未来日期黄、智云双控件黄、定位键重复黄 |
| 收单台账共享直读 | **fetched**（非 local_fallback）；路径= gvfs `smb-share…/lara.zhao/收单台账.xlsx`（guest 可挂） |
| 智云四源日期=今日 | 项目明细/下单/回款/内部译员 mtime **今日** |
| 数字非零+利润表 | fragments **200** 约 4.9MB；sources 行数 5243/4241/1037/3734/**1473** |

scratch：`final_hard.log` `first_run_ok.log` `ledger_fix.log`

## 8 安全审计摘要

- 未访问 `/opt/OpenCode`、`/opt/todesk`
- 无 `rm -rf /` 类命令
- 智云配置未打印、未 commit
- 仅 push **main**，无 tags

## 9 残留 / 明昊待办

见 `明昊待办卡.md`：sudo 迁 `/opt`+nginx:80、改默认管理员密码、手机验 `http://192.168.30.46:8018/`、BIOS、BU 配置、`loginctl enable-linger lee`。

## 10 数字非零真路径证明（补证·防假绿）

命令：overall 登录后 `GET /api/v1/vm/cockpit` + `GET /api/v1/cockpit/fragments` + `GET /api/health`

| 观察 | 实测 |
|------|------|
| `/api/v1/vm/cockpit` | **200**，约 4.4MB（旧 `/api/v1/vm` 为 404，勿用） |
| 非零金额样例 | `4,300.2` `2,762.8` `1,111.2` …（≥3） |
| 利润表关键词 | 收入/成本/毛利/税前利润/期间费用/管理费用/财务费用/**均 True** |
| sources 行数合计 | 15728 |
| 证据文件 | `numbers_proof.log` |

## 11 自愈补证（诚实）

| 指标 | 实测 |
|------|------|
| kill 对象 | `run.py --serve` python 子进程（非 bash MainPID） |
| 新 python 拉起 | **3s**（`PROC_REVIVE_AT=3s`） |
| login HTTP 200 | **冷启动约 2–4 分钟**（本轮 `selfheal_v2` 90s 内未到；后续 wait 约 **22×5s≈110s** 后 200） |
| 结论 | **不**宣称 HTTP≤20s；看门狗进程自愈成立，HTTP 受首次管道影响 |

证据：`selfheal_v2.log` `selfheal_http_wait.log`
