# R-21 ~ R-26 位置清单与复验

## R-21 深浅色零刷新

| 位置 | 机制 | 结果 |
|------|------|------|
| 看端 ThemeToggle | `utils/theme.ts` applyTheme + themeMode ref | 按钮文案即时；class 即时 |
| 图表 option | `void themeMode.value` 依赖 + EchartsHost 听 `kanban-theme-change` | 色/轴即时 |
| 管理端顶栏 | AdminLayout → toggleTheme | isLight 同步 |
| 管理端内容 EP | admin.css 浅色变量补齐 + root class | 不靠整页刷新 |
| 管理端 iframe 控制台 | admin-theme → postMessage → cockpit installThemeListeners | 跨窗同步 |
| 登录页 | LoginView 同 util | 即时 |

## R-22 周期弹层

| 项 | 修复 |
|----|------|
| topbar/wrap 同 z-index:1 | bridge 拆开：topbar=`--z-topbar`(40)，wrap=1 |
| panel z-index | `--z-dropdown` + overlay-panel 背景 |
| 12 月满格 | max-height 提到 520/78vh |
| 背景 | `--overlay-panel` ≥0.95 |

## R-23 图表裁剪/比例

| 图 | 修复 |
|----|------|
| 费用月度趋势 | containLabel + 更高 grid + 容器 min-height |
| 回款情况 | 同 + 容器高度比例 |
| 收入毛利趋势 | 同 |
| 全站同族 | scifi-bridge 统一 .rc-body / trend / heat 高度 |

## R-24 折线不冲顶

| 图 | 修复 |
|----|------|
| 毛利率线 | `ratioAxisBounds` 自适应 max/min |
| 回款率线 | 同 |
| 边界 | 负值/0/超100% 由 unit 测覆盖 |

## R-25 浅色黄条

| 元素 | 修复 |
|------|------|
| .fetch-banner / .banner | `--banner-warn-*` 双套 token；浅色 #78350f on #fef3c7 |
| 管理端 admin-fetch-banner | 浅色深字 |
| 管理端 pill y/g/r | 浅色对比度 |

## R-26 热力图

| 项 | 说明 |
|----|------|
| 组件 | `ExpenseHeatmap.vue` heatmap 月份×大类 |
| 数据 | `expense.area_*` 只读，零金额运算 |
| 挂载 | App.vue + BUPage.vue 费用明细区上方 |
| 375 | 容器 min-width 横滚 |

## R-00

结构/契约测试全绿后，主题切换态纳入走查维度（themeMode 依赖已进各图）。无新增 P0/P1 编号。
