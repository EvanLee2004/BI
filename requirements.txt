# 看板正式程序 运行依赖（精确版本，2026-07-08 本机 macOS + Python 3.12 实测通过）
# 离线部署：在同版本 Python 上 pip download 这些 + 依赖 wheel，拷到部署机 pip install --no-index。
openpyxl==3.1.5
fastapi==0.139.0
uvicorn==0.50.2
requests==2.32.5   # 智云自动抓数(fetch_zhiyun)用
python-multipart==0.0.32  # FastAPI 登录表单(Form)解析用；2026-07-14 部署机实测缺它启动崩，勿删
playwright==1.61.0 # 智云账号密码自动登录(login_zhiyun)用；装完还须 playwright install chromium
# fastapi 传递依赖（pip 自动装，列出便于离线打 wheel）：starlette==1.3.1 pydantic==2.13.4

# 仅测试用（TestClient）：
# httpx==0.28.1
